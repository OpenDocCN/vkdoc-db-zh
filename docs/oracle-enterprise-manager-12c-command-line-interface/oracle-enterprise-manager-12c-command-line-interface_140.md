# --------------------------------------------------------------------------------------------------

if [ ${#1} -gt 0 ]; then
   thisSID=${1}
   SetDBUserCredential
else
   echo "命令行上未设置数据库名称"
   echo "  语法：  SetDBUserCredential [数据库名称]"
   exit 1
fi
}
```

## 与函数相关的 Shell 脚本

本节中的脚本调用或依赖于前面描述的函数。在每种情况下，函数库都会被加载，评估来自命令行的输入值，并设置函数所需的关键输入参数。这些脚本的简洁性值得注意。所有“严肃的”逻辑都存在于函数库中，因此创建新脚本时，可以将调试范围限制在新代码上。

### 示例脚本：`emcli_start_blackout.ksh`

```shell
#!/bin/ksh
