# 总结

这四张图表告诉我，虽然我曾担心在这台笔记本电脑上运行超过 10 个并发用户会话，但实际上它可以处理更多的工作负载。事实上，直到我将会话参数从 1 到 10 递增 1，改为 20 到 200 递增 10，我才达到最大值。该测试范围表明，最大吞吐量或性能下降的拐点出现在大约 180 个并发数据库会话附近。虽然 IO 和运行队列长度都达到了最大值——但我的 CPU 利用率仍低于 20%。因此，即使这台普通的笔记本电脑，通过添加额外的 SSD 磁盘来分散 IO 负载，也可以进一步扩展。



