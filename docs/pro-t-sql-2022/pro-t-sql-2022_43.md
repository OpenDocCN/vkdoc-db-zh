# SQL Server 与 T-SQL 核心概念与技术术语

## A
*   `Adhoc Query Logic`
*   `Aliasing`
*   `Aligned partitioned indexes`
*   Application rules procedure toggle functionality (`T-SQL` database objects)
*   `Application rule table`
*   Approximate numbers
*   Automated deployment advantage (application blue-green canary deployment method, `DACPACs`, dark deployment)
*   `Database deployments`
*   `DbUp`
*   Feature flags (new application, new application code, new database code, original application)
*   Removing PowerShell quality strategies (`T-SQL` code)
*   Automatic database tuning
*   Automatic Index Management
*   Automatic Plan Correction
*   `Azure Data Studio (ADS)`
*   `Azure Key Vault`
*   `Azure SQL Edge`

## B
*   `BINARY`
*   `BIT`
*   B-tree
*   Buffer cache
*   Buffer pool
*   Buffer pool extension

## C
*   Caching
*   Clean pages
*   Change Data Capture
*   Change Tracking
*   Clustered index
*   Clustered Index Scan operator

## 编码标准
*   优势 (`ANSI` standards)
*   挑战 (constraint definition, factor, foreign key, name-value pair, non-clustered indexes, normal form, primary key, quality, software developers, table size, `T-SQL`)
*   Column Selection
*   Common table expressions
*   Cost threshold of parallelism
*   Cursor

## D
*   `CustomerID`
*   `CustomerOrder`
*   Customer order history (`CustomerOrder history data`, `CustomerOrderHistoryFunc`, `CustomerOrderHistoryID`, `CustomerOrderHistoryRang`, `Customer order history status`, `CustomerOrderHistoryStatusID`, `CustomerOrderID`, `CustomerOrder` table)
*   Dirty pages
*   Data
*   Database
*   Database objects
*   Database tables
*   Data compression
*   Data definition language (`DDL`)
*   Data discovery and classification (`Azure` access/setup, adding protection policy, built-in report, data types, `SQL Server`, `Add Classification` button, `Add Classification` window, available sensitivity labels, classifying data)
*   `Data Classification Report`
*   `dbo.Customer.Address` column information type output results (sensitive data, sensitivity view, classified data)
*   `DataEncryption` certificate
*   Data growth
*   Data manipulation language (`DML`)
*   Data retention and archival
*   Data retrieval
*   Data types (`ANSI/Unicode`, Strings, `BINARY/VARBINARY`, binary strings, `BIT`, `CHAR/VARCHAR`, collation, `DATE`, `DATETIME`, `DATETIME2`, `DATETIMEOFFSET`, dates/times, `DECIMAL/NUMERIC`, exact/approximate numbers, factors, `INT`, integer, mathematical calculations, `MONEY/SMALLMONEY`, `NCHAR`, `NTEXT`, number, `REAL/FLOAT`, numbers, `SMALLINT`, `SQL Server`, storing numbers, `TIME`, transaction trucking industry use)
*   `DATE`
*   Daylight Saving Time (`DST`)
*   `dbo.CustomerOrderHistoryArchive` table
*   `dbo.CustomerOrderHistoryLog` table
*   `dbo.CustomerOrderHistoryPartition`
*   `dbo.CustomerOrderHistory` table
*   `dbo.CustomerOrder` table
*   `dbo.Customer` table
*   `dbo.GetCustomer`
*   `dbo.ProductHistory` table
*   `dbo.Product` table
*   `dbo.vwCustomerOrder`
*   `DECIMAL`
*   `DELETE` statements
*   Deployment functionality (source control)

## 设计 T-SQL
*   Adaptive join operator
*   Ad hoc query
*   Applications (coding methodologies)
*   Complex logic
*   Data type flexibility
*   Nest loop operators
*   Operations
*   Parameters
*   Procedures
*   `SELECT` clause
*   `SELECT` statement
*   `SQL Server` stored procedure execution
*   Stored procedures

## Deterministic encryption

## 禁用功能
*   Application (application rule table, business environment, customer table, sample data)
*   `dbo.Customer` Table (delete, customer record, dummy customer record, foreign key, hard delete, `IsActive` Column, update, inactive customers)

## Dynamic cursor

## Dynamic data masking
*   Add Column
*   Create user
*   Error messages
*   Existing column
*   Functions
*   Table creation
*   Temporarily removing
*   Schema binding
*   `TestGranularUnmasking`
*   User (`T-SQL` code, types, vendor information, masked/unmasked)
*   `Vendor` Table

## E
*   Encrypt Column option
*   Encryption advantages (column master key, `Column Selection` screen, configuration, development environment, graphical version, key, master key, options, `SQL Server`, `SSN` column, type, warning message)
*   Estimated execution plan

## 执行计划
*   Applications (caching)
*   Clustered index
*   Clustered index scan
*   Dynamic management view
*   Features (graphical)
*   Logical joins
*   Merge join
*   Non-clustered index
*   Performance reading
*   RID lookup
*   `SHOWPLAN` access
*   `SQL Server`
*   `SSMS`
*   `T-SQL`
*   `T-SQL` code
*   `XML`

## F
*   Feature flags (application creation, database code, database developers, `dbo.GetProduct`, enabling, final stored procedure, high-level process, inserting, original stored procedure, procedure, stored procedure, `T-SQL` code)
*   `FLOAT`
*   Forward-only cursors

## 功能
*   Challenge (designing database code)
*   `dbo.CustomerIsActive` table
*   `dbo.Customer` table
*   `dbo.CustomerXYZ` table
*   Denormalized table
*   Dynamic `SQL`
*   `IsActive` column (insert/update, `MERGE` statements)
*   Normalized table
*   Software development strategies
*   Trigger (`T-SQL` code)

## G
*   Geometry data type
*   `GitHub`
*   Graphical execution plan

## H
*   Hardening
*   `HIERARCHYID`
*   Hybrid buffer pool
*   Hybrid workloads (`CustomerOrders`, data warehouse, non-partitioned table, partitioned and non-partitioned tables, partitioned table, `T-SQL`)

## I, J
*   `IMAGE`
*   Implicit conversion
*   Inline table-valued function
*   Inline table-valued `UDFs`
*   Integration testing (business units, customer, database objects, data warehouse, design and complexity, insert the record, manual, order by product, `SQL Server`, stored procedure testing, type, transactional databases, `T-SQL` code, `Visual Studio`)

## Intelligent query processing
*   Adaptive joins
*   Batch mode on rowstore
*   Memory Grant Feedback
*   `@IsActive`
*   `IX_CustomerOrder_IsActive_CustomerID_OrderID_DateCreated`

## K
*   Kanban
*   Keyset cursor

## L
*   Ledger (action type, append-only ledger table, `ApplicationRule` Table, copying data, database, `dbo.SystemAccess` table, error message, history, information, new, updatable ledger table, system access, tampering, types, updatable ledger objects, updatable table)
*   Load testing
*   Logging (activity, application development, `CATCH` block, change tracking table, `CustomerOrderID`, customer orders, data modifications, menu data, query, `SQL Server`, `SQL Server Audit`, track changes, transaction, `T-SQL`, type)
*   Logical join
*   Logical join types
*   Logical reads

## M
*   Maximum degrees of parallelism (`MAXDOP`)
*   Memory Grant Feedback
*   Merge join
*   Methodology (database developers, development environment, `SQL Server`, `T-SQL` code)
*   Migration-based deployments
*   `MONEY`
*   Multi-Statement Scalar `UDF`
*   Multi-statement table-valued functions

## N
*   Naming persistent database
*   `NCHAR`
*   Nested loop icon
*   Non-aligned partitioned indexes
*   Non-clustered index
*   Non-partitioned table
*   Non-unique aligned index
*   `NTEXT`
*   `NULL` values

## O
*   Object Explorer
*   Object relational mapping (`ORM`)
*   Online transaction processing (`OLTP`)
*   Optimization, `T-SQL` (advantage, customer orders, data pages, duration, execution plan, indexes, logical reads, non-clustered index, `OrderDetail`, partial execution plan, query store, set-based design, `SQL Server`, monitoring tools, third-party tools)
*   `OrderDate`
*   `OutdoorRecreation` (customer information, general Information, individual data record, set-based transactions, set of data, types of information)

## P
*   Page life expectancy
*   Parallelism
*   Parameters (adaptive joins, `CustomerID`, logic, products, query execution, `SQL Server`, stored procedure test)
*   Partitioned table creation (partition function, partition number)
*   Partitioned views
*   Partition elimination
*   Partition function
*   Partitioning (applications, clustered index, column, column lists, data, database code, execution plan, filegroups, key, performance, tables, view)
*   Partition scheme
*   Persistent memory (`PMEM`)
*   Physical read
*   Plan hash
*   Preceding execution plan
*   Procedural code
*   `ProductLog` Table
*   `Product` Table Update

## Q
*   Query execution plan

## R
*   Read-ahead read
*   `REAL` number
*   Relational database management system (`RDBMS`)
*   Relational databases
*   Row-by-row processing
*   Row-level security (`BLOCK PREDICATE`, country, creating a function, definition, error message, execution plan, `FILTER PREDICATE`, security policy, test user)
*   `ROWVERSION`

## S
*   Scalar functions
*   Scalar `UDFs` (execution plan, `SQL Server`, `T-SQL` code)
*   `SELECT` statements
*   Set-based design (data, database engine, data retrieval, data sets, identification terms, writing code, inserting data, sample database, `SeeOutdoorRecreation`, updating data, process)
*   Set-based transactions
*   `ShipDate` column
*   Sliding window partition (check constraints, count of records, `CustomerOrderHistory`, `CustomerOrderHistoryArchive`, `dbo.CustomerOrderActive`, `dbo.CustomerOrderHistory`, managing customer orders, merging partitions, split partitions, truncating a table, view)
*   `SMALLDATETIME`
*   `SMALLINT`
*   Social security number
*   Source control (add stored procedures, advantage, application development, `Azure Data Studio`, changes to source control, database code, database objects, database projects, `Database Projects` window, database project, visible databases, `GitHub`, `IDE`, initial repository setup, local repository, main branch, modifications, new database project, object type, `OutdoorRecreation` database, `Outdoor Recreation` workspace, process, `ProTSQL2022` repository, Queues and User-Defined Types (`CLR`), rollback changes, schema, `Schema Compare` window, syncing changes, third-party tools, tools, `T-SQL` code, update project from database window, uses)
*   Spatial geometry types
*   `SPLIT RANGE` code
*   Sprint/sprint cycle
*   `SQL Server` (buffer pool, buffer pool extension, caching, `CPU`, parallelism, as `RDBMS`, speed and versatility, memory, storage, stored procedures, table variables/temporary tables, write queries)
*   `SQL Server 2014`
*   `SQL Server 2017`
*   `SQL Server 2019`
*   `SQL Server Data Tools (SSDT)`
*   `SQL Server Management Studio (SSMS)`
*   `SQL Server Reporting Service (SSRS)`
*   `SQL_VARIANT`
*   Standardization, `T-SQL` (capitalization, common table expression, formatting, goal, `INSERT` statement, `ORDER BY` statement, performance tuning, queries, pre-existing code, purpose, `ROLLBACK`/`SQL Server`, `SELECT` clause, `SELECT` statement, troubleshoot, `T-SQL` code, user-defined database objects, user-defined table type, `WHERE` clause)
*   State-based deployment
*   State-based migrations
*   Static cursor
*   Storage cost
*   Storage engine
*   Stored procedure
*   Stored procedure execution
*   Stored procedure name
*   Styles of development (Kanban, Scrum method)

## Switching partitions
*   Add check constraints (check constraint, count of records, count of records per table, `CustomerOrderHistoryArchive` Table, `dbo.CustomerOrderHistory` table, `dbo.CustomerOrderNonPartition`, moving data, non-partitioned table, number of records, partitioned table, partition function, partition scheme, `PRIMARY` filegroup, query, `SQL Server`)

## T
*   Table data type
*   Table-valued function
*   Table-valued parameter
*   `tempdb`
*   Temporary objects
*   Temporary stored procedure
*   Temporary tables (advantages, global, local, persistent)
*   Testing (database code, integration testing, `SeeIntegration testing`, static code analysis, type of load, unit, `SeeUnit testing`)
*   `TEXT`
*   `TIME` (`AM` and `PM` format, range)
*   `TINYINT`
*   Transactional data (data warehouse, `Myra` customer orders, nesting views, `OLTP` applications, order informaiton, recipe ingredients, view, `SELECT` statements, `SQL Server`, `stacy` customer orders, type of behavior)
*   Transparent Data Encryption (`TDE`)
*   Triggers (`AFTER`, `Logon`, `T-SQL` code, code execution, database objects, formatting, formatting standard, performance)
*   Implicit conversion (`NOLOCK`, `RECOMPILE`, sargeable, selecting necessary data, `SET NOCOUNT ON`, queries, usability, `BETWEEN`, case statements, `CAST/CONVERT`, column definition, cursors, linked servers, `ORDER BY`, stored procedure parameters, `TRY… CATCH`, `UNION`)
*   Types of deployment (branching, database code, environment, frequency, gated deployment methods, migration-based deployments, rollback strategy, state-based migrations)

## U
*   Unicode values
*   `UNIQUEIDENTIFIER`
*   Unit testing (code, conditions, connection string, creation, customers, active/inactive, database schema, dialog box, `Empty ResultSet`, failed unit test, implementation, manually running, original stored procedure, passed unit test, `ProTSQL2022-VS` project, sample active customers, sample of all customers, source control, stored procedure, test-driven design, `T-SQL` code, types, `Visual Studio`)
*   User-defined functions (`UDFs`)
*   User-defined table type advantages
*   User-defined views

## V, W
*   Variables and Hard-Coded Values
*   View advantages (creating, indexed, performance, performs *vs*. running, query execution plan, users, version)
*   `Visual Studio`

## X, Y, Z
*   `XML`
