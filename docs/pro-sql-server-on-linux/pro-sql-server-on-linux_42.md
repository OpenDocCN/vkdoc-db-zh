# 作用域配置

从 SQL Server 2016 开始，新增的一项实用功能是能够在数据库级别设置配置选项，而这些选项以前仅可在实例级别或通过跟踪标志进行设置。

例如，服务器配置选项 `最大并行度` 可以在数据库级别使用 `ALTER DATABASE SCOPED CONFIGURATION SET MAXDOP = <value>` 进行覆盖。（注意：`最大并行度` 也是一个可以通过查询提示在查询级别进行配置的选项。）

