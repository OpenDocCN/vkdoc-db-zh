# 第 6 章 ■ 在 PHP 中使用 SQLite

## 总结

本章展示了连接到 SQLite 数据库所涉及的基本步骤。

1.  指定要使用的数据库。
2.  创建用于检索数据的查询。
3.  运行查询。
4.  从查询结果中获取数据。
5.  在代码中使用结果。

这些步骤适用于 SQLite，但也适用于你用来检索 SQLite 数据的其他语言和框架（有时术语略有不同，语法肯定不同）。此外，这些相同的基本步骤也适用于 SQLite 以外的数据库。这是从数据库检索数据的基本范式。

后续章节将展示如何使用 PHP 以外的其他语言来建立 SQLite 数据库连接。

