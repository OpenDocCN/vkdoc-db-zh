# 索引

## A

`all[ ]`

`anchors[ ]`

`Apache.exe` 可执行文件

Apache 网络服务器

-   管理访问权限
-   `C:Apache`
-   CGI-BIN 位置
-   配置指令

`DBI.pm`

-   定义

DNS 服务器

-   下载/安装二进制文件
-   功能
-   错误处理
-   `httpd.conf` 文件
-   安装选项
-   安装进度窗口
-   操作系统，变更
-   运行时配置指令
-   `ServerName` 指令
-   `ServerRoot` 指令
-   停止/重启
-   Windows 操作系统，启动

`applets[ ]`

`ASC/DESC` 子句

`autoflush()` 函数

`AUTO_INCREMENT` 列

`AUTO_INCREMENT` id 列

## B

反引号运算符

测试版

`bgChange()` 函数

`BINARY` 标志

`BLOB` 列类型

## C

CGI-BIN 位置

`CGI::Carp` 模块

`CGI` 模块

`CGI.pm`

CGI 入门

`CHAR`

字符串

`children[ ]`

`classes[ ]`

`@col1`

`@col2`

`@col3`

常见的 shebang

综合 Perl 档案网络 (CPAN)

`@contentArray`

`content.pl`

`$count` 变量

`CPAN.bat`

`create.pl`

`CSSproperty`

CSS 样式规则

## D

数据库驱动的菜单系统

数据库驱动的解决方案

数据库驱动的 Web 开发

-   构建结束数组
-   日期和时间格式化
-   收集所有信息
-   管理图像和文件
-   每用户使用统计
-   行删除
-   表删除
-   上传文件
-   用户名和密码维护

数据库驱动的网站

-   带宽限制
-   内容区域
-   电子邮件
-   演变，数据库驱动方法
-   FTP 客户端
-   硬盘空间
-   `.htaccess` 文件
-   使用 `printenv.pl` 的路径语句
-   Perl 模块
-   Plesk/cPanel
-   经销商
-   搜索功能
-   SSL
-   URL
-   使用数据库
-   用户名和密码

`@date_raw` 数组

`DBD.pm` 模块

`DBI.pm` 模块

`DBI.pm` `prepare()` 方法

专用服务器

数字时钟

`@dirlist` 数组

磁盘条带化

DNS 服务器

文档对象模型 (DOM)


访问数组元素

访问 CSS 规则

文档部分

层次树

ID 和 CLASS 选择器

规范

语句

W3C

`do()` 方法

DOM 语言

DOM 语句

双引号操作符 (`qq{}`)

下拉菜单

开始代码块四

开始代码块二

代码块一

代码块三

`create.pl`

CSS 规则

`menu.html`

`page.cgi`

`populate.pl`

E

elementID

`embeds[]`

枚举/集合列类型

错误处理

`execute()` 函数

`execute()` 方法

外部内容模板

F

`fetchrow_array()` 方法

`fetchrow()` 方法

文件传输协议 (FTP) 客户端

FileZilla

`filters[]`

浮点列类型

浮点数

`Foreach()` 循环

`forms[]`

`frames[]`

G

`getElementById()` 方法

GET 方法

H

`.htaccess` 文件

HTML 代码

`$htmlContent` 变量

HTML 标记

`httpd.conf` 文件

超文本传输协议 (HTTP)

GET 方法

POST 方法

I

`$id1` 变量

`$id2` 变量

id 列

`ids[]`

iFrame

`Image::Magick` 对象

图像预加载

`images[]`

innerHTML 属性

整数列类型

互联网协议 (IP)

网络套接字编程

进程间通信

反引号方法

双向通信

`exec` 函数

`handler_sub` 子程序

使用 Perl

进程

可重入

标准输入的文件描述符

标准输出和标准错误输出

`@thisarray`

Unix/Linux 机器

`IO::Socket::INET`

IP 地址

J, K

JavaScript 概念

背景颜色，提交按钮

按钮上的背景图像

改变

INPUT 字段的背景颜色

表格元素的背景颜色

背景图像

INPUT 字段的背景图像

图像位置

图像大小

图像源

图像查看器

INPUT 字段的文本颜色

文本大小属性

点击驱动的横向滑动菜单

文本着色

图像的描述框

描述菜单

电子时钟

下拉列表，背景颜色

下拉菜单

innerHTML 属性

内嵌和外凸放置的按钮

onLoad 事件触发器

页面元素定位

预加载并存储图像

返回光标坐标

选择 HTML 表单中的所有复选框

横向滑动菜单

切换图像

Textarea 元素，背景颜色变化

Textarea 元素，插入背景图像

文本颜色变化，下拉列表

文本跟随光标

使用 onLoad 和 onUnload

使用 onMouseMove

可见和隐藏的页面元素

L

`length()` 方法

许可协议

`links[]`

Linux `chmod` 命令

Linux 操作系统

已加载的链接

LocalAddr

LocalPort

M

msi 包

MySQL 列

列类型

BLOB

字符串

枚举和集合

浮点

整数列类型

时间列类型

收集内容

推入数组

MySQL 数据库

MySQL 数据库服务器

测试版

命令行

定义

下载二进制文件

安装

添加用户

认证方法

配置偏好

配置方案

自定义安装

手册和工具

选项

软件包

过程

产品配置区域

产品

设置管理员用户名/密码

设置网络/选项

安装类型

软件

系统要求

安装过程

MySQL 文档

MySQL 查询

N

基于名称的服务器地址

`nameon()` 函数

`newColor()` 函数

O

常用的 `style.color` 语句

`$oneContentElement`

onload 事件处理器

onMouseMove

onUnload 事件处理器

`operator (.)` 函数

P, Q

`page.cgi`

`param()` 方法

密码

路径语句

PeerPort

Perl

`DBI.pm`

安装，模块

现代网站

模块

和 MySQL

`fetchrow_array()`

`fetchrow()` 方法

`foreach` 循环

从多个数组插入许多数据库项

从一个数组插入许多数据库项

三步法

两步法

WHERE 子句

shebang

Perl 和 MySQL 函数回顾

`foreach()` 循环

循环

表创建

`while()` 循环

Perl 反引号 (`` ` ``) 操作符

Perl `CGI.pm` 模块

Perl CGI 程序

`CPAN.bat`

`PPM`

Perl 通用网关接口 (CGI) 模块

`CGI::Carp` 模块

`content.pl`

错误处理

HTML 内容

HTTP

IP 地址

`param()` 方法

Perl `CGI.pm` 模块

向浏览器打印动态内容

Perl DBI 模块

`Perl.exe`

Perl 模块

Perl .msi 安装包

Perl 包管理器 (`PPM`)

Perl `reverse()` 函数

Perl 服务器

`CPAN.bat`

`C:Perl`

内联语言

安装

模块

Windows 操作系统

Perl 套接字

`accept()` 方法

`add()` 方法

`can_read` 方法

通信过程

互联网

`IO::Select` 模块

`IO::Socket`

`IO::Socket::INET`

单套接字方法

操作系统

双套接字方法

Perl 服务器

`CPAN.bat`

安装 `DBD`

Plesk/cPanel

`plugins[]`

`plugins[]` 数组

`populate.pl` 脚本

POST 方法

`PPM.bat`

`prepare()` 函数

`prepare()` 方法

`printenv.pl`

Proto

R

RAID

代理商

`reverse()` 方法

运行时配置指令

S

ScriptAlias 位置

`scripts[]`

搜索功能

`sendmail`

共享服务器计划

shebang

`split()` 函数

`@split_text` 数组

`style.background` 语句

`style.borderStyle` 属性

`style.position` 语句

`stylesheets[]`

`style.visibility` 属性

T

`tags[]`

TCP/IP

`td.menu`

TEXTAREA 元素

Textarea 空格

`@thisarray`

`@this_column`

`@this_data` 数组

`$thisvariable`

跟踪用户

U

`upload()` 方法

用户名

V

VARCHAR

虚拟专用服务器 (VPS) 计划

`visible()` 方法

VPS 服务器

W, X, Y, Z

`waitpid()` 函数

网络托管

共享服务器

VPS 计划

网站地址 (URL)

网站参数

捕获参数，使用 `param()` 方法

GET 方法

id 和 id2 参数

POST 方法

处理参数

WHERE 子句

`while()` 循环

`while()` 语句
