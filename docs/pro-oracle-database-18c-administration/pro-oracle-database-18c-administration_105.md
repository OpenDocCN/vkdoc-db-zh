# D
## 数据库缓冲区高速缓存
## 数据库配置助手 (`DBCA`)
- `CREATE DATABASE` 语句
- `find` 命令
- 图形模式
- `mydb.rsp` 文件
- `OS` 命令行
- `rman` 实用程序
- 静默模式
- 实用程序
## 数据库目录对象
## 数据库环境
- `OS` 命令提示符
- 重新运行命令
- Bash shell
- 命令行编辑器
- `Ctrl+P` 和 `Ctrl+N`
- `history` 命令
- `ls -altr` 命令
- 反向搜索
- `set -o` 命令
- 上下箭头键滚动
- shell 别名
- shell 函数
- shell 脚本
- `conn.bsh`
- `dba_fcns`
- `dba_setup`
- `filesp.bsh`
- `lock.sql`
- `login.sql`
- `tbsp_chk.bsh`
- `top.sql`
- `users.sql`
- `SQL` 提示符
- `SQL` 脚本 (参见 `SQL` 脚本)
## 数据库索引
- 应用程序行为
- 位图索引创建
- 位图连接索引创建
- B 树索引创建
- 连接索引创建
- 现有索引不可见
- 外键列
- 索引确定
- 索引实现
- 表锁
- 基于函数的索引创建
- 索引创建指南
- 索引维护
- 显示 `DDL`
- 删除索引
- 监控索引
- 重建索引
- 重命名索引
- `UNUSABLE` 索引
- 不可见索引创建
- 键压缩索引创建
- 并行性
- 主动索引创建
- 响应式索引创建
- 避免重做生成
- 反向键索引创建
- 健壮性
- 表空间中的索引放置


## 索引

- `index size`
- `index type`
- 命名标准
- 创建可移植脚本
- 创建独立表空间
- 存储参数
- `ROWID`
- 创建唯一索引

## 数据库智能

- 数据库离线，不完全恢复，`参见` 不完全恢复
- 数据库在线，完全恢复，`参见` 完全恢复
- 数据库时间点恢复 (`DBPITR`)，`另请参阅` 不完全恢复
- 数据库升级助手 (`DBUA`)
- 数据库写入器写入数据块 (`DBWn`)

## 数据定义语言 (`DDL`)

## 数据字典

- 架构
- 动态性能
- 只读视图
- 静态视图
- `DBMS_STATS` 包
- 可推导文档
- 元数据
- 数据库问题
- 逻辑和物理结构
- Oracle 数据库
- 关系
- SQL 查询
- `TABLESPACE_NAME` 列
- 表空间
- 对象依赖关系
- 主键和外键
- SQL 查询
- 表行数
- 用户信息

## 数据文件

- 数据字典视图
- 离线数据文件操作
- 重新创建控制文件和操作系统命令
- SQL 和操作系统命令
- 在线数据文件操作

## 数据操作语言 (`DML`)

## 数据泵

- 附加转储文件
- 架构
- 组件
- 导出和导入信息
- Linux/Unix 系统
- `ps` 命令
- 状态表
- `COMPRESSION` 参数
- 一致的导出文件
- 数据库目录对象
- 数据导出
- 访问目录
- 创建目录对象
- 导出信息，数据库
- 数据泵作业
- 数据库警报日志
- 数据字典视图
- 交互式命令模式状态
- 日志文件
- 操作系统实用程序
- 状态表
- 数据重映射
- 内置 PL/SQL 包 `DBMS_DATAPUMP` 和 `DBMS_METADATA`
- `DDL` 文件
- `DISABLE_ARCHIVE_LOGGING` 参数
- 转储文件
- `ENCRYPTION` 参数
- 排除对象
- `expdp` 和 `impdp` 实用程序
- 导出和导入
  - 整个数据库
  - Linux 服务器
  - 方案级别
  - Solaris 服务器
  - 表级别
  - 表空间级别
  - 传输数据
- 导出作业组件
- 文件大小估算
- 过滤数据和对象
- 导入作业组件
- 交互式命令模式
  - `KILL_JOB` 命令
  - 运行作业和查看
  - `STOP_JOB` 参数
- 日志文件抑制
- 主进程
- 旧的 exp/imp 实用程序
- 并行性
- 参数文件
- PL/SQL 包
- `REUSE_DUMPFILES` 参数
- 状态表
- 实质性功能
- `TABLE_EXISTS_ACTION` 和 `CONTENT` 参数
- 表导入
- 表级压缩
- 表重命名
- 测试数据库
- 传输数据
  - 复制数据文件
- 网络导出和导入
- 用户克隆
- 实用程序
  - `VIEWS_AS_TABLES` 参数
- 工作进程

## 数据恢复顾问

## 数据类型

- 字符
  - `CHAR`
  - `NVARCHAR2` 和 `NCHAR`
  - `VARCHAR2`
- 日期/时间
- `JSON`
- `LOB`
- 数字
- `RAW`
- `ROWID`
- `DATE` 数据类型
- 日期/时间数据类型

## DBA 视图和参数

- `DBA/ALL/USER_CONSTRAINTS` 视图
- `DBA/ALL/USER_EXTENTS`
- `DBA/ALL/USER_INDEXES` 视图
- `DBA_DEPENDENCIES` 视图
- `dba_fcns` 脚本
- `DBA_ROLE_PRIVS` 视图
- `DBA_SCHEDULER_JOBS`
- `DBA_SEQUENCES` 视图
- `dba_setup` 脚本
- `DBA_SYS_PRIVS` 视图
- `DBA_TAB_PARTITIONS` 视图
- `DBA_USERS_WITH_DEFPWD` 视图
- `DB_CREATE_FILE_DEST`
- `DB_FLASHBACK_RETENTION_TARGET` 参数
- `DBMS_METADATA` 包
- `DBMS_SCHEDULER` 包
- `DBMS_SPACE` 包
- `DBMS_STATS` 包
- `DBMS_UNDO_ADV` 包
- `DB_WRITER_PROCESSES` 参数
- `DDL_LOCK_TIMEOUT` 参数
- `DDL LOGGING`
- `DEFAULT_PWD$` 视图
- 默认用户
- 延迟段创建
- `DELETE` 语句
- `DELETE NOPROMPT OBSOLETE` 命令
- `DELETE OBSOLETE` 命令
- 确定性
- `DEVDB` 数据库
- `DICT_COLUMNS` 视图
- `DICTIONARY` 视图
- `DIRECTORY` 参数
- `DISABLE` 过程
- 显示归档日志模式
- `DISTINCT` 子句
- `DML` 语句
- `DOP_JOB` 过程
- `DROP CATALOG` 命令
- 删除数据库
- `DROP SEQUENCE` 语句
- `DROP SYNONYM` 语句
- `DROP TABLE` 命令
- `DROP TABLESPACE` 语句
- `DROP USER` 语句
- `DROP VIEW` 语句
- `DUAL` 表
- 转储目录
- `DUPLICATE DATABASE` 命令

## E

- `ECHO` 命令
- `ENABLE` 过程
- `ENABLED` 列
- `ENABLE PLUGGABLE DATABASE` 子句
- 环境分离，应用程序

## Exadata Express

- `EXCLUDE` 命令
- `EXPLAIN_MVIEW` 过程
- 导出命令
- `EXTENT MANAGEMENT LOCAL` 子句

## 外部表

- 高级转换
  - `CREATE TABLE` 语句
  - 嵌入式转换
  - `EXA_INFO` 表
  - `exa_trans` 函数
  - 管道函数
  - `rec_exa_type`
- CSV 文件
  - 架构组件
  - `CREATE TABLE...ORGANIZATION EXTERNAL` 语句
  - 数据库对象创建
  - 目录对象和授权访问
  - `EXADATA_ET`
  - `ex.csv` 文件
  - 加载常规表
  - SQL 生成
  - 访问步骤
  - 查看元数据
  - 数据选择
- 定义
- 与 `SQL*Loader` 对比
- 文本文件
  - 卸载和加载数据
    - 优点
    - 组件
    - 定义
    - 目录对象


