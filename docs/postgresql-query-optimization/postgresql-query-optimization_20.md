# 数据库技术术语索引

## A

`Index A` `敏捷团队` `算法` `ANALYZE 命令` `AND 运算`
`Anti-join 执行计划 带执行计划 JOIN 操作符 NOT EXISTS 关键字 NOT IN 无执行计划`
`应用开发者` `应用开发模式` `结合律`
`ATTACH 和 DETACH 分区` `Autovacuum` `AUTOVACUUM 系统参数`

## B

`位图索引` `布尔搜索模型` `B 树索引` `内置函数` `业务逻辑`

## C

`笛卡尔积` `CASCADE 关键字` `coalesce() 函数` `代码维护`
`列转换 B 树索引 coalesce() 函数 不同搜索条件 执行计划 查询执行计划 函数式索引 last_name 列 搜索条件 搜索标准 SELECT 语句 顺序扫描 转换值 update_ts::date=CURRENT_DATE`
`关系组合 基于哈希的算法 嵌套循环 排序归并算法`
`COMMIT/ROLLBACK 命令`
`公共表表达式 (CTE) 定义 执行计划 强制物化 INSERT/DELETE/UPDATE 语句 MATERIALIZED 和 NOT MATERIALIZED 关键字 目的 示例查询 SELECT 语句`
`交换律` `比较运算符` `复杂查询` `复合类型`
`array_agg() boarding_pass_flight 函数 booking_leg_record 类型 复杂对象 函数参数 内部函数 嵌套结构 PostgreSQL 记录类型定义`
`复合索引 按 arrival_airport/scheduled_departure 搜索 用于数据检索的覆盖索引 前两列的执行计划 较低的选择性 仅索引扫描的计划`
`并发控制 高性能数据库 锁等待 MVCC SI 协议 事务 两阶段锁协议 VACUUM 操作 WAL`
`连接` `正确性`
`基于成本的优化算法` `成本模型` `覆盖索引`
`CREATE FUNCTION 命令` `CREATE FUNCTION 操作符` `CREATE FUNCTION 语句`
`CREATE PROCEDURE 命令` `CREATE TABLE 语句`
`自定义转换函数 custom_field 表`

## D

`数据访问算法 比较成本和查询 全扫描 基于索引的表访问 仅索引扫描 存储结构`
`数据库设计` `数据库开发者` `数据库 DML 函数` `数据库管理系统` `数据库对象` `数据库优化` `数据库优化器` `数据库查询` `数据库系统`
`数据定义语言 (DDL)` `数据操作函数`
`数据操作语言 (DML) 命令 并发控制 影响 执行部分 函数和索引 低级输入/输出 OLTP 系统 优化 SELECT 语句 和 vacuum`
`日期/时间转换函数` `声明式语言` `声明式查询` `声明式查询语言`
`DECLARE 子句`
`反规范化数据结构`
`分配律`
`文档`
`美元符号引用`
`DROP 命令`
`动态函数`
`动态 SQL 定义 执行计划 booking_ids 连接池 创建函数，参数 函数行为 函数调用 哈希连接 嵌套循环 规划时间 quote_literal() 函数 返回类型 select_booking_leg_country_dynamic 函数 灵活性 任意列表 预订 完整 搜索标准 函数执行 函数源代码 参数 性能 查询 搜索字段 搜索，参数 调用序列/执行时间 优化器 参见优化器 *对比* PostgreSQL SQL 注入 SQL 语句`
`最终用户界面`
`实体-属性-值 (EAV) 模型`
`等价规则 结合律 交换律 分配律`
`等效表达式`
`异常处理部分`
`过度选择条件 执行计划 过滤条件 PostgreSQL 搜索空间 短查询 带有难以优化的过滤 次优执行计划 带有查询重写的表`
`执行计划 箭头 成本 图形化表示 基于索引的逻辑计划 节点 优化 优化器 读取 排序 子计划`
`执行时间`
`EXPLAIN 命令`
`外部指标`

## F

`fillfactor 参数`
`过滤操作`
`首次提交者获胜`
`首次更新者获胜`
`灵活性 *对比* 效率和正确性`
`外部数据包装器 (FDW)`
`外键约束`
`外键`
`频繁更新`
`全文搜索`
`函数创建 美元符号引用 内部函数 过程式语言 用户定义`
`函数执行`
`函数执行内部机制`
`函数输出`
`函数重载`
`函数参数 函数 复合类型 数据操作 现代编程语言 和性能 可移植性 影响和安全性 类型依赖 用户定义数据类型`

## G

`geqo_threshold 配置`
`get_flight_by_departure_airport()`
`图形工具`
`GROUP BY`

## H

`哈希索引`
`哈希连接算法`
`堆`
`启发式方法`
`层次结构`
`混合哈希连接`

## I

`标识符`
`阻抗失配`
`命令式语言`
`命令式构建的查询`
`命令式编程语言`
`索引 行访问 避免使用索引 B 树和列转换 创建新索引 定义 哈希和连接顺序 和 “like” 操作符 部分索引 PostgreSQL 修改执行计划 防止 PostgreSQL R 树和短查询 可扩展性 结构`
`索引化 booking_jsonb 表 数据库系统 GIN 索引 问题 JSON JSONB 执行计划 GIN 索引 *对比* JSON 表创建 大型表 生产系统 稀疏索引 总结方法`
`仅索引扫描 数据检索方法`
`索引选择性`
`索引类型 CREATE INDEX 操作符 全文搜索 GIN GIST 接口`
`内部函数`
`内部指标`
`倒排索引`
`I/O 操作`

## J

`连接条件`
`连接顺序 反连接 公共表表达式 执行计划，两个半连接 禁用基于成本优化的执行计划 带两个半连接、不同选择性的执行计划 EXPLAIN 命令 强制 强制顺序扫描 join_collapse_limit JOIN 操作符 反连接 半连接 部分禁用基于成本优化 半连接 两个半连接，一个长查询`
`JSON 列`
`JSON/JSONB 数据类型`

## K

`键值模型`

## L

`Like 操作符`
`语言处理`
`语言规则`
`锁等待`
`逻辑操作`
`逻辑计划`
`长查询 复杂条件 用 OR、UNION ALL 重写 EAV 表 EXCEPT EXCEPT，执行计划 全扫描 分组条件 执行计划 先过滤再分组 最后选择 最后到子查询 哈希连接 INTERSECT INTERSECT，执行计划 连接顺序 参见连接顺序 带大结果集 多次扫描 执行计划 大型表 postgres_air 模式 多表扫描 OLAP 系统 带单行结果集 单表扫描 带复杂选择条件的查询 带 OR 集合操作 长查询`
`维护操作`
`映射复杂对象`
`物化视图 条件 创建 定义 依赖关系 优化 刷新 存储结果`
`医疗记录号 (MRN)`
`内存分配参数`
`缺失索引`
`多维搜索`
`多层次方法`
`多个索引`
`多个市场研究`
`多版本并发控制 (MVCC)`

## N

`嵌套循环算法`
`嵌套循环连接算法`
`非最优数据库设计`
`非关系型数据库系统`
`非关系型解决方案`
`无对象-关系失配 (NORM) 应用/数据库 应用开发者 应用视角 JSON 清单，传输对象 Python 类 预订合约 数据库视角 数据库代码 数据修改 过滤条件 JSON NORM-GEN 搜索条件 SELECT 子句 数据库表 数据交换 ERD 航班数据 GitHub JSON 对象定义 *对比* ORM 性能 传输对象 传输对象 层次结构`
`规范化 死理论 ER 模型 提升性能 邮寄地址 目的 USPS 自助服务亭 num_passengers(int) 函数`

## O

`面向对象编程 (OOP)`
`对象-关系阻抗失配 (ORIM)`
`对象-关系映射器 (ORM) 抽象 问题解决 定义 阻抗失配问题 实现细节 对象 ID 运行任意数据库查询的过程`
`OLTP 查询`
`OLTP 系统，函数 动态 SQL 执行能力 无显式依赖 参数化`
`单表设计`
`单表解决方案`
`联机分析处理 (OLAP) age_category 函数 年龄类别分配 age_category_dyn 函数 age_category 函数 分析查询 代码作为文本 代码 提取 创建函数 执行时间 统计报告`
`在线预订系统`
`联机事务处理 (OLTP)`
`优化 优化算法 增量更新 长查询 非增量长查询 短查询 步骤 技术`
`优化栅栏`
`优化器 预订/出发机场 booking_ids 执行计划 执行时间 次优计划`
`连接顺序`

## P

`并行执行`
`并行性`
`并行处理`
`参数调优 默认内存分配 执行计划 EXPLAIN ANALYZE EXPLAIN 命令 限制 优化 查询 shared_buffers 总执行时间 work_mem`
`部分索引`
`分区表`
`分区 描述 提升查询性能 大型数据分布 PostgreSQL 主要原因`
`模式搜索索引`
`性能指标`
`劣化`
`物理操作`
`PL/pgSQL 函数`
`PL/pgSQL 解释器`
`postgres_air 数据库`
`PostgreSQL 数据库开发者 数据库开发 文档 执行计划 函数 IT 专业人员 优化步骤 postgres_air 模式 关系理论 SELECT 语句`
`PostgreSQL 配置参数 连接和会话 数据库开发者 数据库性能 内存分配 调优`
`PostgreSQL 表达式`
`PostgreSQL 特性`
`PostgreSQL 优化器`
`预定义配置`
`预处理语句`
`过程式语言`
`积运算`
`投影运算`

## Q

`查询 转换日期 编译器 复杂 声明式 执行时间 操作符 简单 总乘客数`
`查询编译器`
`查询选择性`

## R

`random_page_cost 参数`
`范围分区`
`冗余`
`参照完整性`
`REFRESH 命令`
`关系代数`
`关系数据库`
`关系模型 数据库类型 定义 EAV FDW 层次结构 键值模型 关系操作`
`响应时间`
`负责任地 RETURNS 子句 RETURNS VOID 函数`
`R 树索引`

## S

`标量函数`
`SECURITY DEFINER 函数`
`选择规则`
`选择列表`
`选择操作符`
`选择性`
`SELECT 列表`
`SELECT 语句`
`半连接 条件 定义 执行计划 EXISTS 关键字 IN 关键字 JOIN 操作符 连接顺序`
`顺序扫描`
`会话`
`购物清单问题`
`短查询`
`简单查询`
`单表设计`
`小百分比`
`SMART 目标`
`快照隔离 (SI)`
`软件工程 架构模式`
`排序`
`排序归并算法`
`空间搜索`
`SQL 注入`
`SQL 操作符`
`SQL 查询 编译 优化`
`SQL 语句`
`存储过程 异常处理 *对比* 函数 无结果函数 事务管理`
`代理键 账户表 优势 飞机表 机场表 登机牌表 booking_leg 表 预订表 定义 额外连接 flight_id frequent_flyer_id 内部标准 MRN 乘客表 postgres_air 模式 主键 要求 唯一性`

## T

`表膨胀`
`临时表 CREATE 语句 CREATE TABLE 语句 创建时间 低效使用 中间结果 问题 存储结果 text_to_date 函数`
`转换规则`
`触发器`
`微不足道的语法错误`
`两阶段锁 并发控制协议`
`两表设计`
`两表解决方案`

## U

`唯一约束`
`唯一索引`
`批量 UPDATE/DELETE`
`用户定义转换函数`
`用户定义数据类型`
`用户定义函数`
`用户定义标量函数`

## V

`VACUUM 操作`
`视图 列转换 创建 定义 封装 执行计划 连接和搜索的顺序 性能下降 目的 查询 查询计划 SELECT 语句 表`

## W, X, Y, Z

`Web 应用`
`万维网等待`
`预写日志 (WAL)`
