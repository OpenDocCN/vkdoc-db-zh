# 索引

## A

`ABS()` 访问控制列表 (`ACL`) 活动工作负载存储库 (`AWR`) 管理网络字段 分配单元 (`AUs`) `alter griddisk` 命令 应用程序编程接口 (`APIs`) `asmcmd` 命令 异步与重叠方式 自动数据优化 (`ADO`) 数据生命周期管理 ILM 策略 参见(信息生命周期模型 (`ILM`)) 实现 与手动生命周期管理相比 自动并行度 (Auto `DOP`) 控制并行排队 `DBRM` 提示 参数 并行度 内存并行执行特性 参数设置 I/O 校准 新机制 旧机制 `PARALLEL_DEGREE_POLICY` 并行语句排队 总结 自动服务请求 (`ASR`) 自动存储管理 (`ASM`) 单元安全 `ACL` ASM 范围的安全 最佳实践 `cellkey.ora` 数据库范围的安全 移除单元安全 安全密钥存储领域 配置选项 `DBAs` Exadata 磁盘架构 单元磁盘 创建 默认策略 磁盘轨道 故障组 网格磁盘 交错 命名 `SAME` 网格磁盘 `DBFS` 磁盘组 `FlashDisk` 关键属性 `OCR` 和投票文件 隔离 存储单元访问层 存储单元 自动工作负载存储库 (`AWR`)

## B

备份与恢复过程 完全备份与恢复 增量备份 备份/Data Guard 以太网网络 `BASIC` 压缩 块更改跟踪 (`BCT`) 块更改跟踪文件 布隆过滤器

## C

缓存层 单元警报 `CELLBOOT` USB 闪存驱动器 `cellcli` 命令 `cellcli` 语法 `cellcli` 工具 `ATTRIBUTES` 关键字 配置与管理，存储单元 `DESCRIBE` 命令 `DETAIL` 关键字 Exadata 存储软件 `HELP` 命令 `LIKE` 运算符 `LIST` 命令 `ObjectType` `ORDER BY` `OS` 命令行 `SQL*Plus` 命令 `STDOUT` 创建 `WHERE` 子句 `XML` 单元详细信息文件 单元磁盘故障 单元磁盘更换 预测性故障 模拟磁盘故障 单元闪存缓存 `cellkey.ora` 文件 单元智能索引扫描 `cellsrv` 清理 `SCN` 客户端访问网络 客户端以太网网络 集群(n) 列投影 注释屏幕 压缩建议器 压缩单元 (`CU`) `computeImageMaker` 文件 计算节点布局 `CPU` 资源 I/O 性能与容量 内存资源 多机架集群 非 RAC 配置 打补丁与测试 分区机架集群 典型的 Exadata 配置 创建表作为选择 (`CTAS`) 方法 跨平台可传输表空间 (`XTTS`) 实现 设置与配置 自定义应用程序数据

## D

数据库层 数据库机器前缀 数据库资源管理器 (`DBRM`) `CDB` 资源计划 创建 可插拔数据库 调度程序 验证 组件 限制参数 监控 `ORACLE_FUNCTION` `resmgr: cpu quantum` 资源映射 视图 等待事件 数据库资源管理器 (`DBRM`) 数据库智能闪存缓存 (`DBFC`) 数据库写入器 (`DBWR`) 数据库写入器 (`DBWR`) 数据定义语言 (`DDL`) Data Guard 最大可用性 最高性能 最大保护 数据层 数据生命周期管理 数据操作语言 (`DML`) 数据模型 评分 数据泵 `COMPRESSION` 选项的优势 `COMPRESSION_ALGORITHM` `FLASHBACK_TIME`, `FLASHBACK_SCN` `FULL`, `SCHEMAS`, `TABLES` `LOGTIME` `NETWORK_LINK` `PARALLEL` 参数 `REMAP_DATAFILE` `REMAP_SCHEMA` `REMAP_TABLESPACE` `SQLFILE` `TABLE_EXISTS_ACTION` `TRANSFORM` 数据仓库 (`DW`) 设备导向的工作负载 链式行 压缩 `EFSC` 启用 提示代码 索引 分区 逐行处理 `WHERE` 子句，函数 `DBA_SEGMENTS` `DBFS_DG` `dbm01` 数据库 `DBMS_FILE_TRANSFER` `DBMS_LOGSTDBY.INSTANTIATE_TABLE` 过程 `DBMS_XPLAN.DISPLAY_CURSOR` `dcli` 工具 `all_group` `cell_group` 命令行选项 `dbs_group` 性能指标 四分之一机架 SSH 等效性 决策支持系统 (`DSS`) 定义集群 定义客户网络 并行度 (`DOP`) 诊断脚本 诊断脚本 `as.sql` `calibrate_io.sql` `check_px.sql` `comp_ratio.sql` `create_display_raw.sql` `dba_tables.sql` `display_raw.sql` `dplan.sql` `dump_block.sql` `esfc_keep_tables.sql` `flush_pool.sql` `flush_sql.sql` `fs.sql` `fsxo.sql` `fsx.sql` `gather_table_stats.sql` `get_compression_ratio.sql` `mystats.sql` `obj_by_hex.sql` `offload_percent.sql` `parmsd.sql` `parms.sql` `part_size2.sql` `pool_mem.sql` `queued_sql.sql` `report_sql_monitor.sql` `si.sql` `snapper.sql` `ss_off.sql` `ss_on.sql` `table_size.sql` `valid_events.sql` `whoami.sql` 直接路径读取 基于磁盘的备份 `DISKMON` 磁盘状态 `DISPLAY_CURSOR()` 函数 `dmesg` 命令 `DML` 性能 域名服务 (`DNS`)

## E

`ESFC`。 参见 Exadata 智能闪存缓存 (`ESFC`) 以太网端口 `ethtool` 命令 `exachk` `AUTORUN_SCHEDULE` 参数 配置检查 参数 数据库选择 文本 –get 所有参数 网格基础结构软件 MOS 说明 #1070954.1 `nextautorun` 参数 `NOTIFICATION_EMAIL` 参数 `oradw` 账户 `orahr` 账户 配置文件 报告摘要 root 密码 zip 文件 Exadata 架构 `ASM` `DBRM` 与 `IORM` `DISKMON` `LIBCELL` `RDS` 协议 `SGA` 配置选项 数据库机器 X4-8 数据库机器 X5-2 存储扩展机架 X5-2 型号 整合平台 数据库层 数据库服务器 `LVM` 快照备份 重镜像过程 诊断工具 单元警报 `sundiag.sh` 基于磁盘的备份 `DW` 设备 硬件组件 部件 磁盘 Exadata X2-2 全机架 闪存缓存 操作系统 存储服务器 X4-8 数据库服务器 X5-2 数据库服务器 历史 InfiniBand 网络 迁移 逻辑迁移 参见(逻辑迁移) 物理迁移 策略 `OLTP` 机器优化 `RMAN` 参见(恢复管理器 (`RMAN`)) 软件组件 数据库服务器 存储服务器 备用数据库 存储单元备份 `CELLBOOT` USB 闪存驱动器 外部 USB 驱动器 存储单元恢复 单元磁盘故障 单元故障 单元闪存缓存故障 系统卷故障 存储层 基于磁带的备份 Exadata 配置 绑定铜缆客户端访问网络 客户端访问网络 创建网络 `VLANs` 客户详细信息 “管理网络” 屏幕 自动服务请求字段 “自动服务请求” 屏幕 备份/Data Guard 以太网网络字段 “备份网络” 屏幕 单元警报字段 “单元警报” 屏幕 客户端以太网网络字段 “客户端以太网网络” 屏幕 集群 (n) 字段 集群 (n) 屏幕 “注释” 屏幕 “定义集群” 屏幕 定义客户网络字段 “定义客户网络” 屏幕 部署助手 “完成” 按钮 网格控制代理 字段和屏幕 “硬件选择” 屏幕 “识别计算节点操作系统并启用按需扩容” 屏幕 “InfiniBand 网络” 屏幕 `OCM` 字段和屏幕 白金配置 字段和屏幕 “机架详细信息” 屏幕 “查看并编辑详细信息” 屏幕 “查看并编辑” 屏幕 工厂配置 `applyElasticConfig.sh` `firstboot` 过程 回收磁盘空间 `ILOM` 安装要求 管理网络 `OneCommand` 文件和 Oracle 软件 执行硬件安装 专用网络 布线与供电 运行 `CheckIP` 运行 `OEDA` 运行 `OneCommand` 升级 创建新 RAC 集群 现有集群 Exadata 数据库机器 X4-8 Exadata 数据库机器 X5-2 Exadata 智能闪存缓存 (`ESFC`) `cellcli` 数据库 与 `DBFC` `DEFAULT` `ESFL` 参见(Exadata 智能闪存日志 (`ESFL`)) 闪存缓存压缩 闪存磁盘 `KEEP` `metriccurrent` 属性 `metricValue` 监控 `NONE` `OLTP` 工作负载 性能计数器 `AWR` 报告 `V$CELL` 视图 `v$sesstat` 性能视图 读取操作 I/O 路径 `WBFC` 参见(写回闪存缓存 (`WBFC`)) `WriteBack` 写入操作 I/O 路径 X4-2 存储服务器 X5-2 第一个存储服务器 Exadata 智能闪存日志 (`ESFL`) 特性 重做日志写入延迟 重做写入 I/O 路径 Exadata 存储服务器补丁 计算节点 `dbnodeupdate.sh` InfiniBand 交换机 回滚过程 `patchmgr` 输出 重启前阶段 先决条件阶段 重启阶段 回滚过程 滚动与非滚动补丁 `RAID` 设备 备用系统 `expdp` 命令 外部 USB 驱动器 提取、加载、转换 (`ELT`)

## F

快速恢复区 (`FRA`) 胖树拓扑 `fdisk` 命令 金融数据 闪存缓存 `flashcache-detail` 闪存缓存模块 `FlashDisk` `flashlog-detail` 闪存日志 `fsxo.sql` 脚本 功能配送架构

## G

千兆以太网 (`GigE`) 网格控制代理

## H

硬件选择 高可用性 (`HA`) `HR` 服务器 混合列压缩 (`HCC`) `ADO` 参见(自动数据优化 (`ADO`)) 压缩机制 `ADVCMP` 组件 `BASIC` 压缩 压缩类型与单元 `HCC` 内部原理 `OLTP` 压缩 `ORADEBUG` `ULEVEL` `UTS` `DML` 性能 预期压缩率 压缩建议器 自定义应用程序数据 金融数据 零售销售数据 电信呼叫详细数据 加载性能 Oracle 块，拆解 Oracle 存储 审查 `PCTFREE` 标准 Oracle 块格式 标准 Oracle 行格式 查询性能 限制/挑战 锁定问题 非 Exadata 平台 串行直接路径读取，禁用 单行访问 混合列压缩 (`HCC`)

## I, J, K

`imageinfo` 命令 索引层 索引组织表 (`IOT`) InfiniBand 卡 网络 网络字段 端口 信息生命周期模型 (`ILM`) 用于压缩 用于存储分层 插入为选择 (`IAS`) 方法 集成式光照管理器 (`ILOM`) 智能数据库协议 (`iDB`) 感兴趣事务列表 (`ITL`) I/O 性能—交错策略 I/O 资源管理器 (`IORM`) 架构 类别 配置类别 资源计划 跨数据库配置 `cellcli` 命令 闪存属性 限制 份额 跨数据库资源计划 数据库内资源计划 管理 指标 后台进程 数据库 `metric_iorm.pl` 概述 工作负载管理 目标 性能配置文件 可插拔数据库资源管理 配置文件

## L

Linux 操作系统 `LIST GRIDDISK` 命令 加载性能 锁字节 逻辑迁移 复制数据，数据库链接 复制表 `CTAS`/`IAS` 并行化数据加载 `TCP` 缓冲区大小 区大小 提取与加载方法 数据泵 参见(数据泵) 导出与导入 `HCC` 合并数据库 平台差异 预订购 基于复制的迁移 逻辑复制 逻辑备用 Oracle Streams 和 Golden Gate 并行加载 物理复制 选择性迁移 分阶段迁移 逻辑卷管理器 (`LVM`) 快照 恢复系统 备份 日志写入器 (`LGWR`) `lvcreate` 命令

## M

`make_cellboot_usb` 命令 `make_cellboot_usb` 脚本 `makeImageMedia.sh` 脚本 管理网络 管理服务器 (`MS`) 手动生命周期管理 `MegaCli64` 命令 `Minscn` 优化 混合工作负载系统 索引优化器参数 资源管理系统 统计信息 监控性能 SQL 监控 参见(SQL 监控) 存储单元层 参见(存储单元层监控) 系统化方法 `ms-odl.log` 多机架集群 My Oracle Support (`MOS`) `mystats.sql`

## N

网络时间协议 (`NTP`) 非 Exadata 系统 非 RAC 配置 非智能扫描 快速文件创建 `RMAN` 增量备份 不同值数量 (`NDV`)

## O

可卸载功能 卸载 定义 特性 通用版本 目标 非智能扫描 参见(非智能扫描) 参数 性能优势 智能扫描 参见(智能扫描) `offload_percent.sql` 脚本 卸载服务器 在线事务处理 (`OLTP`) 导向的工作负载 压缩 机器导向的工作负载 `ESFC` 可扩展性 写密集型工作负载 Oracle 10gR1 Oracle 集群注册表 (`OCR`) Oracle 配置管理器 (`OCM`) Oracle Exadata 部署助手 (`OEDA`) Oracle 现场服务工程师 (`FSE`) `ORACLE_HOME` Oracle 性能计数器 Oracle 的自动服务请求 (`ASR`) Oracle 会话嗅探器 `ORACLE_SID` Oracle 虚拟机 (`OVM`) `OS` 配置屏幕

## P

并行执行 (`PX`) 并行操作 自动 `DOP` 参见(自动并行度 (Auto `DOP`)) 数据库参数 存储层 故障排除 直通模式 打补丁 Exadata 存储服务器补丁 最新版本和补丁新闻 `QDPE` 参见(Exadata 季度数据库补丁 (`QDPE`)) `PAYROLL` `PCTFREE` 性能计数器 单元块 缓存层 数据层 索引层 `minscn` 优化 事务层 单元提交缓存查询 单元 `CUs` 发送压缩 单元 `CUs` 发送头部 单元 `CUs` 发送未压缩 单元闪存缓存读取命中 单元索引扫描 单元 `IO` 未压缩字节 单元快速响应会话数 单元智能 `IO` 会话数 单元物理 `IO` 字节 单元扫描 单元智能 `IO` 会话 链式行处理 块 `I/O` 人为且不现实的最坏情况 `DBMS_UTILITY` `HCC` 压缩被单元拒绝 行链接 行链接特殊性 行迁移 被单元跳过 提交缓存块 `I/O` 模式 清理 `SCN` `DBWR` 影响 `ITL` 锁字节 修改数据块 多块读取 单块 `I/O` 单块读取 事务提交缓存 单元事务找到 `EHCC` 相关计数器 Exadata 存储服务器软件用户指南 `log file sync` 等待事件 含义与解释 覆盖闪存缓存 物理读取总字节数已优化 嗅探器会话统计信息 `table fetch continued row` 表扫描 故障排除 `V$SYSSTAT` 计数器 您的会话事件 性能指标 `ASM` 层 数据库层 Oracle 性能计数器 Oracle 会话嗅探器 性能计数器 参见(性能计数器) 会话性能计数器 智能扫描 索引分支块 `iSCSI` 设备 `NFS` 挂载 性能计数器 `storage()` 谓词 `SQL` 语句 `V$CELL` 系列 `cellsrvstat` 工具 `CON_ID` 和 `CELL_TYPE` `V$CELL_OFL_THREAD_HISTORY` `V$CELL_STATE` 视图 physicaldisk-detail 文件 物理迁移 备份与恢复策略 跨平台 `TTS` 物理备用策略 可传输表空间 `VLDB` 迁移 物理备用策略 应用服务 Data Guard 重做传输服务 白金配置屏幕 可插拔数据库 (`PDB`) 迁移后任务 谓词过滤 专用网络

## Q

Exadata 季度数据库补丁 (`QDPE`) 克隆主目录 就地方法 `OPatch auto` 查询性能

## R

机架详细信息 实时 SQL 监控 `DBMS_SQLTUNE` 长时间运行查询 绑定变量 单元卸载效率 `I/O` 统计信息 受监控 `SQL` 语句执行概述 时间与等待统计信息 手动查询 受监控 `SQL` 执行 行源活动 执行计划 智能扫描时间线列 语句持续时间 恢复管理器 (`RMAN`) 单元智能增量备份 单元智能从备份恢复 重做应用 冗余 可靠数据报套接字 (`RDS`) 基于复制的迁移 逻辑备用 Oracle Streams 和 Golden Gate 并行加载 资源管理 整合 多租户 模式 服务器 控制组 配置 特性 安装 `DBRM` 参见(数据库资源管理器 (`DBRM`)) 实例限制 配置 `CPU_COUNT` 特性 过度分配 使用与结果 `IORM` 参见(I/O 资源管理器 (`IORM`)) `PGA_AGGREGATE_LIMIT` `THREADED_EXECUTION` 重启服务器 (`RS`) 零售销售数据 反向索引 参见存储索引 查看并编辑详细信息屏幕 往返时间 (`RTT`) `ROWID`

## S

`SALES` 可扩展性 `SCRATCH` `SCRATCH_DG` 选择性迁移 会话性能计数器 简单邮件传输协议 (`SMTP`) 简单网络管理协议 (`SNMP`) 模拟磁盘故障 单客户端访问名称 (`SCAN`) 智能闪存缓存 智能闪存日志 智能扫描 布隆过滤器 特性 列投影 数据模型 评分 解密 直接路径读取 禁用者 块传输模式 直通模式 简单不可用 跳过 解压缩 加密 Exadata 存储 全表扫描 `HCC` 压缩/解压缩 `IO_CELL_OFFLOAD_ELIGIBLE_BYTES` `JSON` 支持 `LOBOFFLOAD` 卸载函数 谓词过滤 `RMAN` 恢复 `SQL` 监控 存储索引 `10046` 跟踪 `V$MYSTAT` `V$SESSSTAT` 虚拟列 区域映射 智能扫描事件。 参见用户 I/O 类 脊交换机 分区机架集群 `SQL` 应用 `SQL` 监控 控制 实时 参见(实时 SQL 监控) `V$SQL` `V$SQLSTATS` `SQL` 语句性能 `SQL` 跟踪 分阶段迁移 `storage()` 谓词 存储单元备份 `CELLBOOT` USB 闪存驱动器 外部 USB 驱动器 恢复 单元磁盘故障 单元故障 单元闪存缓存故障 系统卷故障 存储单元层监控 `CellCLI` Exadata `IORM` Exadata 存储服务器网格 Exadata 目标 Oracle `OS` 级指标 `ExaWatcher` `I/O` 操作 `metric_iorm.pl` 存储索引 至少一个谓词 绑定变量 控制 `_cell_storidx_minmax_enabled` 参数 `_cell_storidx_mode` 参数 `_kcfis_storageidx_diag_mode` 参数 `_kcfis_storageidx_disabled` 参数 存储软件参数 定义 加密 特性与语法，预防 `HCC` 连接 限制 监控 数据库统计信息 跟踪机制 总结 多列谓词 并行查询 分区对象 性能优势 空值优化 物理分布，值 `WHERE` 子句 潜在问题 `cellsrv` 11.2.1.2.6 `cellsrv` 11.2.2.2.0 四位数年份格式 不兼容编码技术 错误结果 分区大小 简单比较运算符 智能扫描结构 子查询 压力测试 条带与镜像一切 (`SAME`) 结构化查询语言 (`SQL`) `sundiag.sh` 脚本 `alerthistory` 文件 `alert.log` 文件 `asr` 单元 `cell-detail` `celldisk-detail` 磁盘 `dmesg` `fdisk-l` 和 `parted` 文件 `flashcache-detail` `flashlog-detail` `griddisk-detail` `griddisk-status` `ilom` `lspci` `lsscsi` `lun-detail` `MegaCli64` 命令 消息 `ms-odl.log` `ms-odl.trc` `net` `physicaldisk-detail` `physicaldisk-fail` `raid` `sysconfig` 同步 `I/O` 事件 系统全局区 (`SGA`) 系统统计信息 系统卷故障 `ASM` 冗余 单元磁盘和网格磁盘 单元救援选项 软件与补丁

## T

基于磁带的备份 电信呼叫详细数据 临时单元故障 `tnsnames.ora` 文件 可传输数据库 可传输表空间 (`TTS`) 只读模式 限制 事务层

## U

不可破企业内核 (`UEK`) 通用追踪工具 (`UTS`) `UNIX` 工具 用户 `I/O` 类 块 物理读取单元多块 物理读取单元单块 物理读取单元智能文件创建 单元智能表扫描 `AUs` 单元—每个 `CELL_PATH` 对象 `ID` `OEM` 12c `oradebug doc` 跟踪文件 单元统计信息 收集 次要事件

## V

超大型数据库 (`VLDBs`) `vgdisplay` 命令 虚拟列 虚拟局域网 (`VLANs`)

## W, X, Y

等待事件 单元事件 空闲类 单元智能闪存保持 杂项 非 Exadata 直接路径读取 `enq reliable message` 资源管理器 (`resmgr`) 系统 `I/O` 类 备份事件 单元智能增量备份 单元智能恢复 触发事件 用户 `I/O` 类 参见(用户 `I/O` 类) `WEEKDAY_WINDOW` `WEEKNIGHT_WINDOW` `WORKLOAD` 模式 写回闪存缓存 (`WBFC`) 基准测试 快速数据文件创建 特性 写回模式 写密集型工作负载

## Z

区域映射
