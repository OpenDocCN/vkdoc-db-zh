# 第二部分：窗口函数
## 4. 窗口函数基础
什么是函数？ 41
窗口函数语法 42
分区定义语法 42
排序定义语法 42
帧定义语法 43
WINDOW 子句语法 45
窗口函数参考 47
`AVG()` 47
`BIT_AND()` 47
`BIT_OR()` 48
`BIT_XOR()` 48
`COUNT()` 48
`CUME_DIST()` 48
`DENSE_RANK()` 49
`FIRST_VALUE()` 50
`LAG()` 50
`LAST_VALUE()` 50
`LEAD()` 51
`NTH_VALUE()` 51
`NTILE` 52
`PERCENT_RANK()` 53
`RANK()` 53
`ROW_NUMBER()` 54
`SUM()` 55
总结 56

## 5. 识别窗口函数的应用场景
对结果进行分区和排序 57
维护累计总和 61
对结果集中的行进行排名 65
总结 70

### 6. 实践中的窗口函数
开始之前 71
处理时间序列数据 72
同时使用多个窗口函数 73
绘制时间序列结果图 77
分析水果销售 82
单店内的水果销售情况 82
跨所有门店比较水果销售 85
总结 87

### 7. 结合窗口函数与 CTE
开始之前 89
计算降水日之间的平均间隔时间 90
添加主键列 93
查找间隙与连续段 96
间隙 96
连续段 100
总结 104

索引 105

**内容概览**
关于作者 xi
关于技术审阅者 xiii
致谢 xv
引言 xvii
语法 xix

**第一部分：公用表表达式**
1.  第 1 章：公用表表达式基础 3
2.  第 2 章：非递归公用表表达式 11
3.  第 3 章：递归公用表表达式 19

**第二部分：窗口函数**
4.  第 4 章：窗口函数基础 41
5.  第 5 章：识别窗口函数的应用场景 57
6.  第 6 章：实践中的窗口函数 71
7.  第 7 章：结合窗口函数与 CTE 89

索引 105

**关于作者与关于技术审阅者**
关于作者
关于技术审阅者

