# L

`叶块`

`左深树` 2nd

`左侧输入数据`

`旧式连接语法` 2nd

`库缓存，存储父/子游标`

`许可，SQL 优化建议器与`

生命周期
`游标生命周期`
`顺序/迭代式` 2nd

`LIKE 条件`

`LIMIT 语句`

`语言比较`

`语言索引` –2nd

`列表分区` 2nd

`字面量`
    `游标共享与`
    `替换为绑定变量` 2nd

`load_plans_from_cursor_cache 函数`

`load_plans_from_sqlset 函数`

`本地索引`

`lock_schema_stats 过程`

`lock_table_stats 过程`

`锁定对象统计信息` –2nd

`log4j 日志框架`

`日志框架`

`逻辑读` 2nd –3rd
    `行预取与`
    `行检索测试与`

`逻辑 rowid`

`日志`

`LONG 数据类型（已弃用）`

`长时间解析` 2nd, 3rd

`低（强）选择性` 2nd –3rd

`low_value`

