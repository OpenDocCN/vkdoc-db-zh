# J

`Java 管理扩展（JMX）`

`Java 虚拟机工具接口（JVMTI）`

`带有 JDBC、绑定变量的 Java`

`java.sql.PreparedStatement 类`

`java.sql.Statement 类`

`JDBC`

`数组接口与`

`绑定变量与`

`行预取与`

`会话属性`

`JMX（Java 管理扩展）`

`Jnnn（作业队列进程）`

`连接条件`

`连接相关表达式`

`连接消除`

`连接过滤器裁剪`

`连接语法、物化视图与`

`连接树` –2nd

`连接` –2nd
    `基本方法` –2nd
    `方法选择` 2nd
    `定义` –2nd
    `提示`
    `优化` 2nd, 3rd –4th, 5th
    `并行处理与` 2nd
    `PARTITION RANGE SUBQUERY 操作与` –2nd
    `限制（过滤条件），与连接条件对比`
    `语法` 2nd
    `类型` –2nd

`JPetStore（示例应用程序），性能剖析与` 2nd

`JProbe（Quest 软件）`

`JVMTI（Java 虚拟机工具接口）`

