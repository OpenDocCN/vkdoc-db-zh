# 索引
## A, B
*   `Azure Data Factory (ADF)` 框架
*   `IAM` 添加角色分配完成任务
*   `Contributor` 角色
*   选择文本框
*   `Azure-SSIS` 框架
*   `Azure Storage Explorer` 创建 `File Share`
*   资源管理器窗口
*   `fs-ssis`
*   安装 `stframeworks`
*   存储账户更新
*   `PackageLocation` 值
*   上传 `SSIS` 包
*   `SSISConfig` 数据库完成门户
*   “计算 + 存储”属性
*   配置数据库连接详细信息
*   创建 `Database`
*   创建 `SSISConfig` 数据库数据详细信息
*   启用高级数据安全属性
*   重新配置数据库查询执行结果
*   检查 + 创建页面
*   `SSISConfig` `DDL` 订阅和资源组属性
*   `T-SQL` 查询更新的设置
*   `Azure-SSIS` 集成
*   `ADF` Azure 仪表板
*   `Azure Data Factory` 页面创建按钮以预配创建资源
*   `Data Factory Overview` 页面
*   数据工厂页面
*   启用 `GIT` 复选框
*   新建数据工厂页面
*   资源组下拉列表
*   搜索数据工厂
*   选择位置
*   `Azure Storage`
    *   配置 `Access tier` 属性
    *   配置 `account kind` 属性
    *   配置 `replication` 属性
    *   创建存储账户
    *   存储账户实例详细信息
    *   `performance` 属性
    *   存储账户名称
    *   存储账户设置
*   `Azure-SSIS` `IR` 高级设置
    *   配置选项
    *   配置名称、描述和位置
    *   创建 `SSIS` 目录
    *   仪表板部署设置
    *   `edition/license` 属性
    *   集成运行时设置
    *   集成运行时设置界面
    *   监视 `Integration runtimes` 节点
    *   `number` 属性
    *   `node size` 选项
    *   打开 `ADF` 连接
    *   减少 `BYOL`
    *   运行状态
    *   启动状态
    *   状态对话框
    *   `stop` 按钮
    *   调查对话框
    *   触发器

## C, D
*   控制器模式
*   `FWDemo.DailyProcessController`
*   `Monthly Process Controller`

## E
*   `Execute Package Task (EPT)`
    *   添加 `Execute Child Package`
    *   `ChildPackage`
    *   `ChildPackage` 文件连接管理器
    *   配置 `EPT` `Execute Child Package`
    *   `ConnectionString Override`
    *   `ConnectionString` 属性
    *   `Expression Builder`
    *   `Expression Editor`
    *   `Expressions` 属性
    *   文件连接管理器编辑器
    *   文件系统新建连接
    *   `Parent.dtsx` 包选项卡
    *   `ReportAndSucceed.dtsx` 文件
    *   `ReportAndSucceed.dtsx` 文件
    *   `Task Editor`
    *   `Test Execution Results`
    *   `TestSSISSolutionTestSSISProjectReportAndSucceed.dtsx` 文件

## F, G, H
*   `Fault Tolerance`
    *   `Logic check` 逻辑条件活动
    *   连接 `Failure` 输出
    *   添加执行应用程序包实现
    *   `Log Application Package Instance`
    *   添加触发器应用程序和应用程序包
    *   配置 `Log Application Instance Cancelled`
    *   删除 `Log Application Package Instance Failure`
*   `ForEach Array Item`
*   `if` 条件活动
*   链接服务属性
*   `Log Application Instance Cancelled`
*   父级和子级管道执行
*   `ReportAndFail.dtsx`
*   `Set Cancel Parent Pipeline Command`
*   停止父级执行
*   `StopParentPipelineRunIdString`
*   `True` 活动
*   `T-SQL` 查询结果
*   `URL` 属性 Web 活动

## 框架分析
*   创建 `Schema` 和日志表
*   `daily Process`
*   `monthly Process`

## I, J, K
*   `Integrated development environment (IDE)`

## L
*   `Log.ApplicationInstance` 表
    *   添加 `Execute Child Pipeline`
    *   添加子级管道
    *   添加 `log.UpdateApplicationInstanceStatus`
    *   添加查找活动
    *   `ApplicationInstanceId` 参数值
    *   `ApplicationInstanceId` 变量
    *   `Application Instance` 结果
    *   `ApplicationName` 参数
    *   `ApplicationPackageId` 元数据
    *   `ApplicationStatus` 属性 `Failed`
    *   `Azure Data Studio` 消息
    *   `Child` 管道参数
    *   配置 `ParentRunId` 参数
    *   删除 `Execute Application Package`
    *   `Execute Application Package`
    *   `Execute Child Pipeline` 导入参数
    *   链接服务
    *   `Log Application Instance Failure`
    *   `Log Application Instance Start`
    *   `Log Application Instance Success`
    *   `log.InsertApplicationInstance`
    *   `log.UpdateApplicationInstanceStatus`
    *   修改表
    *   `PackagePath` 参数
    *   `query timeout` 属性
    *   记录集
    *   `ApplicationInstanceId` 设置
    *   `ApplicationRunId` 参数
    *   `ssisFrameworkDataSet` 属性文本执行
    *   查看父级管道
*   添加 `Log Application Package`
    *   添加 `.Net` 代码插桩消息日志记录
    *   `Parent.dtsx` 控制流
    *   `Script Task Editor`
    *   添加和重命名 `Log` 执行值
    *   `ApplicationName` 参数调试执行信息
    *   事件信息事件代码
    *   `ReadOnlyVariables`
    *   脚本变量和参数

## M, N
*   元数据控制器 `@DailyProcName`
*   月度控制器 `@MonthlyProcName`
*   过程完成
*   创建表
*   `Daily Process Application`
*   `DISABLE` 过程
*   `FWDemo.Application`
*   `FWDemo.ApplicationProcessProcedure`
*   `FWDemo.Application` 表
*   `FWDemo.ProcessProcedure`
*   `FWDemo.ProcessProcedure` 表
*   月度过程应用程序协调器
*   `ProcessLogID`
*   `ProcessLogMessage`
*   `Process` 日志输出

## O, P, Q, R
*   `Orchestrator` 过程
*   `execute` 语句
*   `FWDemo.ProcessLog`
*   `FWDemo.ProcessOrchestrator`
*   `@RunDate`

## S, T, U, V, W, X, Y, Z
*   添加 `SQL` `Get Application Packages`
    *   添加 `SQLStatement` 属性
    *   配置 `ADO.NET`
    *   `ADO.NET Connection Manager`
    *   执行 `SQL Task Editor`
    *   框架 `Test` 完整结果集
    *   新建 `Connection`
    *   新建变量参数 `Mapping`
    *   父级容器 `SQL` 任务编辑器
    *   `SSISConfig` 数据库任务编辑器
*   `SQL Server Management Studio (SSMS)`
*   `SSIS` 框架
    *   添加 `ApplicationName` 参数
    *   添加参数配置
    *   `ReportAndFail Application Packages` 的执行配置属性。
    *   `ApplicationPackages` 表
    *   `config.Packages` 表默认约束
    *   密钥管理组件
    *   `config.Applications` 表
    *   `Config Schema`
    *   创建 `TestSSISSolution` 调试执行同理错误事件功能信息事件
    *   安装 `Integration Services` 扩展
    *   安装 `Visual Studio 2019`
*   添加 `log.ApplicationPackageInstance` 表
    *   动态内容添加 `ApplicationPackageRunId`
    *   `ApplicationInstanceId` 参数
    *   `ApplicationPackageRunId` 参数
    *   `ApplicationStatus` 参数失败
    *   `Log Application Package Instance Failure`
    *   `Log Application Package Instance Success`
    *   `log.InsertApplicationPackageInstance`
    *   映射参数 `log.UpdateApplicationPackageInstanceStatus`
    *   父级管道活动
    *   `ReportAndSucceed.dtsx` 结果开始查找活动
    *   更新 `ApplicationPackageStatus`
*   `Main()` 方法
*   元数据驱动执行管理原则
*   `ReportAndSucceed.dtsx`
    *   `Script Task Editor`
    *   `SCR Log Values`
*   简化 `SQL Server Integration Services`
*   `SSISConfig` 数据库
*   `SSIS Framework`
    *   添加 `ADF Execution Engine` 活动输出的动态内容
    *   添加 `ForEach` 活动
    *   添加 `Source` 数据集
    *   重命名 `Azure-SSIS-Files` 集成
    *   配置路径连接测试
    *   复制 `Primary Key`
    *   创建新管道
    *   `event_messages` 日志
    *   `Execute Application Package` 失败连接测试
    *   `ForEach Application Packages Item` `Get Application Packages` `JSON` 返回
    *   日志记录级别名称属性
    *   新建数据集
    *   `PackagePath` 属性
    *   父级管道 `Password` 属性文本框
    *   `ReportAndSucceed.dtsx` 服务器名称链接
    *   显示防火墙设置
    *   `SSISConfig` 数据库 `T-SQL` 语句
    *   查看日志文件夹视图输出查看 `SQL` 数据库
*   `SSIS` 框架应用程序
    *   `FailApplicationOnPackageFailure` 功能
    *   `FailApplicationOnPackageFailure` 逻辑元数据 `ApplicationPackageStatus` 结果
    *   `FailPackageOnFailure` 属性容错
    *   foreach 循环容器 `MaximumErrorCount` 属性
    *   运行 `ApplicationPaackageStatus`
*   更新 `SQL Log Application Package Instance Failure`
    *   更新 `ReportAndFail.dtsx` 包结果
    *   `Parent.dtsx` 执行失败
    *   查看实例日志数据查看实例结果
*   创建 `SSIS` 框架日志记录
    *   日志模式
    *   `log.ApplicationInstance` 表
    *   `log. ApplicationPackageInstance` 表
*   添加 `SSMS` `SQL Log Application Instance`
    *   添加 `T-SQL` 添加 `Variable` `ApplicationInstanceId`
    *   `ConnectionType` 属性
    *   创建新变量
    *   `Event Handlers` 选项卡
    *   `OnError Event` 处理程序参数 `Mapping`
    *   父级 `Executable`
    *   `ResultSet` 属性 `SQL Log Application Instance Success`
*   `SQL Query` 对话框 `SQL Task Editor`
*   添加 `SQL Log Application Package Instance`
    *   添加 `T-SQL` 添加 `Variable` 对话框 `ConnectionType` 属性
    *   失败脚本任务
    *   `FOREACH Application Package` foreach 循环容器
    *   新建变量参数 `Mapping`
    *   `SQL Query` 对话框成功脚本任务
*   添加 `SSIS Framework Metadata Management`
    *   `Framework Test`
    *   添加 `SSIS` 应用程序
    *   添加 `SSIS` 包分配
    *   `SSIS Application Packages` 配置模式框架应用程序
    *   重新执行 `SSIS Application Packages`
    *   `ReportAndSucceed.dtsx` `Test ApplicationId` `T-SQL` 查询
*   添加 `SSIS` 包 `EPT`
    *   `Execute Child Package`
    *   `ChildPackage`
    *   `ChildPackage` 文件连接管理器
    *   `Configure EPT` `Execute Child Package`
    *   `ConnectionString Override`
    *   `ConnectionString` 属性
    *   `Expression Builder`
    *   `Expression Editor`
    *   `Expressions` 属性
    *   文件 `Connection Manager` 编辑器
    *   文件系统新建连接
    *   `Parent.dtsx` 包选项卡
    *   `ReportAndSucceed.dtsx` 文件
    *   `ReportAndSucceed.dtsx` 文件
    *   `Task Editor` 测试执行结果
*   迭代集合
    *   `ADO.Net` 数据集
    *   `ADO` 对象源变量分配
    *   `User` 更改序数集合页面 `ExecutionOrder` 列
    *   `Foreach ADO Enumerator`
    *   `FOREACH Application Package` `PackagePath` 变量
    *   `Parent.dtsx` 控制流 `SSIS` 变量用户变量
    *   `Mappings` 页面
*   添加 `Log Application Package`
    *   添加 `.Net` 代码插桩消息
    *   `Logging` `Parent.dtsx` 控制流
    *   `Script Task Editor` 记录执行值
    *   添加和重命名 `ApplicationName` 参数调试执行
    *   `Information` 事件 `Information` 事件代码
    *   `ReadOnlyVariables` 脚本变量和参数
*   添加 `Log Package Execution`
    *   脚本应用程序包失败
    *   脚本任务信息事件
    *   `.Net` 代码 `Parent.dtsx` 执行失败
    *   `ReadOnlyVariables` 属性
    *   成功脚本任务查看应用程序包
*   添加 `SQL Get Application Packages`
    *   添加 `SQLStatement` 属性
    *   配置 `ADO.NET` `ADO.NET Connection Manager`
    *   `Execute SQL Task Editor` 框架 `Test` 完整结果集
    *   新建 `Connection` 新建变量参数映射
    *   父级容器 `SQL` 任务编辑器
    *   `SSISConfig` 数据库任务编辑器
