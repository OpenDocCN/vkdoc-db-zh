# 恢复管理器 (`RMAN`)

-   恢复管理器 (`RMAN`)
    -   架构决策
    -   备份
        -   `自动备份` 控制文件特性
        -   `命令`
        -   `控制文件` 恢复
        -   `数据库` 恢复
        -   `数据库`，重命名
        -   `DBA` 技能
        -   `目标服务器`
        -   `init.ora` 文件
        -   位置与文件格式
        -   在 `装载` 模式下
        -   在 `未装载` 模式下
        -   `在线重做日志`
        -   `Oracle` 二进制文件安装
        -   `操作系统变量`
        -   `服务器目录`
        -   `集`
        -   `临时文件`
        -   `用户`
    -   `二进制压缩`
    -   `块变化跟踪`
    -   `通道配置命令`
    -   `客户端`，远程
    -   `命令行实用程序`
    -   `组件`，架构
    -   `可配置变量`
    -   `CONTROL_FILE_RECORD_KEEP_TIME` 初始化参数
    -   `并行度`
    -   描述
    -   `基于磁盘的备份`
    -   `加密备份`
    -   `映射副本`
    -   实现，架构决策
    -   `增量备份策略`
    -   `信息输出`
    -   `介质管理器`
    -   `介质恢复`（*参见* `介质恢复`）
    -   `内存缓冲区`
    -   `Oracle 服务器进程`
    -   `PL/SQL 包`
    -   `恢复目录`
    -   `恢复与恢复`（*参见* `恢复与恢复，RMAN`）
    -   `保留策略`
        -   `备份删除`
        -   `恢复窗口`
        -   `冗余`
    -   `Shell 脚本`
    -   `快照控制文件`
    -   `SQL 命令`
    -   `类型`，备份

