# 恢复与恢复 (`RMAN`)

-   恢复与恢复，`RMAN`
    -   `已归档重做日志文件`
    -   `自动备份`，控制文件
    -   `备份文件名`
    -   `容器与可插拔数据库`
    -   `CONTROL_FILES` 初始化参数
    -   `DDL 脚本`
    -   `介质恢复`
    -   `非默认位置`
    -   `可插拔数据库`
    -   `RECOVER DATABASE 命令`
    -   `修复故障`
    -   `RESTORE DATABASE 命令`
    -   `RESTORE…PREVIEW 命令`
    -   `恢复`，`spfile`
    -   `根容器数据文件`
    -   `SQL*Plus`
    -   `停止/启动 Oracle`
    -   `表级恢复特性`
    -   `测试介质恢复`
    -   `验证`，备份文件

`RMAN`。*参见* `恢复管理器 (RMAN)`
![images](img/sq.`)



### S

基于 SNC 的不完整数据库恢复
SQL 与操作系统命令
存储操作
数据文件路径与名称
段与存储属性
大小，数据文件
表空间元数据

![images](img/sq.jpg)

### T

表空间时间点恢复 (TSPITR)
表空间与数据文件
更改，大小
应用程序
大文件特性
CREATE TABLESPACE 语句
数据库，打开
数据字典视图
描述
删除
逻辑存储对象与物理存储
管理实践
现代存储配置
装载模式，数据库
重做生成
重命名
大小显示
SYSTEM 表空间
表压缩
UNDO 表空间
USERS 表空间
写入模式
基于时间的恢复与复原
可传输表空间机制

![images](img/sq.jpg)

### U, V, W, X, Y, Z

用户管理的备份与恢复
冷备份 (*参见* 冷备份策略)
热备份 (*参见* 热备份策略)
类型
