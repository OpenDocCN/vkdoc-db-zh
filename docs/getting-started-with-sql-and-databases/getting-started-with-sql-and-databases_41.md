# 索引

## A
*   `acos()` 函数
*   **关联性**
*   **聚合函数**：计算数据、将数据计数作为筛选器、数字、处理值
*   **聚合**
*   **别名**：双引号中的列名、无 `AS` 的间距
*   **字母顺序**
*   **`ALTER` 语句**
*   **模糊列**
*   **`AND` 运算符**
*   **ANSI 模式**
*   **撇号**
*   **近似函数**
*   **算术计算**
*   **算术运算**
*   **算术运算符**
*   **艺术家**
*   **断言**：全有或全无、无关
*   **原子数据**
*   **自增键**
*   **自动序列号**

## B
*   **`BETWEEN` 操作**
*   **BLOB (二进制数据)**
*   **布尔数据**
*   **布尔值**
*   **方括号**

## C
*   **级联**
*   **`CASE`**
*   **`CASE` 表达式**
*   **大小写敏感性**
*   `cast()` 函数
*   **类型转换**：日期字面量转字符串，参见日期，字符串，参见字符串，子查询，测试计算
*   **字符数据**
*   **字符函数**：更改大小写、替换、子字符串、字符串长度、子串、修剪空格
*   **`CHECK` 约束**
*   **子表**
*   **子句顺序**
*   `coalesce()` 函数
*   **排序规则**
*   **列**：计算、独立、相互依赖、布局、名称、顺序、单个、类型、使用 `SELECT *`、选择
*   **列值**：类型转换，参见日期，参见字符串
*   **注释**：块的使用
*   **公共表表达式 (CTE)**：聚合函数、高消费客户、分组、连接、子查询
*   **比较运算符**
*   `concat()` 函数
*   **连接**
*   **约束**：`CHECK`、数据类型、`DBMS`、`DEFAULT`、外键、索引、`NOT NULL`、`UNIQUE`
*   **连续值**
*   **相关子查询**
*   `count()` 函数
*   `count(*)` 函数
*   **计数**：数据集、选择性数据、值
*   **`CREATE` 语句**
*   **`CREATE TABLE` 语句**
*   **`CREATE UNIQUE INDEX` 语句**
*   **`CREATE VIEW`**
*   **`CROSS JOIN`**
*   **文化注释**：地址、货币、日期、电子邮件地址、地理州、度量、电话号码、邮政编码、价格、城镇

## D
*   **数据**
*   **数据库**：数据 *vs.* 值、定义、术语
*   **数据库管理员 (DBA)**
*   **数据库管理系统 (DBMS)**
*   **数据操作**
*   **数据操作语言 (DML)**：`DELETE`、前端软件、`INSERT`、安全性、`UPDATE`
*   **数据处理**
*   **数据查询语言**
*   **数据选择**：获取、登录脚本、结果集、单列
*   **数据类型**：转换函数、大小写敏感性、排序规则、日期、数字、排序顺序、字符串
*   `datediff()` 函数
*   **日期**：年龄、计算、比较、从日期时间提取、在 MariaDB/MySQL 中、在 Microsoft SQL 中、Oracle、在 PostgreSQL 中、格式、格式化、按月分组、按周几分组、MariaDB、Microsoft SQL、MySQL、Oracle、PostgreSQL、函数、字面量、简单计算
*   `datetime()` 函数
*   **日期时间**
*   **小数**
*   **`DEFAULT` 约束**
*   **`DELETE` 语句**
*   **派生列表**
*   **差异**
*   **离散值**
*   **不相交**
*   **`DISTINCT` 子句**
*   **`DISTINCT` 关键字**
*   **`DISTINCT` 运算符**
*   **域**
*   **双引号**
*   **`DROP VIEW` 语句**
*   **重复项**

## E
*   **错误报告**
*   **`EXCEPT` 运算符**
*   **扩展**

## F
*   `fetch()` 函数
*   `fetchColumn()` 函数
*   **字段**
*   **筛选**：大小写敏感性、日期、分组、字符串、通配符匹配
*   **筛选数据**：断言、多个、日期、`NULL`、数字、字符串、`WHERE` 子句、通配符匹配
*   **筛选器**
*   **定点**
*   `floor()` 函数
*   **外键**
*   **格式化**：日期、函数、时间
*   **`FROM` 子句**
*   **前端软件**

## G
*   **金发姑娘三分法**
*   **商品及服务税 (GST)**
*   **总计**
*   **分组聚合**
*   **`GROUP BY` 子句**
*   **分组**：带 `count()` 函数、多表、`WHERE` 子句

## H
*   **`HAVING` 子句**
*   **历史顺序**

## I
*   **`ILIKE` 运算符**
*   **索引**
*   **`INNER JOIN`**
*   **`IN` 运算符**
*   **插入顺序**
*   **`INSERT` 语句**
*   **整数**
*   **`INTERSECT`**
*   **交集**
*   **间隔**
*   **ISO 8601**

## J, K
*   **`JOIN` 子句**
*   **`JOIN` 语法**
*   **连接，表**：列、布局、简化、子查询、补充表
*   **连接类型**：`INNER JOIN`、`NULL`

## L
*   **布局**
*   **`LEFT JOIN`**
*   **逻辑错误**
*   **逻辑表达式**
*   **逻辑运算符**
*   **登录页面**

## M
*   **MariaDB**
*   **数学函数**
*   `max()` 函数
*   **Microsoft**
*   **Microsoft Access**
*   **Microsoft SQL Server**
*   `min()` 函数
*   **被误解的列**
*   **缺失数据**
*   **修饰符**
*   **取模 (“mod”) 运算符**
*   **MSSQL**
*   **多行**
*   **多个值**：复杂关系、一对多关系、原则、相关表、表间关系

## N
*   **MySQL**
*   **非字母字符串顺序**
*   **非字符串**
*   **`NOT` 运算符**
*   **`NOT NULL`**
*   **可为空**
*   **`NULL`**：查找、忽略
*   **数字**：聚合函数、近似函数、算术运算符、计算、比较运算符、小数、离散 *vs.* 连续值、格式化、函数、整数、数学函数、余数、变体
*   **数字顺序**

## O
*   **`ON` 子句**
*   **一对多关系**
*   **一对一关系**
*   **在线商店**
*   **运算符**
*   **Oracle**
*   **Oracle 日期格式**
*   **`ORDER BY` 子句**：列、相互依赖、排序方向、`NULL`、结果数量、客户、`OFFSET` 部分、MariaDB、MySQL、分页、PostgreSQL、SQLite、排序方向、排序
*   **序数**
*   **`OR` 运算符**
*   **`OUTER JOIN`**

## P
*   **画作**
*   `password_verify()` 函数
*   **模式和大小写敏感性**，字符、匹配
*   **PDO (PHP 数据对象)**：连接建立、`DBMS`、PDO 对象、`PDOStatement`、预处理语句、重复执行、未准备的 SQL 语句、供应商中立方法
*   **PostgreSQL**
*   **前序查询**
*   **价目表**
*   **主键列**
*   **主键**

## Q
*   **引号**
*   **引号**

## R
*   `random()` 函数
*   **范围**
*   **比率**
*   **实数**
*   **记录**
*   **冗余分组**
*   **正则表达式**
*   **取余运算符**
*   **重复执行**
*   **`RIGHT JOIN`**
*   **`ROLLBACK`**
*   **根**
*   **行**：随机添加、子句排序、删除、独立、分号、唯一性、无序、更新

## S
*   **示例数据库**，相关表
*   **关系型数据库**
*   **测量尺度**
*   **安全性**
*   **选择性联合**
*   **`SELECT` 子句**
*   **`SELECT` 语句**：大小写敏感性、子句排序、间距、无 `FROM` 子句的 `SELECT`
*   **自连接**
*   **自画像**
*   **自引用表**
*   **分号**
*   **排序**
*   **集合操作**：聚合、比较、虚拟表
*   **集合**
*   **更简单的扩展**
*   **单引号**
*   **排序方向**
*   **排序**：按字母顺序、计算列、多列、`ORDER BY`、`ORDER BY` 子句、`SELECT` 语句、表
*   **排序顺序**
*   **尾随空格**
*   **间距**
*   **特殊字符串**
*   `sprintf()` 函数
*   **SQL 数据库**：聚合，参见聚合函数，大小写敏感性，列，选择，数据类型，日期，`DBMS`，`ORDER BY` 子句，引号，关系型数据库，行，`SELECT` 语句，分号，表列，表，参见表，术语
*   **SQL 数据值**：计算、存储值、字符串字面量、变量
*   **SQL 注入**
*   **SQLite**
*   **存储值**
*   `strftime()`, `strftime` 函数
*   **字符串**：撇号、计算、大小写敏感性、字符函数、比较运算符、MariaDB、MySQL、引号、尾随空格
*   **结构化查询语言 (SQL)**：参见 SQL 数据库
*   **子查询**
*   **子字符串**
*   **小计**
*   `sum()` 函数
*   **语法错误**

## T
*   **表**：添加行、别名、更改、替代语法、列名、创建、客户、删除、行、`GROUP BY ()`、分组、连接，参见连接、表、操作、匹配行、多行、添加、规范化、主键、选择、`SELECT` 语句、术语、更新行
*   **测试计算**
*   **`TEXT` 类型**
*   **时区**
*   **事务**
*   `trim()` 函数
*   **修剪空格**

## U
*   **`Union` 子句**：列名、重复项、运算符、`SELECT` 子句、`SELECT` 语句、分号、集合、组合、排序、表
*   **`UNION`**
*   **`UNION ALL` 子句**
*   **`UNIQUE`**
*   **唯一约束**
*   **`UPDATE` 语句**
*   **用户**

## V
*   **值**
*   **`VARCHAR` 类型**
*   **变量**
*   **虚拟表**

## W, X, Y, Z
*   **`WHEN` 表达式**
*   **`WHERE` 子句**：全有或全无、断言、函数、`NULL`、无关断言
*   **通配符**：大小写敏感性、扩展、非字符串、模式字符、模式、正则表达式、更简单的扩展
*   **通配符匹配**
*   **`WITH` 子句**

## 脚注
1
