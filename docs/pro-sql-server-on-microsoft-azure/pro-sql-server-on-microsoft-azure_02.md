# 在 Azure 虚拟机上运行的 SQL/Oracle

Azure 虚拟机允许用户配置 `Microsoft SQL Server`、`Oracle` 或任何其他 `DBMS` 的实例。正如在关于计算产品和虚拟机部分中所提及的，Azure 市场提供了多种映像，可用于部署 `SQL Server` 实例（截至本书撰写时，有 `SQL 2012`、`SQL 2014` 和 `SQL 2016 RC` 版本）或 `Oracle` 实例。此外，还可以使用自定义的 `VHD` 创建其他 `DBMS` 环境，并将该 `VHD` 上传到 Azure。

