# 第二部分：安装与配置

## 第 3 章：系统规划
- 确定你的优先级
- 高可用性要求
- 数据节点
- SQL 节点
- 管理节点
- 为网络分区做准备
- 可扩展性
- 灾难恢复
- 典型拓扑
- 副本数
- 最大数据节点数
- 最大总节点数
- 仲裁等级
- 将 SQL 节点和数据节点放置在同一台机器上
- 典型拓扑示例
- 平台考量
- 处理器类型与操作系统
- CPU 性能与特性
- 内存消耗
- 磁盘性能
- 虚拟机
- 网络设计
- 网络设备
- 网络冗余
- 直接连接
- 安全考量
- 使用磁盘对象扩展容量
- 性能考量
- 存储要求
- 内存消耗
- 总结

## 第 4 章：配置
- 配置概述
- `config.ini` 的格式
- 重启类型
- 管理节点选项
- 管理节点主要选项
- 管理节点配置要点
- 数据节点选项
- 基本选项
- 内存数据存储选项
- 模式对象选项
- 事务选项
- 总内存消耗估算
- 检查点选项
- 估算重做日志大小
- 多线程选项
- CPU 属性考量
- 备份选项
- 传输器选项
- 磁盘对象选项
- 心跳与看门狗选项
- 日志选项
- 推荐配置策略
- SQL 节点选项
- SQL 节点主要选项
- SQL 节点配置要点
- TCP 传输器选项
- 传输器主要选项
- 通过独立网络路径的传输器
- 程序启动选项 (`my.cnf`)
- 通用选项
- `ndb_mgmd` 主要选项
- `ndbd`/`ndbmtd` 选项
- `mysqld` 选项
- 选项文件位置
- 总结

## 第 5 章：安装
- 软件包安装
- 获取软件包
- 在 Linux 上安装
- 在 Windows 上安装
- 在 macOS 上安装
- 使用自动安装程序安装 `MySQL NDB Cluster` 实例
- 验证安装
- 配置文件
- 初始启动
- 检查状态
- 卸载软件包
- `Tar.gz` 和 `Zip` 归档包
- `RPM` 包
- `Windows Installer` 包
- `macOS Native` 包
- 总结

## 第 6 章：复制
- `NDB Cluster` 复制概述
- 复制架构概述
- 复制通道故障切换
- `NDB Cluster` 复制表
- `NDB Cluster` 复制的使用场景与优势
- 设置 `NDB Cluster` 复制
- 使用空数据库设置 `NDB Cluster` 复制
- 使用现有数据库设置 `NDB Cluster` 复制（离线）
- 使用现有数据库设置 `NDB Cluster` 复制（在线）
- `NDB Cluster` 复制通道故障切换
- `NDB Cluster` 复制日常维护
- 监控 `NDB Cluster` 复制
- 重启主集群
- `NDB Cluster` 复制性能调优
- 冲突检测与解决
- 多主复制
- 由多主复制引起的冲突
- 冲突检测方法
- 读操作的冲突检测
- 设置冲突检测与解决
- 监控冲突检测
- 冲突检测案例研究
- 冲突检测所需的程序修改
- 冲突检测的注意事项与限制
- 复制到 `InnoDB`
- 要求与限制
- 设置复制到 `InnoDB`
- 使用 `InnoDB` 作为从库时的提示
- 与集群复制相关的 `MySQL` 服务器选项
- `NDB Cluster` 复制的注意事项与限制
- 总结

