# 验证 `datastax` 键空间创建

要验证 `datastax` 键空间是否已在 Cassandra 中创建，请使用以下命令登录 Cassandra 客户端界面。如果使用的 Apache Cassandra 版本不包含 `cassandra-cli`，请使用早期版本 Apache Cassandra 2.1.7 来获取 `cassandra-cli`。

```bash
cassandra-cli
```

