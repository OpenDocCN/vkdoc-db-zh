# C 索引
基数估算器
目录视图
检查点页数/秒
清理策略
聚集列存储索引
聚集索引
聚集索引键
列存储筛选索引
列存储索引
公共语言运行时 (CLR) 执行
常见等待
`ASYNC_NETWORK_IO`
`CXCONSUMER`
`CXPACKET` 等待
`IO_COMPLETION`
`OLEDB` 等待
`PAGELATCH_*` 和 `PAGEIOLATCH_*`
`SOS_SCHEDULER_YIELD`
`THREADPOOL`
计时器
`WRITELOG`
编译与重新编译过程
压缩
压缩延迟
`COMPRESSION` 设置选项
配置，SQL Server
高级跟踪标志
备份压缩默认值
已阻塞进程阈值
基数估算器
并行度的成本阈值
环境变量
准则
附加配置
内核设置
内存与 OOM killer
即时文件初始化
Linux
锁定内存页
最大并行度
内存配置
即席工作负载优化
查询调控器成本限制
查询优化器修补程序
SQL Server 企业版
统计信息自动更新标准
跟踪标志 2371
tempdb
系统目录争用
矛盾检测
成本估算
并行度的成本阈值
配置设置
与 CPU 相关的内核设置
`CREATE INDEX` 语句
`CREATE TABLE` 语句
`CXCONSUMER` 等待
`CXPACKET` 等待

