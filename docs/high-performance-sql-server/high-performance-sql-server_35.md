# W, X, Y, Z 索引
wait_completed
`WAITFOR` 语句
wait_info 事件
wait_info_external
等待信息
扩展事件，等待
`sys.dm_exec_requests` 和 `sys.dm_os_waiting_tasks`
`sys.dm_exec_session_wait_stats`
`sys.dm_os_wait_stats`
system_health 扩展事件会话
waiting_task_address
wait_resource
等待和队列
性能方法学
等待性能方法学
等待性能调优方法学
等待统计信息
wait_type
`WHERE` 子句
工作线程
`WRITELOG`
