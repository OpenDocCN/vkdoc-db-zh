# 第 12 章：分区：分而治之

## 哪些表应该被分区？

## 创建分区表

### 按范围分区

### 按列表分区

### 按哈希分区

### 混合使用不同的分区方法

### 按需创建分区

### 创建与父表匹配的分区

### 在虚拟列上分区

### 让应用程序控制分区

## 维护分区

## LOB 管理

### 添加 LOB 列

### 删除 LOB 列

### 缓存 LOB

### 行内与行外存储 LOB

### 使用 SecureFile 特性

#### 压缩 LOB

#### 去重 LOB

#### 加密 LOB

#### 从 BasicFiles 迁移到 SecureFiles

### 查看 LOB 元数据

### 加载 LOB

#### 加载 CLOB

#### 加载 BLOB

### 测量 LOB 消耗的空间

#### BasicFile 已用空间

#### SecureFile 已用空间



### 查看分区元数据

### 移动分区

### 自动移动更新后的行

### 对现有表进行分区

### 添加分区

### 将分区与现有表交换

### 重命名分区

### 拆分分区

### 合并分区

### 删除分区

### 为分区生成统计信息

### 从分区中移除行

### 操作分区内的数据

## 分区索引

### 使索引的分区与其表的分区一致

### 以不同于其表的方式对索引进行分区

## 分区修剪

## 总结

