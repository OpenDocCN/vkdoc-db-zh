# RMAN 备份，dk：2010 年 5 月 1 日，已更新。
```

如果您管理着数十个数据库服务器（每个都有自己的 cron 任务）并且有多个 DBA，您将需要某种机制（而且不必很复杂）来跟踪谁做了更改以及何时做的。

2.  接下来，在编辑 cron 表之前制作一个副本。这允许您在引入错误且无法轻易找出问题所在时，恢复到原始状态。这也为您提供了 cron 表更改的审计跟踪：
    ```
    $ cp mycron.txt mycron.jul29.txt
    ```

3.  您现在可以用您喜欢的文本编辑器编辑 `mycron.txt` 文件：
    ```
    $ vi mycron.txt
    ```

例如，要将一个名为 `backup.bsh` 的脚本安排在每晚 11:05 运行，请在文件中输入以下内容：
```
#---------------------------------------------------------------------------------



