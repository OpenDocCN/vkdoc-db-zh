# 1. 确定并收集配置

首先，确定需要收集的配置。将配置组织在一个或多个表中，其中每个表对应一个配置项。配置项可以是单行或多行的，就像收集监控指标一样。如果定义配置的表之间存在关系，则可以定义具有层次关系的完整 ECM 模式。可以将其视为 ECM 元数据，它向 EM12c 描述配置信息，以便可以像使用开箱即用的目标一样使用它。ECM 元数据是一个 XML 文件，放置在 OMS 暂存区的 `<STAGE>/oms/metadata/snapshotlive` 目录中。基于 ECM 元数据，EM12c 会在存储库的 `SYSMAN` 模式中创建自定义表和视图。

