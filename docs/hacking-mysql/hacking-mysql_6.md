# 索引

## A
访问控制
账户类别
账户锁定
ACID 与大数据特性
`ALTER` 查询
`ALTER TABLE` 查询
`ALTER USER` 查询
Amazon S3 服务器
API 与大数据
数据库管理系统架构
字符集与排序规则
客户端-服务器存储
MySQL 架构
客户端组件、层、服务器、存储
ARCHIVE 存储引擎
认证插件
`autocommit` 参数
`Auto-increment` 选项

## B
备份
大数据集
鸟瞰视图
命令
压缩与安全
MariaDB 中的逻辑或物理备份
及恢复陷阱
SQL 语句
策略类型与工具
BASE
基本多文种平面 (BMP)
Bcrypt/Blowfish
大数据与 ACID 及 API
被泄露的数据
数据分析
数据库数据类型
数据窃取
黑客操作
Linux 选项
`LOAD DATA INFILE`
索引与 MariaDB 及 MySQL
NoSQL 数据库
优化 MySQL
优化查询
优化模式
解析陷阱
数据恢复
脚本小子
搜索引擎
安全
集合与存储引擎
唯一索引
基于大数据的搜索引擎
`BINARY` 和 `VARBINARY` 数据类型
二进制日志
二叉搜索树 (BST)
`BIT` 数据类型
BLACKHOLE 存储引擎
`BLOB` 和 `TEXT` 数据类型
BMP 参见 基本多文种平面 (BMP)
最速降线
被泄露的数据
受损的访问控制
BST 参见 二叉搜索树 (BST)
B-tree 索引
备份
BST
复制粘贴
数据类型
演示目的
等值运算符
MySQL 中的 InnoDB 索引
查询性能
随机邮件
`SELECT` 查询
用户名列值
缓冲区
内置计划任务

## C
`CALL` 过程
汽车相关网站
CDN
`CHAR` 和 `VARCHAR` 数据类型
字符集与排序规则
CloudFlare
聚簇索引
CMS 用例
`COALESCE PARTITION` 语句
性能代码
代码审查
编码系统
为 MySQL 性能和安全编码
高级特性
应用与数据库架构
备份你的数据
Bcrypt/Blowfish
基准测试与性能剖析
最速降线
整洁代码
代码性能剖析器
完全安全的应用/完全安全的数据库
CSRF 令牌
为高性能建立索引
负载均衡器
服务器组件
终止连接
用户输入
Coffman 条件
复合索引
压缩
配置文件
配置 MySQL
磁盘 I/O 参数
缓冲区
每表一个文件特性
文件
ibdata1
InnoDB
Linux 用户
带注释的 `my.ini`
`my.ini` 中的 `my.InnoDB` 设置
MyISAM
临时方案
Windows 用户
构建索引
`CONVERT PARTITION … TO TABLE` 查询
`count_cities`
覆盖索引
B-tree 索引
列详细信息
MariaDB 中的列功能
多列索引
列顺序
要求
`CREATE` 查询
`CREATE PROCEDURE`
`CREATE ROLE` 语句
`CREATE TABLE` 和 `INSERT INTO ...` 语句
CRUD（增、查、改、删）查询
操作三角
加密失败
CSRF 令牌
CSV 存储引擎
网络犯罪
网络安全

## D
共享托管中的数据库备份性能
数据库管理系统
数据泄露
边界
基础设施
人员访问
搜索引擎
数据控制语言 (DCL)
数据定义语言 (DDL)
数据加密
数据碎片
数据操作语言 (DML)
数据脱敏
数据修剪
数据分割
数据类型
字符
字符集与排序规则
数据库实例
日期和时间
日期、时间、空间和 JSON
定义
ENUM 错误
整数
JSON
数值
数值数据
优化模式
正确的
SET
空间
空间（地理空间）用于存储地理数据
存储要求
存储日期和时间值
用于存储字符串值
字符串用例
`utf8mb4`
`DATE` 数据类型
日期和时间数据类型
`DATETIME` 数据类型
日期、时间、空间和 JSON 数据类型
数据库管理员
数据库管理系统
DCL 参见 数据控制语言 (DCL)
DDL 参见 数据定义语言 (DDL)
DDoS 攻击
死锁
Debian
`DECIMAL` 和 `NUMERIC` 数据类型
清理，查询
专用服务器
“默认”类型的复制
`DELETE` 查询
降序索引
`DISABLE KEYS`
磁盘 I/O
DML 参见 数据操作语言 (DML)
文档
重复索引

## E
电子商务商店
企业级数据库
企业级安全控制
`ENUM` 数据类型
等值运算符
ERD 模式
错误代码，MySQL
42000 无法创建/写入文件
数据库管理系统错误 1040（连接数过多）
错误 1045（访问被拒绝）
错误 1064（语法错误）
错误 1114（表已满）
错误 1659（时间戳）
错误 2006（服务器连接已关闭）
错误 2008（客户端内存耗尽）
错误 2013（查询期间丢失连接）
`HY000`
数据包过大
分区
错误消息
错误代码
错误条件
错误编号
MariaDB 中的格式
MariaDB 语法错误
`SQLSTATE` 值
故障排除
错误
条件
理解和模拟
`EXPLAIN` 操作
查询

## F
`FEDERATED` 和 `EXAMPLE` 存储引擎
联邦表
`file_per_table`
每表一个文件特性
`filter_input()` 函数
`filter_var()` 函数
为 MariaDB 设置防火墙
防火墙解决方案
`FLOAT` 和 `DOUBLE` 数据类型
`FLUSH PRIVILEGES`
`FLUSH TABLES WITH READ LOCK` 查询
`foreach` 循环
碎片
完整备份
全文索引

## G
Galera 集群
GDPR 与 NIST
GEN_CLUST_INDEX
全局事务 ID (GTID)
Google Chrome
希腊字符集
`GROUP BY` 子句

## H
hacking_mysql 数据库
硬件
硬件测试
哈希索引
哈希分区
HDD 存储
htmlspecialchars

## I
ibdata1
Imperva
`incrementing_id` 列
索引顺序访问方法 (ISAM)
索引设计，完美
索引
建议
大数据
B-tree 索引
列
覆盖索引
数据库
数据库管理系统定义
磁盘空间
功能
硬件
高性能
索引扫描
索引查找
在 MariaDB 中
误解
迷思、误解和碎片化问题
`ORDER BY` 子句性能
`SELECT` 查询
`UPDATE` 查询
`WHERE` 子句
基于注入的攻击
InnoDB
ACID 兼容性
应用程序
缓冲池
缓冲池和日志文件
检查点操作
组件配置
配置
数据文件
删除数据
检测只读事务
特性
刷新方法
ibdata1
`innodb_adaptive_hash_index`
`innodb_buffer_pool_dump_at_shutdown`
`innodb_buffer_pool_dump_now`
`innodb_buffer_pool_instances`
`innodb_buffer_pool_load_at_startup`
`innodb_buffer_pool_size`
`innodb_data_file_path`
`innodb_default_row_format`
`innodb_doublewrite`
`innodb_file_per_table`
`innodb_flush_log_at_trx_commit`
`innodb_flush_method`
`innodb_force_recovery`
`innodb_ft_enable_stopword`
`innodb_ft_max_token_size`
`innodb_ft_min_token_size`
`innodb_io_capacity`
`innodb_lock_wait_timeout`
`innodb_log_buffer_size`
`innodb_log_files_in_group`
`innodb_log_file_size`
`innodb_max_dirty_pages_pct`
`innodb_optimize_fulltext_only`
`innodb_page_size`
`innodb_purge_threads`
`innodb_read_io_threads`
`innodb_redo_log_capacity`
`innodb_stats_on_metadata`
`innodb_strict_mode`
`innodb_thread_concurrency`
`innodb_undo_log_truncate`
`innodb_write_io_threads`
日志文件
MyISAM 转换为
参数
主要竞争者
只读操作
行格式
设置
`skip_innodb_doublewrite`
存储引擎
系统表空间
表
表空间文件
包含记录的表
变量
`innodb_adaptive_hash_index`
`innodb_buffer_pool_dump_at_shutdown`
`innodb_buffer_pool_dump_now`
`innodb_buffer_pool_instances`
`innodb_buffer_pool_load_at_startup`
`innodb_buffer_pool_size`
`innodb_data_file_path`
`innodb_default_row_format`
`innodb_doublewrite`
`innodb_file_per_table`
`innodb_flush_log_at_trx_commit`
`innodb_flush_method`
`innodb_force_recovery`
`innodb_ft_enable_stopword`
`innodb_ft_max_token_size`
`innodb_ft_min_token_size`
InnoDB 索引
`innodb_io_capacity`
`innodb_lock_wait_timeout`
`innodb_log_buffer_size`
`innodb_log_files_in_group`
`innodb_log_file_size`
`innodb_max_dirty_pages_pct`
`innodb_optimize_fulltext_only`
`innodb_page_size`
`innodb_purge_threads`
`innodb_read_io_threads`
`innodb_stats_on_metadata`
`innodb_strict_mode`
`innodb_thread_concurrency`
`innodb_undo_log_truncate`
`innodb_write_io_threads`
输入清理
`INSERT` 操作
`INSERT` 查询
互联网
ISAM 参见 索引顺序访问方法 (ISAM)

## J
`JOIN` 操作
`JOIN` 查询
JSON
JSON 数据类型

## K
键分区
`KILL` 操作

## L
`Last_query_cost`
`LIKE` 子句
`LIKE` 查询
`LIMIT` 子句
列表/列表列分区
负载均衡器
`LOAD DATA INFILE`
`LOCK TABLE` 语句
`log_bin` 和 `server_id` 选项
逻辑备份

## M
MariaDB
备份
B-tree 索引
调用过程
字符集与排序规则
列
组件与插件
覆盖索引
数据文件
默认行为
定义过程
错误消息
功能
索引类型
`Last_query_cost`
`mariadb-slap`
MERGE、SPIDER 和 CONNECT
MUL
使用 `mysqlslap` 及其伙伴的 MyBB 1.8.38 数据库
Planet
过程
查询缓存
数据恢复
复制
类型
受保留账户
安全安装
`SHOW STATUS`
查询
SQL 查询语法错误
工作
`your_table_backup.txt` 文件
MariaDB 与大数据
数据库管理系统
删除
插入
读取
更新
`mariadb-dump`
`mariadb-secure-installation`
`mariadb-show`
`mariadb-slap`
主-主复制
主-从复制
`max_allowed_packet` 变量
`MEMORY` 和 `TempTable` 存储引擎
MERGE 存储引擎
混合模式复制
用于防火墙的 `mod_security`
MongoDB
多列（复合）索引
多主复制
多个 root 用户
多版本并发控制 (MVCC) 数据
MyISAM
廉价
数据不一致
已弃用的特性
反面功能
InnoDB 的
ibdata1
InnoDB 的表空间
MRG_MyISAM
非事务性存储引擎
存储
存储引擎
MyRocks
批量加载数据
已安装并启用
内存
操作系统
`ps-admin`
RocksDB
`rocksdb_bulk_load`
`rocksdb_column_default_value_as_expression`
`rocksdb_create_checkpoint`
`rocksdb_db_write_buffer_size`
`rocksdb_error_if_exists`
辅助索引
`sudo` 命令
未排序数据
MySQL
架构
AND 或 OR
备份类型
B-tree 索引
字符集与排序规则
通信
配置文件
数据类型
默认复制模式
历史
索引
键
NULL 值
分区类型
性能
查询 参见 查询
关系型数据库管理系统
受保留账户
搜索引擎
慢查询性能
SQL 查询
存储
存储引擎
UTF-8
另请参见 安全
MySQL 5.7
MySQL 企业审计
MySQL 企业防火墙
`mysqli_real_escape_string()`
`mysqlslap`

## N
NDBCluster 网络数据库 (NDB) 存储引擎
配置文件
数据节点
安装和使用
管理节点
管理服务
MySQL NDB 集群
NDBCluster 服务
无共享架构
SQL 节点
NIST 框架
非关系型数据库
NoSQL 数据库
`NULL` 值
数值数据类型
NVMe 驱动器
NVMe SSD

## O
`OFFSET` 子句
在线/离线数据备份
在线事务处理 (OLTP) 查询
操作内存
服务器优化
ACID 特性
建议
之前的预算选择
选择服务器和硬盘
配置数据
数据库
数据库配置
数据库管理系统
经验
灵活性
加固
硬件测试
位置和维护要求
错误成本
操作内存
MySQL 性能
移除阻碍
搜索引擎
安全措施
慢速查询
用例
网络服务器问题
存储引擎优化
`autocommit`
备份策略
InnoDB 的缓冲池和日志文件
配置文件
制作和选择索引
数据库集合
数据分区和数据类型
数据类型和字符集
InnoDB
InnoDB 检测只读事务
加载和提取数据
MyISAM
`START TRANSACTION READ ONLY` 语句
数据类型/数据类别
查询优化器
优化 SQL 查询
大数据
另请参见 大数据
数据
数据类型
死锁
`DELETE` 查询
索引
`INSERT` 查询
查询缓存
`SELECT` 查询
`SHOW PROFILES`
慢 SQL 查询
类型
`UPDATE` 查询
`ORDER BY` 子句
OWASP 框架
OWASP Top 10

## P
参数化语句
查询解析器
分区数据
哈希定义
键内部
列表/列表列
限制
`NULL` 值
范围/范围列目的
规则
模式、查询和索引
SQL 层
子分区
子表
表值
分区
分区与大数据
按 COLUMNS
磁盘空间
按 HASH 函数
按 KEY
按 LIST
在 MariaDB 中
迷你表
MySQL
`NULL` 值
按 RANGE 修剪
子分区
有分区和无分区的表
包含记录的表
类型
`PASSWORD EXPIRE` 语法
密码管理
Percona Server
字符集与排序规则
免费且开源
MyRocks
TokuDB
XtraDB
完美的模式设计
数据现实
设计制定
ERD 模式
性能改进/重构
选择小数据类型
简单原则
列类型
SQL 客户端
数据库结构
性能小故障
MySQL 性能
物理备份
插件
PostgreSQL
前缀索引
InnoDB 的主要竞争者
数据库管理员
MyISAM
`PRIMARY KEY` 索引
问题用例
可用性问题
可用性、性能和安全
性能小故障
安全问题
修剪

## Q
查询
避免使事情复杂化
避免过度优化数据库
避免重复不良实践
破坏缓存
字符集与排序规则
CRUD
数据类型
数据库管理员
DCL
DDL
开发者
DML
文档
索引
`INSERT`、`SELECT`、`UPDATE`、`DELETE`
优化
优化器
分区
完美的模式设计
性能
`SHOW CHARACTER SET`
性能缓慢
结构
TCL
事务
书面类型
查询不喜欢
列
重复索引
使用 `EXISTS` 而不是 `IN`
`EXPLAIN`
`SHOW STATUS`
存储过程和触发器
查询组件和错误消息
解析器和优化器
查询性能和存储过程

## R
`RAND()`
`RANDOM PASSWORD` 子句
随机值
范围/范围列分区
RBAC 参见 基于角色的访问控制 (RBAC)
关系型数据库管理系统，存储引擎
恢复大数据
MariaDB
恢复陷阱
关系数据库管理系统
复制
配置
实现
延迟
注意事项和提示
安全
同步
类型
受保留账户
基于 REST 的 API
基于 REST 的架构
正确的数据类型
正确的模式
头脑风暴选择
数据库和表服务器
用例
RocksDB
基于角色的访问控制 (RBAC)
Root 用户
R-Tree 索引

## S
清理
模式
薛定谔的表
脚本小子
搜索引擎
`secure-file-priv`
安全
访问控制
账户类别
账户锁定
大数据类别
组件和插件
数据可用性
为 MariaDB 设置防火墙
指南
基于注入的攻击
安装，MariaDB
MariaDB 中的安全态势
措施
PHP 版本
复制
受保留账户
基于联合查询的 SQL
网站/应用所有者
安全指南
企业级安全控制
通用 MySQL 安全
组件和插件
用户安全和访问控制
安全配置错误
安全相关框架
`SELECT … INTO OUTFILE`
`SELECT` 权限查询
`SELECT` 查询
访问尽可能少的数据
应用和搜索
`BETWEEN … AND` 和 `IN` 子句
BreachDirectory
列
基于条件的过滤
`DESCRIBE` 实战
`DISTINCT` 子句
`EXPLAIN`
全表扫描
索引
调查表
调查过程
`JOIN` 操作
`LIMIT` 子句
MariaDB 与 `mysqlslap`
`NULL`
性能
寻找答案
不言自明
`SHOW PROCESSLIST`
SQL 查询性能剖析器
表结构
`SELECT user FROM mysql.user`
服务器组件及与 MySQL 的交互
带宽
硬盘
InnoDB 刷新方法
MySQL 基础设施
`mysqlslap`
操作系统
处理器
存储引擎
压力测试
服务器资源
CPU 信息
专用服务器
文件系统
`innodb_buffer_pool_size`
`innodb_log_file_size`
`join_buffer_size`
内存容量
`mysqld`
`nproc`
分区
共享托管
统计
`table_open_cache` 和 `max_connections`
版本
VPS
`SET` 数据类型
`SET PASSWORD` 查询
`SET PERSIST` 查询
`SET ROLE`
共享托管
共享服务器
`SHOW CHARACTER SET`
`SHOW CREATE TABLE` 语句
`SHOW MASTER STATUS` 查询
`SHOW PRIVILEGES` 查询
`SHOW RELAYLOG EVENTS`
`SHOW REPLICA STATUS` 查询
`SHOW STATUS`
`SHOW TABLES`
`SHOW TABLE STATUS`
`skip_innodb_doublewrite`
慢查询性能
`DELETE`
禁用监控且不加载
启用监控
全文搜索
`INNER JOIN`
`INSERT` 是响应时间监控
`NULL` 值
性能剖析
查询结构
用你的查询 ID 替换 ID
`SELECT`
状态码
子任务
`UPDATE`
通配符
运行缓慢的查询
慢 SQL 查询
软件工程
固态硬盘
空间数据类型
空间索引
空间参考标识符 (SRIDs)
SphinxSE
SQL 客户端
SQL 注入攻击
SQL 查询，在循环中
SQLSTATE 代码
SQL 语句
SQLSTATE 值
SRIDs 参见 空间参考标识符 (SRIDs)
SSD 驱动器
Stack Overflow
无状态
存储
存储引擎
ARCHIVE 和大数据
BLACKHOLE
CSV
EXAMPLE
FEDERATED
InnoDB
`innodb_buffer_pool_size`
`innodb_data_file_path`
`key_buffer_size`
`log_error`
`log_error_verbosity`
基于 LSM 的
MariaDB
`max_allowed_packet`
`MEMORY`（在 MySQL 4.1 之前为 `HEAP`）
MERGE
MERGE（原名 MRG_MyISAM）
MyISAM
NDB
优化
Percona Server
另请参见 Percona Server
Percona XtraDB
端口
原理及基于行的数据库
TempTable
原生
存储要求
存储过程
存储数据
基于字符串的数据类型
子分区
Sucuri
同步复制
语法错误
系统表空间
`SYSTEM_USER` 权限

## T
表模式
TCL 参见 事务控制语言 (TCL)
TCP 端口
`TEXT` 列
威胁检测
威胁建模
威胁扫描
`TIME` 数据类型
时间戳
`TIMESTAMP` 数据类型
TokuDB
流量分析
事务控制语言 (TCL)
事务
触发器

## U
Ubuntu
理解和模拟错误
类别
数据库管理系统
MySQL 错误代码
`SQLSTATE` 代码触发
`UNION` 查询
基于联合查询的 SQL
`uniq` 语句
唯一索引
`UPDATE` 查询
`ALTER` 查询
表中表数据的副本
列上的默认值
导入数据
索引
牢记锁定
`LIMIT` 子句
分区列之后
`SET` 定义表创建
值
`WHERE` 子句
安全用例
MySQL 的用例
ACID
数据库管理系统
数据库管理系统
行业
问题
用户名列
用户注册
用户安全
用户表
`utf8mb4`
UTF-8
Unicode

## V
`validate_password` 组件
VPS
漏洞
漏洞扫描

## W
基于网络的防火墙 (WAF)
网络服务器问题
避免优化你的应用程序
备份策略
CDN 基础设施
磁盘空间不足
操作内存
过载
运行旧的、过时的软件版本
`WHERE` 子句
通配符
WordPress

## X
XtraDB

## Y, Z
`YEAR` 数据类型
