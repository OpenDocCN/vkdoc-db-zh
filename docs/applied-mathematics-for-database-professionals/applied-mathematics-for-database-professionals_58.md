# 在 Oracle 中实现数据库设计

当你在 SQL DBMS 中实施数据库设计时，通常从创建表结构开始。通过 `create table` 命令，你可以在 SQL DBMS 中实现数据库设计的表结构。在本章中，我们将逐一实现 UEX 数据库设计。具体来说，我们将为每个表创建一个对应的 SQL 表结构。

**注意**：本书的目标并非探讨*反规范化*（以提升性能）或其他可能导致偏离正式数据库设计的理由。此外，根据我们的个人经验，鉴于 Oracle 当前 SQL DBMS 中优化器技术的水平，这种一对一的实现通常能提供出色的性能。

