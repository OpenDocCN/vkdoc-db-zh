# 第 3 章 答案


## 第三章 答案

### 练习 2

**a.** 真；集合 `A` 中的所有元素都大于 1。

**b.** 真；存在一个元素 5 属于 `B`，当为变量 `x` 选择该元素时，`mod(x,5) = 0`。

**c.** 假；如果你为 `x` 选择 `A` 中的元素 2，并为 `y` 选择 `B` 中的元素 1，那么 `x + y = 3`，这并不大于或等于 4。因为存在这样的 `x` 和 `y` 组合，所以给定的谓词并非对所有 `x` 和 `y` 的值都成立，因此为假。如果你将集合 `B` 重新定义为 `{3,5,7,9}`，该谓词将为真。

**d.** 真；对于你能从 `A` 中选择的每一个元素，总能在 `y` 中选取一个元素，使得 `x + y = 11`。对于 `x = 2, 4, 6, 8`，分别选取 `y = 9, 7, 5, 3`。

**e.** 假；在集合 `B` 中不存在这样一个值，如果你将该值加到集合 `A` 中每个可用的值上，那么每次相加的结果总是 11。如果你重新定义集合 `A`，使其只包含*一个*元素，这实际上将内部的全称量化降级为了存在量化，此时该谓词将为真。对于 `A = {2}`，你可以选择 `y = 9`。对于 `A = {4}`，你可以选择 `y = 7`。对于 `A = {6}`，你可以选择 `y = 5`。对于 `A = {8}`，你可以选择 `y = 3`。另一种选择是将集合 `A` 重新定义为空集。那么内部的全称量化将为真，与表达式 (`x + y = 11`) 无关。只要 `B` 至少有一个元素，整个谓词就总是为真。

**f.** 真；从 `A` 中为 `x` 和 `y` 都选择 2。你可能认为不允许两次选择相同的值，但在这种情况下是可以的。变量 `x` 和 `y` 是独立地绑定到集合 `A` 的。`y`（与内层）`A` 的绑定不受你为 `x`（与外层 `A` 绑定）所选择的值的影响。

### 练习 3

**a.** `∀x∈A: div(x,2) = 0`

**b.** `∀x∈B: x < 9`

**c.** `∃x∈A: ∃y∈A: ∃z∈A: x ≠ y ∧ y ≠ z ∧ z ≠ x ∧ x + y + z = 18`

### 练习 4

**a.** `∀x∈A: x ≥ 5`

**b.** `∃x∈B: mod(y,2) = 0`

### 练习 7

为了证明这个等价式，你需要从等价式的左边或右边开始，并应用一系列重写规则，最终得到另一边。让我们从左边开始：

```
¬( ∃x∈S: ∀y∈T: P(x,y) )
```

注意，你可以这样看这个表达式：`¬( ∃x∈S: R(x) )`，其中 `R(x) = ( ∀y∈T: P(x,y) )`。现在你可以应用清单 2-15 中的第三条重写规则，结果如下：

```
( ∀x∈S: ¬R(x) )
```

将 `R(x)` 的表达式代回，得到以下结果：`( ∀x∈S: ¬( ∀y∈T: P(x,y) ) )`

现在，你可以对内部（被否定的）全称量化应用清单 2-15 中的第四条重写规则，结果如下：

```
( ∀x∈S: ∃y∈T: ¬P(x,y) )
```

至此，给定等价式的证明结束。

## 第四章 答案

### 练习 3

**a.** 函数。

**b.** 函数。

**c.** 函数。

**d.** 函数。

**e.** 根据枚举法指定，该集合是 `{ (a;f), (b;e), (b;f) }`。这不是一个函数。

**f.** 这不是一个函数。第一个序对包含一个不在 `A` 中的第二个坐标。第二个序对包含一个不在 `B` 中的第一个坐标。

### 练习 6

**a.** 该表达式计算结果为 `{ (X;1), (Y;3), (Z;2), (R;1) }`。这是一个函数。

**b.** 该表达式计算结果为 `{ (X;1) }`。这是一个函数。

**c.** 该表达式计算结果为 `{ (X;1), (Y;3), (X;2) }`。这不是一个函数；第一个坐标 `X` 出现了两次。

### 练习 7

**a.** `{ { (a;1), (b;1) }, { (a;1), (b;2) } }`

**b.** `∅`

### 练习 8

**a.** `{ (ean;9786012), (price;24.99) }`

**c.** `{ (descr;'A very nice book') }`

### 练习 11

**a.** 左边计算结果为 `{ {(empno;104)}, {(empno;106)}, {(empno;102)} }`：一个由三个函数构成的集合。右边计算结果为 `{ {(empno;103)}, {(empno;104)}, {(empno;105)}, {(empno;106)} }`；这是一个由四个函数构成的集合。该命题为假。

**b.** 该表达式计算结果为 `{ {(deptno;10)}, {(deptno;20)} } ⊂ { {(deptno;10)}, {(deptno;20)} , {(deptno;30)} }`。该命题为真；左边的由两个函数构成的集合确实是右边由三个函数构成的集合的真子集。

## 第五章 答案

### 练习 1

**a.** 这不是一个表；并非所有元组都有相同的定义域。

**b.** 这是一个表；事实上它是一个空表。

**c.** 这是一个基于 `{partno}` 的表。

**d.** 这是一个基于 `{suppno,sname,location}` 的表。

**e.** 这是一个基于 `{partno,pname,price}` 的表。

### 练习 2

**a.** `{ p | p∈∏(chr_PART) ∧ p(name)='hammer' ⇒ p(price)>250 }`

**b.** `{ p | p∈∏(chr_PART) ∧ p(price)<400 ⇒ p(name)≠'drill' }`，或 `{ p | p∈∏(chr_PART) ∧ p(name)='drill' ⇒ p(price)≥400 }`

**c.** `{ p | p∈∏(chr_PART) ∧ p(partno) ∈ {10,15,20} ⇒ p(instock)≤42 }`

### 练习 3

`E1` 包含五个元组：来自表 `P` 的四个元组和元组 `{(partno;201)}`。因为它们并非都具有相同的定义域，所以 `E1` 不是一个表。

`E2` 包含五个元组，它们都具有定义域 `{partno,pname,price}`；因此它是一个表。

### 练习 4

`E6`，即 `S` 和 `SP` 的连接，是一个基于 `{partno,suppno,available,reserved,sname,location}` 的表。它包含了来自 `SP` 的六个元组，这些元组被扩展了供应商名称和位置信息。

`E8` 表示 `S` 和 `P` 的笛卡尔积。它是一个基于 `{partno,pname,price,suppno, sname,location}` 的表，包含八个元组。

### 练习 5

`P1` 声称：对于所有能从表 `P` 中选择的零件对，这两个零件都有不同的零件号。这是一个假命题，因为形式化规范允许一个序对包含相同的零件两次。

`P2` 声称：对于所有*不同的*零件对（从表 `P` 中选择），这两个零件都有不同的零件号。这是一个真命题。

## 第六章 答案



### 练习 1

**a.** 该命题指出，表 `PAR1` 中所有偶数编号部件的价格都小于或等于 15。这是一个 **真** 命题。

**b.** 如果你将这个命题重写为全称量化形式，你会发现它声称所有部件的价格都是 5 且当前都有库存。显然，这是一个 **假** 命题。

**c.** 如果你将这个蕴涵式重写为析取式，你会发现该命题指出 `PAR1` 表中有六个部件，其库存数量小于等于 10，或者成本小于等于 10。这是一个 **真** 命题；`PAR1` 表中的所有六个部件都满足该蕴涵式。

### 练习 4

这涉及指定三个子集要求和一个附加约束，以声明 `EMP1` 中的每个元组在其中一个特化关系中都有对应的元组。

```
TRN1⇓{empno} ⊆ EMP1⇓{empno} ∧
MAN1⇓{empno} ⊆ EMP1⇓{empno} ∧
CLK1⇓{empno} ⊆ EMP1⇓{empno} ∧
(∀ e∈EMP1: e(job)='TRAINER'⇒(∃ t∈TRN1: t(empno)=e(empno)) ∧
e(job)='MANAGER'⇒(∃ m∈MAN1: m(empno)=e(empno)) ∧
e(job)='CLERK'
⇒(∃ c∈CLK1: c(empno)=e(empno)))
```

### 练习 5

这涉及指定三个子集要求和一些附加约束，以声明 `EMP1` 中的所有元组都被其特化关系中的**恰好一个**元组所覆盖。

```
TRN1⇓{empno} ⊆ EMP1⇓{empno} ∧
MAN1⇓{empno} ⊆ EMP1⇓{empno} ∧
CLK1⇓{empno} ⊆ EMP1⇓{empno} ∧
TRN1⇓{empno} ∩ MAN1⇓{empno} = ∅ ∧
TRN1⇓{empno} ∩ CLK1⇓{empno} = ∅ ∧
MAN1⇓{empno} ∩ CLK1⇓{empno} = ∅ ∧
#EMP1 = #TRN1 + #MGR1 + #CLK1
```

### 练习 7

这是一个元组-连接谓词。我们根据 `empno` 属性连接 `EMP1` 和 `CLK1`，然后根据 `manager` 属性（这需要属性重命名）连接回 `EMP1`。

```
(∀ e∈(EMP1⊗CLK1)⊗(EMP1◊◊{(manager;empno),(m_deptno;deptno)}): e(deptno)=e(m_deptno))
```

这是一个 **假** 命题；职员 105 和 107 的经理在不同的部门工作。

## 第 7 章答案

### 练习 2

谓词 `o(STATUS)='CONF' ⇒ o(TRAINER)≠-1` 等价于谓词 `o(TRAINER)=-1 ⇒ o(STATUS)∈{'CANC','SCHD'}`。这体现了以下重写规则：`(A ⇒ B) ⇔ (¬B ⇒ ¬A)`

因此，你的回答应该是：添加该元组约束没有带来任何新的限制。

### 练习 3

```
tab_MEMP :=
{ M | M∈℘(tup_MEMP) ∧
/* EMPNO 唯一标识一个元组 */
( ∀m1,m2∈M: m1(EMPNO) = m2(EMPNO) ⇒ m1 = m2 ) ∧
( ∀m∈M: |{ e| e∈M ∧ e(MGR)=m(MGR) }| ≤ 10 )
}
```

在设计此约束时，你可能需要与用户确认 `TERM` 表结构是否应在此约束中发挥作用。

### 练习 6

在 `tab_RESULT`（见清单 7-25）的十三个元素中，只有以下两个可以与给定的 `LIMIT` 表组合：

```
{ ∅
, { { (POPULATION;'DP'),
(COURSE;'set theory'), (AVG_SCORE;'C') }
, { (POPULATION;'DP'),
(COURSE;'logic'),
(AVG_SCORE;'B') }
, { (POPULATION;'NON-DP'), (COURSE;'set theory'), (AVG_SCORE;'E') }
, { (POPULATION;'NON-DP'), (COURSE;'logic'),
(AVG_SCORE;'D') } }
}
```

所有其他结果表要么有数据库专业人士（DP）的平均成绩为 A，要么有非数据库专业人士的平均成绩为 F；这些都被 `DB_U2` 中给定的数据库约束所禁止。

### 练习 7

你可以通过以下方式表达：对于每个部门经理，其所属部门的部门编号必须是该经理所管理部门的部门编号集合中的一个元素。

```
PTIJ5(EMP,DEPT) :=
( ∀d1∈DEPT⇓{MGR}: ↵{ e(DEPTNO)| e∈EMP ∧ e(EMPNO)=d1(MGR)} ∈
{ d2(DEPTNO)| d2∈DEPT ∧ d2(mgr)=d1(mgr) } )
```

注意，这现在不再是一个元组-连接谓词。

### 练习 9

约束 `PTIJ3` 和 `PTIJ4` 防止了这些循环。

### 练习 11

给定的约束抽象地具有以下形式：`(∀o∈OFFR: (P(o) ⇒ (Q(o,REG) ∧ R(o,OFFR)))`

这里 `P`、`Q` 和 `R` 是谓词，其自由变量分别为 `o`、`o` 加 `REG`，以及 `o` 加 `OFFR`。你可以将该谓词形式重写为：

`(∀o∈OFFR: (P(o) ⇒ Q(o,REG)) ∧ (P(o) ⇒ R(o,OFFR)))`

你进而可以将其重写为以下合取式：`(∀o∈OFFR: P(o) ⇒ Q(o,REG)) ∧ (∀o∈OFFR: P(o) ⇒ R(o,OFFR))`

注意，第二个合取项现在只涉及 `OFFR` 表结构，因此是一个表谓词。

## 第 8 章答案

### 练习 3

```
STC(EMPB,EMPE) :=
(∀e1∈EMPB, e2∈EMPE: (e1(EMPNO) = e2(EMPNO) ∧ e1(MSAL) > e2(MSAL))
⇒ e1(SGRADE) < e2(SGRADE))
```

### 练习 4

```
STC2(OFFRB,OFFRE) :=
/* 新的课程安排必须以状态 SCHED 开始 */
( ∀o∈(OFFRE⇓{COURSE,STARTS}-OFFRB⇓{COURSE,STARTS})⊗OFFRE: o(STATUS)='SCHD' )
```

### 练习 5

我们假设日期数据类型上已定义了加法运算。通过增加 31 天，我们形式化地指定了“一个月”。

```
STC(EMPB,EMPE) :=
( ∀e∈(EMPE⇓{EMPNO}−EMPB⇓{EMPNO})⊗EMPE: e(HIRED)<=sysdate+31 )
```

### 练习 6

如果你将 `STC7` 量化蕴涵的结论重写为合取范式，最终会得到六个合取项。按照第 7 章练习 11 的相同方法，你可以将该蕴涵式重写为六个蕴涵式的合取。由此，你可以将全称量化重写为六个量化式的合取。

你可以将 `STC3` 重写为如下合取式：

```
( ∀o1∈OFFRB, o2∈OFFRE:
(o1↓{COURSE,STARTS} = o2↓{COURSE,STARTS} ∧ o1(STATUS) ≠ o2(STATUS))
⇒ (o1(STATUS)='SCHD' ⇒ (o2(STATUS)='CONF' ∨ o2(STATUS)='CANC') )
∧
( ∀o1∈OFFRB, o2∈OFFRE:
(o1↓{COURSE,STARTS} = o2↓{COURSE,STARTS} ∧ o1(STATUS) ≠ o2(STATUS))
⇒ (o1(STATUS)='CONF' ⇒ o2(STATUS)='CANC') )
```

### 练习 9

在以下规约中，`CRSE` 代表终态中的 `CRS` 表。你需要连接到 `CRS` 表以确定课程安排的最后一天。

```
STC7(REGB,REGE) :=
( ∀r1∈REGB, r2∈REGE⊗(CRSE◊◊{(COURSE;CODE),(DUR;DUR)}):
(r1↓{STUD,STARTS} = r2↓{STUD,STARTS} ∧ r1(EVAL) ≠ r2(EVAL))
⇒
( ( r1(EVAL) = -1 ∧ r2(EVAL) = 0 ∧ r2(STARTS) ≤ sysdate ∧
r2(STARTS)+r2(DUR) ≥ sysdate )∨
( r1(EVAL) = 0 ∧ r2(EVAL) ∈ {1,2,3,4,5} ) ) )
```

## 第 9 章答案



## 练习 1

```
{ t↓{empno,name} | t∈dbs(EMP) ∧ t(deptno)=10 }
```

```sql
select e.EMPNO, e.NAME
from EMP e
where e.deptno=10
```

## 练习 3

注意，一个员工只属于一个部门。你可以尝试检索这个问题的答案，但它将始终是一个空集（表）。

```
{ t↓{empno,name} | t∈dbs(EMP) ∧ t(deptno)=10 ∧ t(deptno)=20}
```

```sql
select e.EMPNO, e.NAME
from EMP e
where e.DEPTNO=10 and e.DEPTNO=20
```

## 练习 5

```
{ (message;'约束被违反') | x∈{1} ∧
#{ e | e∈dbs(EMP) ∧ e(job)='PRESIDENT' } > 1 }

∪

{ (message;'约束被满足') | x∈{1} ∧
#{ e | e∈dbs(EMP) ∧ e(job)='PRESIDENT' } ≤ 1 }
```

```sql
select '约束被违反'
from DUAL
where 1 < (select count(*)
            from EMP e
            where e.job='PRESIDENT')

union

select '约束被满足'
from DUAL
where 1 >= (select count(*)
            from EMP e
            where e.job='PRESIDENT')
```

7451AppE.qxd 5/15/07 1:43 PM Page 362

**362**

附录 E **■** 部分练习答案

## 练习 7

“经理”有几种解释方式。经理可以是职位等于 `MANAGER` 的员工，也可以是管理其他员工的员工，或者是管理部门的员工，或是这些情况的任意组合。我们将针对前两种解释给出查询。

```
{ e↓{empno,name} | e∈dbs(EMP)⊗(dbs(GRD)◊◊{(sgrade;grade),(ulimit;ulimit)}) ∧
e(job)='MANAGER' ∧ e(msal)=e(ulimit) }
```

```
{ e↓{empno,name} | e∈dbs(EMP) ∧ e(empno) ∈ { m(mgr) | m∈dbs(MEMP) } ∧
e(msal)=↵{ g(ulimit) | g∈dbs(GRD) ∧ g(grade)=e(sgrade) }
```

```sql
select e.EMPNO, e.NAME
from EMP e
where e.JOB='MANAGER'
  and e.MSAL = (select g.ULIMIT
                from GRD g
                where g.GRADE = e.SGRADE)
```

```sql
select e.EMPNO, e.NAME
from EMP e
where e.EMPNO in (select m.MGR
                  from MEMP m)
  and e.MSAL = (select g.ULIMIT
                from GRD g
                where g.GRADE = e.SGRADE)
```

## 练习 10

此问题的表述方式表明，“经理”指的是管理其他员工的员工（`MEMP` 表结构）。这种情况下，问题的答案很简单：∅。这样的经理不存在，因为有两个元组连接约束规定，经理的收入总是高于他们管理的员工。

你也可以将“他的员工”解释为在该“经理”管理的部门中工作的员工。我们将提供这种情况下的查询：

```
{ em↓{empno,name,msal} | em∈dbs(EMP)⊗(dbs(DEPT)◊◊{(empno;mgr),(mdeptno;deptno)})
∧((∃e∈dbs(EMP): e(job)≠'SALESREP' ∧
   e(deptno)=em(mdeptno) ∧
   e(msal)>em(msal)) ∨
 (∃e∈dbs(EMP)⊗dbs(SREP):
   e(job)≠'SALESREP' ∧
   e(deptno)=em(mdeptno) ∧
   e(msal)+e(comm)/12>em(msal))) }
```

7451AppE.qxd 5/15/07 1:43 PM Page 363

附录 E **■** 部分练习答案

**363**

```sql
select distinct /* 必须使用 distinct！ */
       em.EMPNO, em.NAME, em.MSAL
from EMP em
    ,DEPT d
where d.MGR = em.EMPNO
  and (exists (select '一位"他的员工"收入更高（非销售代表情况）'
               from EMP e
               where e.job <> 'SALESREP'
                 and e.DEPTNO = d.DEPTNO
                 and e.MSAL > em.MSAL)
       or
       exists (select '一位"他的员工"收入更高（销售代表情况）'
               from EMP e
                   ,SREP s
               where e.EMPNO = s.EMPNO
                 and e.DEPTNO = d.DEPTNO
                 and e.MSAL + s.COMM/12 > em.MSAL))
```

## 练习 13

```
{ {(empno;e(empno)),(name;e(name)),(smempno;sm(empno)),(smname;sm(name))}
| e∈dbs(EMP) ∧ m1∈dbs(MEMP) ∧ m2∈dbs(MEMP) ∧ sm∈dbs(EMP)∧
e(empno)=m1(empno) ∧ m1(mgr)=m2(empno) ∧ m2(mgr)=sm(empno) }
```

```sql
select e.EMPNO, e.NAME, sm.EMPNO as SMEMPNO, sm.NAME as SMNAME
from EMP e
    ,MEMP m1
    ,MEMP m2
    ,EMP sm
where e.EMPNO = m1.EMPNO
  and m1.MGR = m2.EMPNO
  and m2.MGR = sm.EMPNO
```

## 练习 21

```
{ c↓{code,dur} ∪
{(2006_offerings; { o↓{starts,status} ∪
{(students; #{ r| r in dbs(REG) ∧
r↓{course,starts}=o↓{course,starts} )}
| o∈dbs(OFFR) ∧ o(course)=c(code) ∧ year(o(starts))=2006 }
)}
| c∈dbs(CRS) ∧ c(code) ∈ {'DB1','DB2','DB3'} }
```

7451AppE.qxd 5/15/07 1:43 PM Page 364

**364**

附录 E **■** 部分练习答案

```sql
select c.CODE, c.DUR, o.STARTS, o.STATUS
      ,(select count(*)
        from REG e
        where e.COURSE = o.COURSE
          and e.STARTS = o.STARTS) as students
from CRS c
    ,OFFR o
where c.CODE in ('DB1','DB2','DB3')
  and c.CODE = o.COURSE
  and to_char(o.STARTS,'YYYY') = '2006'
```

注意，SQL 表达式重复了 `CRS` 的信息。

## 第 10 章答案


## 附录 E：部分习题答案

## `习题 1`

首先，涉及两个表约束：

```
/* 与会者和开始日期唯一标识一个元组 */
( ∀r1,r2∈R:
r1↓{STARTS,STUD} = r2↓{STARTS,STUD} ⇒ r1 = r2 )

/* 课程提供由所有与会者评估，或尚未到评估该课程提供的时候 */
( ∀r1,r2∈R:
( r1↓{COURSE,STARTS} = r2↓{COURSE,STARTS} )
⇒
( ( r1(EVAL) = -1 ∧ r2(EVAL) = -1 ) ∨
( r1(EVAL) ≠ -1 ∧ r2(EVAL) π -1 )
) )
```

如果某位管理员已经注册了该课程提供，则第一个约束条件可能被违反。

无论`初始状态`如何（假设 3 月 1 日仍在未来；即，已存在的其他注册信息仍然显示`EVAL=-1`），第二个约束条件都无法被事务`ETX1`违反。

`PTIJ8`（入职前四周内不能注册课程提供）被涉及，并且由于`WHERE 子句`中`e.HIRED`介于`sysdate - 91`和`sysdate`之间的谓词，它无法被`ETX1`违反。

`PTIJ9`（不能在离职日期当天或之后注册课程提供）被涉及。对于`ETX1`，`初始状态`应反映过去三个月内雇佣的所有管理员仍在为公司工作。

`PTIJ10`（不能注册时间重叠的课程）被涉及。对于`ETX1`，`初始状态`应反映这些管理员中无人已注册另一个与 3 月 1 日的`AM4DP`课程提供时间重叠的课程。

`PTIJ13`（培训师不能注册由其本人教授的课程提供）被涉及。此约束无法被违反，因为管理员不能担任某个课程提供的培训师。

`PTIJ15`（不能注册与你作为培训师的另一门课程时间重叠的课程提供）被涉及。由于与`PTIJ13`相同的原因，此约束也无法被违反。

`PODC4`（注册人数超过六人的课程提供必须具有“已确认”状态）被涉及。根据当前的注册人数、该课程提供的当前状态以及被`AM4DP`课程提供注册的管理员人数，`ETX1`很可能会违反此约束。

`PODC5`（注册人数不能超过课程提供的最大容量）被涉及。同样，根据当前的注册人数以及`ETX1`添加的人数，此约束也可能被违反。

`PODC6`（已取消的课程提供不能有注册信息）被涉及；如果该课程提供的当前状态等于`'已取消'`，则此约束将被违反。

## `习题 3`

事务`ETX4`的`UPDATE`语句涉及以下完整性约束：`PTIJ1`、`PTIJ3`、`PTIJ4`、`PTIJ6`和`PTIJ7`。除`PTIJ3`外，其余所有约束都有被该语句违反的风险。

### 索引

## 数字

## B

`2VL`（二值逻辑），262

行前触发器，249

`3VL`（三值逻辑），262，340–346

语句前触发器，249，268

`4VL`（四值逻辑），341，345

`初始状态`，186，226，229，296

二元运算符，14

## 符号

二元关系，68–70

`BL 代码`（业务逻辑代码），244

`/* ... */` 注释，144，315

布尔，乔治，4

布尔代数，4

## A

博尔科夫斯基，卢德维克·斯特凡，341

缩写连续嵌套量词，56

自底向上的数据库全域规约方法，141

吸收律，50

约束变量，8

行后触发器，249

可允许的元组，82

商业应用，243–247

语句后触发器，249，268，274

业务逻辑代码（`BL 代码`），244

聚合运算符，111

业务规则，139

代数性质，47–51

`alter table add constraint` 命令，248，259，262

规范形，59

`alter table` 命令，248，292

集合的基数，29，81

`始终执行模型`，268

笛卡尔连接，108

应用锁，286–289

笛卡尔积，39，68，191

结合性，11，19，48

特征描述，78，139，141

`属性约束`，142，222，224，306

样例数据库设计与，147–151，314

数据完整性代码与，246

实现，259–262

可允许元组集与，142

属性-值对，81


检查约束, 265, 338, 344

属性-值集, 73, 78, 314, 339

属性约束及, 259

属性, 78

元组约束及, 262

值的适用性及, 337, 341

选择运算符, 30

重命名, 82, 108

数据完整性分类模式

自主事务, 286

约束, 142

`AVG` (平均运算符), 111, 235

**367**

7451Index.qxd 5/22/07 9:52 AM Page 368

**368**

## ■索引

封闭世界假设, 81

### D

运算符的闭包性质, 75, 102

数据完整性谓词, 185–190

CNF。参见合取范式

数据完整性代码 (DI 代码), 245, 247,

Codd, E. F., 5, 68, 74, 345

注释 (/* ... */), 144, 315

高效的, 248, 250, 254

`COMMIT` 语句, 266

执行模型, 265–284

提交触发器, 300

实现, 247–255

可交换性, 19, 48

数据完整性约束, 224, 306–307

相容函数, 75

与业务规则比较, 139

复合谓词的复杂性, 9

分类模式, 142

谓词的组成部分, 9

数据完整性代码及, 245

复合逻辑表达式, 真值

延迟检查, 299–303

表及, 11

文档记录, 140

复合谓词, 9, 10, 53

示例数据库设计及, 158

概念骨架。参见数据库

数据完整性谓词, 117–138, 306

骨架

数据管理, 关系模型用于,

并发事务, 266, 284, 298

5, 68, 81

合取成员 (合取项), 13,

数据操作, 221–237, 306

数据操作代码, 243

合取, 8, 12, 341

数据检索, 199–220, 306

合取范式 (CNF), 59, 63

数据检索代码, 243

元组约束及, 264

数据值, 适用性及, 337, 341

元组谓词及, 120

数据库特征描述, 170, 324

连接词, 8–11, 340

数据库约束, 143, 163, 306, 338

约束规范, 81

数据完整性代码及, 246

约束验证查询, 245, 268,

实现, 291–296

282, 289

示例数据库设计及

约束

违规及, 299

延迟检查, 299–303

数据库设计 (示例), 143–181, 199,

RuleGen 框架及, 303

矛盾, 17

数据库约束及, 170–181

有序对的坐标, 38, 68, 305

数据库谓词及, 170,

计数运算符, 111, 209

175–181

课程 (CRS), 312

数据库骨架及, 144–147

属性约束对于, 261

数据库全域及, 167–181

表全域对于, 321

形式化规范, 311–331

元组约束对于, 264

状态转换约束对于, 186,

`CREATE ASSERTION` 命令, 242

192–196

`create table` 命令, 248, 255–259

数据库设计

`create trigger` 命令, 269

定义, 92

实现, 241–304

形式化指定, 139–184

7451Index.qxd 5/22/07 9:52 AM Page 369

## ■索引

**369**

数据库管理系统 (DBMS),

定义

93。另见 SQL DBMS

属性重命名运算符, 109

数据库谓词, 117–136, 306

平均运算符 (`AVG`), 112

模式, 127–136

二元关系, 69

示例数据库设计及, 170,

集合的基数, 29

175–181

笛卡尔积, 39

数据库骨架, 100, 139, 141

相容函数, 77

示例数据库设计及, 144–147

数据库, 93

规范, 313

数据库设计, 92

数据库状态空间, 127

数据库管理系统

数据库状态, 79, 98, 306

(DBMS), 93

约束及, 91–93, 266

数据库状态, 93

数据完整性约束及, 143

数据库全域, 92

定义, 93

数据库变量, 92

查询及, 199–201

不相交集合, 34

形式化指定, 98

Find

自顶向下视图, 141

函数复合, 83

事务及, 221–233

函数, 71

faster

数据库全域, 36, 77, 139, 306

集函数的广义积,

athttp://superinde

定义, 92

标题, 96

查询及, 199–216

连接运算符, 108

示例数据库设计及, 167–181

可连接函数, 77

规范, 325–330

函数的局限性, 76

形式化指定, 141

最大运算符 (`MAX`), 112

x.apr

事务及, 221–236

最小运算符 (`MIN`), 112

数据库变量, 定义, 92

单元并运算符, 36

ess.com/

数据库

有序对, 38

定义, 93

划分, 37

文档记录, 140

幂集, 35

Date, Chris, 7

投影运算符, 104

DBMS (数据库管理系统),

集函数, 78


### 索引

**■** 索引

## 370

93. *另见* `SQL DBMS`

集合，24

`dbms_lock 包`，286

单例模式，29

De Brock, Bert，142，229

特化谓词，133

De Morgan 律，19，50

子集需求谓词，129

陈述句，5

子集，30

声明式策略，用于`DI 代码`执行，248，254

求和算子，40

表，94，95

`可延迟约束`，300

表设计，93

延迟数据完整性检查

表结构，93

约束，299–303

`元组-连接谓词`，136

`唯一标识谓词`，128

## E

`DELETE 语句`，229，235，243，266

删除`TE 视图`，275

元素，24

删除触发器，249

嵌入式过程策略，用于`DI 代码`执行，251，254

`员工历史记录（HIST）`，312

其属性约束，260

其表定义域，323

`员工（EMP）`，312

其属性约束，259

其表定义域，315

空集，34–35，54，95

终止状态，186，226，229，296

实体-关系图（`ERDs`），68

`部门（DEPT）`，312

枚举方法，25

其属性约束，261

等式公理，39

其表定义域，319

等价，8，14

`DI 代码`。*参见* 数据完整性代码

`ERDs`（实体-关系图），68

对角线符号，30

执行模型

差运算符，101，103，305

数据完整性代码的，265–284

函数与，75

延迟检查与，300

集合与，31–35

练习

SQL 与，213

答案，347–365

直接推理（假言推理），18

二元关系，85

有向图，190

数据完整性谓词，138

不相交集合，34

数据库设计规范，183

析取项（成员），13，61

函数，85

析取式，8，13，341

逻辑，22，64

析取范式（`DNF`），60

查询，219

量词的分配律，57

集合，42

分配律，19，49

状态转换约束，197

`DML 语句`，243，266

表，114

延迟检查与，299

事务，236

触发器，249

存在量化，47，51–59，305

`DNF`（析取范式），60

聚合与，112

数据库/数据完整性文档化

嵌套，205

表达式，格式不良，8

函数的定义域，71

延拓算子，109，306

双重否定（对合），19，50

外部谓词，79，139，146，314

`DUAL 表`，268

双目运算符，14，36

## F

动态约束。*参见* 状态转换约束

`FALSE 值`，340，344

有限集，29，41

闪回查询，296

**■** 索引

## 371

外键约束，312

`涉及列及其涉及表极性执行模型`与，274

外键，291

`转换效应属性执行模型`与，277

自由变量，7

`转换效应属性加优化查询执行模型`与，282

Frege, Gottlob，4

`检查属性`，134

函数复合，82

谓词实例化，7

函数完全性，16，62，262

交运算符，101–102，305

函数，67–87，305

函数与，75

定义域/值域，71

集合与，31–35

映入集合，72

SQL 与，213

局限性，76

对合（双重否定），50

操作，75–77

**G**

集合上的函数，70

不可约子集，128

子集，74

itfaster

符号，333

泛化谓词，134，306

集合函数的广义乘积，36，a 79–81，97

**J**

`连接属性`，108

Gödel, Kurt，4

`连接算子`，106，203，306

可连接函数，75

**H**

标题，96

**L**

Hilbert, David，4

`分层方法`，118–119，141

x.apr

人类推理，3，17

Leibnitz, Gottfried，4

ess.com/

混合方法，27

函数的局限性，76

逻辑，3–22，47–65，305

**I**

历史，4

数学符号与，334

幂等性，19，50

逻辑与，8

同一性，48

逻辑联结词，8–11，47–51，305

恒等函数，74

函数完全性与，16

格式不良表达式，8

蕴涵，8，13，14

`蕴涵重写规则`，19


