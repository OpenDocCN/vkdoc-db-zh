# 第 7 章 指定数据库设计

**161**

这个集合有 13 个元素，每个元素都是一个表。第一个代表空表；其他 12 个都是由 4 个元组组成的集合。你可能想自己验证一下，*所有*这些表是否都满足指定的四个表约束，并且*没有其他*表（未在代码清单 7-25 中列出）也满足所有这四个表约束。

如果你在阅读这个嵌套的集合时遇到困难，图 7-3 可能会澄清其中的含义。它展示了另一种使用表的简写符号来指定表全域 `tab_RESULT` 的方法。

**图 7-3.** *使用表简写符号对 `tab_RESULT` 进行枚举式指定*

`注意` 通常，表全域包含的元素数量太多，无法一一列举。

现在让我们继续我们的示例数据库设计的指定。看看代码清单 7-26，它展示了 `EMP` 表结构的表全域 `tab_EMP` 的定义。

此定义正式指定了 `EMP` 的四个表约束。一些非正式的说明作为注释嵌入其中。

7451CH07.qxd 5/15/07 9:43 AM Page 162

**162**

