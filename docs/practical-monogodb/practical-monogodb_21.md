# MongoDB 技术概念参考

## 核心概念

### ACID 事务
*   `ACID` 与 `BASE` 对比

### CAP 定理
*   `CAP` 定理
*   `BASE` 缩写
*   布鲁尔定理（参见 `CAP` 定理）

### 大数据
*   定义
*   数据访问、定制化、数据模型、决策制定
*   `IDC` 的分析
*   信息与知识、创新、遗留系统、策略与流程、行业细分
*   数据规模、来源统计、技术与技巧
*   `Twitter` 类型、可见性用途
*   3V 特性（参见`数据量、多样性与速度`）

## 数据存储与建模

### BSON
*   二进制 `JSON` (`BSON`)
*   32 位与 64 位对比

### 数据文件
*   数据文件
*   日志文件
*   `MMAPv1`
    *   `mydbpoc` 数据文件
    *   命名空间（参见`命名空间 (.ns 文件)`）
    *   `storage.mmapv1.smallFiles` 选项
*   `WiredTiger` 存储引擎

### 数据模型
*   `MongoDB` 数据模型
    *   二进制 `JSON` (`BSON`)
    *   `capped collection`
    *   数据库模型、文档、动态模式、标识符 (`_id`)、`JSON`、多态模式
*   决策因素
    *   应用增长
    *   文档与多个集合对比
    *   操作因素、引用
    *   关系型数据库

### 数据库对象
*   `BTree`、`bucket` 数据结构、关键节点
*   集合
    *   `capped collection`
    *   命名空间
    *   数据文件（摘要）
    *   数据记录结构、删除列表详情
    *   区域 (`extent`)、元数据、记录数据结构

### 存储引擎
*   `WiredTiger` 存储引擎
    *   数据压缩算法、内存映射文件、虚拟内存
    *   数据 `db.serverStatus()` 命令

### 标识符
*   标识符 (`_id`)

### 命名空间
*   命名空间 (`命名空间 (.ns 文件)`)
    *   `MMAPv1`
    *   集合 `bucket` 数据结构
    *   `$freelist` 限制、索引、`BTree`

## CRUD 操作

### 查询操作符
*   条件操作符（用于 `aggregate()` 等）
    *   `$gt` 和 `$gte` 操作符
    *   `$in` 和 `$nin` 操作符
    *   `$lt` 和 `$lte` 操作符
*   正则表达式

### 查询方法
*   `find()` 命令
*   `findOne()` 方法
*   `explain()` 命令
*   `hint()` 方法
*   `next()` 函数
*   `printjson()` 函数
*   `skip()`、`sort()`、`limit()`

### 数据操作
*   读取与写入
    *   `MongoDB` `compact` 命令
    *   修改操作
    *   `SkipList`、更新操作
    *   `WiredTiger` 缓存、写入操作
*   `remove()` 方法
*   `update()` 命令
*   `validate()` 选项

### 查询处理
*   集合创建、命令提示符、创建与插入选项
*   `CSV` 文件、游标对象、数据库服务器、`db` 命令
*   删除、`explain()` 函数、`findOne()` 方法、`for` 循环
*   `hasNext()` 函数、`help()` 命令、`_id` 字段、导入命令
*   索引（参见`索引`）、`limit()`
*   `localhost` 数据库服务器、`MyDB` 数据库、`MYDBPOC` 数据库
*   `next()` 函数、`printjson()`、投影器 (`projector`)
*   查询文档、读取选择器、`skip()`、`sort()`
*   `SQL` 术语和概念

## 索引

### 索引类型
*   `BTree` 结构
*   复合索引
*   地理空间索引
    *   `Geohaystack` 索引
    *   地理空间索引
*   多键复合索引
*   稀疏索引
*   `TTL`（生存时间）索引
*   唯一索引

### 索引管理
*   `dropIndex` 命令
*   `ensureIndex()` 函数
*   `reIndex` 命令
*   `system.indexes`

### 索引行为
*   行为与限制
*   数据结构
*   `ensureIndex()` 事件
*   `explain()` 命令
*   特性
*   `find()` 操作
*   `hint()` 方法
*   `_id` 索引
*   交集限制
*   读取操作
*   关系型数据库中的二级索引
*   单键索引
*   排序操作

## 复制与分片

### 复制
*   数据复制过程
    *   密钥、成员启动、正常操作
    *   操作日志 (`oplog`)、要求
    *   从库链式同步、通过链式从库的写入
*   主从复制
*   副本集
    *   仲裁者成员、配置、集群（参见`集群`）
    *   一致性、数据中心
    *   延迟成员、部署架构
    *   选举、故障转移、容错
    *   心跳消息交换、隐藏成员
    *   限制、主从复制
    *   成员、非投票成员、要点
    *   主节点、读写关注
    *   回滚、扩展读取
    *   次节点、状态报告、次要成员类型
*   `rs.status()` 命令

### 分片
*   分片环境
    *   块大小、预分片、分片键
*   分片集群
    *   集合限制、正确的分片键、问题、限制
    *   `MongoDB` 操作、关系型数据库、更新系统
    *   `add shard` 命令、均衡器、均衡过程
    *   跨集合的块、命令、组件、`ConfigServers`
    *   `count()` 命令、数据目录、数据库与集合
    *   数据分布过程、基于哈希的分区
    *   实现、`listshards` 命令、内存、迁移
    *   监控与分析、`moveChunk` 命令、操作
    *   主分片、`printShardingStatus()` 命令
    *   基于范围的分区、移除集群场景
    *   服务器、`serverStatus` 命令、设置集群
    *   `ShardedEnvironment`、分片键、单一逻辑数据库
    *   基于标签的分片（参见`基于标签的分片`）
    *   终端窗口、`testcollection.count` 命令
*   基于标签的分片
    *   块分布、集合分布、多重标签
    *   先决条件、通过标签扩展、标签化

## 部署与管理

### 部署
*   部署策略
    *   `CPU`、数据重要性、磁盘类型、硬件组件、内存
    *   `MongoDB` 2*1 `NUMA` 硬件要点
    *   分片与复制
*   安全部署
    *   `admin` 数据库与用户
    *   认证、授权、启用认证、级别
    *   网络暴露、`practicaldb` 数据库、产品数据库
    *   基于角色的方法、`system.users` 集合
    *   用户与启用授权
*   独立部署

### 云管理与监控
*   `Cloud Manager`
    *   账户、配置、自动化代理
    *   部署组创建、安装说明
    *   `MMS` 监控解决方案、审查与部署按钮
    *   独立实例
*   监控与分析
    *   配置服务器、锁状态、服务分片状态
    *   均衡与块分布、系统问题诊断
    *   `MongoDB` Cloud Manager、`mongod` Web 界面
    *   `mongostat`、第三方工具
    *   批量插入事件、`CSV` 格式、数据管理
    *   插入数据操作、查询模式、模式设计
    *   分片集群用例、`moveChunk` 命令

### 性能与维护
*   性能与特性对比
*   时间优化
*   固态硬盘 (`SSD`)
*   `mongodump` 和 `mongorestore`
    *   备份实用程序、集合级备份
    *   数据库还原、`--drop` 命令、转储文件夹
    *   `--help` 选项、`mongorestore` 还原数据库
    *   单一数据库备份、终端窗口
*   服务器管理
    *   集合级、数据损坏数据库、`db.ServerStatus()`
    *   启动命令、状态、停止命令
*   单一数据库备份

### 命令与工具
*   `mongo` Shell
*   `mongod` Web 界面
*   `mongoimport` 命令
*   `dropDuds`（应为 `dropDups`）
*   `fsync` 和锁命令
*   `help()` 命令
*   `map()` 命令（应为 `mapReduce` 框架）
*   `next()` 函数

## 编程接口与工具

### 驱动与 API
*   `PyMongo` 驱动
*   `GridFS` API
    *   块文档、文件系统、`find()` 命令
    *   `fs.files` 文档、`read()` 命令
    *   规范、存储文件、结构、`write()` 命令

### 数据导入导出
*   `mongoexport`
*   `mongoimport`

### 应用示例
*   博客应用程序
    *   `ACID` 事务、`Brewer` 定理（参见 `CAP` 定理）
    *   关系型数据库图、规范形式、问题

## 数据安全与一致性

### 数据安全
*   数据安全与一致性
*   敏感字段
*   应用安全部署（参见`安全部署`）
*   安全认证、加密限制

### SQL 对比
*   结构化查询语言 (`SQL`)
    *   `ACID` 事务、对比、数据更新查询
    *   `RDBMS` 数据库、可扩展性、模式灵活性
    *   `SQL` 与 `NoSQL`、技术场景

## 概念与理论

### NoSQL
*   `BASE` `NoSQL` 数据库
    *   表示法、配置、`NRW`
    *   `RDBMS` `ACID` 事务、读写操作
*   `NoSQL`
    *   优点、类别、定义、缺点
    *   历史、非关系型数据库、`SQL`
    *   参见（结构化查询语言）
    *   结构化与非/半结构化数据对比

### 其他概念
*   `capped collection`
*   区分大小写的查询
*   分块 (`Chunks`)、块拆分
*   聚合 (`aggregate()`) 操作
*   应用写关注点、命令部署步骤、`w` 选项、`writeConcern`
*   数据生命周期管理
*   遗留系统（大数据）
    *   数据处理、数据存储结构
*   `LINUX` 系统安装、仓库
*   `MapReduce` 框架
*   `NoSQL` 方法（非关系型）
*   面向对象编程
*   生产集群架构
    *   架构组件、配置服务器可用、`mongos` 不可用
    *   副本集不可用、分片不可用
*   引用模型 (`Referencing model`)
*   社交网络
    *   评论创建操作、帖子视图、模式设计
    *   序列创建、分片解决方案
*   结构化与非/半结构化数据
*   事务
*   `3V` 特性（数据量、多样性与速度）
    *   方面与大数据、数字宇宙规模、多样性、速度、数据量
