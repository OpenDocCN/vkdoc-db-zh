# SQL Server 数据工具与技术参考

## SQL Server 数据工具 (SSDT)

## SQL Server 集成服务 (SSIS)
-   连接管理器
-   控制任务
-   数据库和表级别
-   数据流
-   DDL
-   删除操作
-   对话框-控件操作
-   执行 SQL 任务
-   插入操作
-   OLEDB
-   包
-   点
-   程序运行
-   源任务
-   更新操作
-   工作流程

## SQL Server 元数据资源管理器 (SSME)

## SQL Server 迁移助理 (SSMA)

## SQL Server 源
-   临时数据加载
-   CLR 数据类型
-   OLEDB 提供程序
-   工作流程

### 临时数据加载至 SQL Server Azure
-   导入/导出向导
-   `OPENROWSET` 和 `OPENDATASOURCE`
-   工作流程

### 复杂子集数据传输
-   快照复制
-   工作流程

### `COPY_ONLY` 备份

### 表间复制粘贴

### 从另一 SQL Server 实例获取数据（永久连接）
-   配置
-   链接服务器插入
-   链接服务器选项
-   网络名称
-   工作流程

### SQL Server 数据库间数据传输
-   `BULK INSERT`
-   `SSIS` 大容量插入任务
-   工作流程

### 无需复杂程序的数据传输
-   `SSMS` 脚本化数据
-   工作流程

## ETL 流程
-   `SSIS` 大容量插入任务
-   `SSIS SQL Server` 目标
-   工作流程

### 表的导入和导出
-   `BCP` 导入导出脚本
-   不同版本的 `SQL Server`
-   工作流程

### 功能特性

### 使用命令行加载导出的数据
-   `BCP.exe`
-   `BCP` 选项
-   数据类型
-   `QueryOut`
-   工作流程

### 使用 `T-SQL` 加载大型数据集
-   `BCP` 文件
-   `BULK INSERT` 选项
-   本地磁盘
-   工作流程

### 原生 `SQL Server` 文件
-   提示信息
-   非日志操作
-   `OPENROWSET (BULK)` 命令
-   选项
-   `ROWS_PER_BATCH`
-   工作流程

## SQL Server Azure
-   基于云的数据库连接
-   连接参数
-   命令行
-   工作流程

### SQL Server Azure，常规 `ETL` 流程
-   连接管理器
-   `OLEDB` 连接
-   `SSIS` 和 `.NET` 连接
-   工作流程

## SQL Server 表
-   执行 SQL 任务编辑器
-   Foreach 循环编辑器
-   脚本任务编辑器

## `SSIS` 包

### 数据库间表传输
-   方法、优缺点
-   属性
-   `SMOServer` 连接管理器
-   `SQL Server` 传输对象任务编辑器
-   工作流程

## SQL Server 表

## 二进制大型对象 (BLOBs)

## 动态 `SQL`

## `OPENROWSET(BULK)`

## 即席动态 `SQL`

## 图像和文档
-   `ItemImage` 列
-   导入多个文件

### 使用 `SSIS`
-   导入列高级编辑器
-   `FileDataColumnID`
-   导入列任务
-   导入多个文件
-   输入/输出列
-   脚本转换编辑器
-   `SSIS` 转换

### 使用 `T-SQL`
-   工作原理

## `SSIS` 数据流
-   清理数据
-   `DQS` 清理域
-   `DQS` 连接管理器
-   数据反规范化
-   多数据源整合
-   多关系表，数据规范化
-   数据透视
-   在 `SQL Server 2005` 和 `2008` 中
-   在 `SQL Server 2012` 中
-   缓慢变化维度 (SCDs)
-   核心流程
-   数据流任务
-   [派生列转换](#9781430247913_Ch09.xhtml#cXXX.



