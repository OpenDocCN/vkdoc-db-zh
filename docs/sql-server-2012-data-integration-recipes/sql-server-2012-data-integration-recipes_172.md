# 技术术语索引

## 概述

`lookup columns`，`merge column mapping`，`Multicast and conditional split task`，`unpivot source data`，`SSIS load package`，`data concatenation`，`SSIS Script task`，`working principle`，`fixed-length column`，`remove duplicate records`，`title case conversion`，`variable-length column`，`Transparent network substrate (TNS)`，`T-SQL`，`attributes profiling functions`，`change tracking and data capture`，`destination table`，`source table changes`，`SQL Server transaction log`，`data concatenation`，`denormalize data, reference/lookup tables`，`linked server`，`MERGE function`，`MERGE Is Not Practical`，`linked server`，`ADAMREMOTE`，`working process`，`multiple data source consolidation`，`multiple relational table, normalize data`，`pivoting data`，`data denormalization`，`SELECT statement`，`remove duplicate records`，`advantages`，`testing`，`use temporary tables and ROW_NUMBER()`，`working principle`，`SCDs`，`limitations and drawbacks`，`MERGE command`，`track changes`，`upsert techniques`，`source table`，`structured ETL process`，`CarSales`，`control flow pane`，`Create staging table`，`drop staging table`，`MERGE function`，`OLEDB source`，`SSIS package`，`subsetting column data, string manipulation`，`title case conversion`，`unpivot command`。

## T, U

`Transparent network substrate (TNS)`，`T-SQL`，`attributes profiling functions`，`change tracking and data capture`，`destination table`，`source table changes`，`SQL Server transaction log`，`data concatenation`，`denormalize data, reference/lookup tables`，`linked server`，`MERGE function`，`MERGE Is Not Practical`，`linked server`，`ADAMREMOTE`，`working process`，`multiple data source consolidation`，`multiple relational table, normalize data`，`pivoting data`，`data denormalization`，`SELECT statement`，`remove duplicate records`，`advantages`，`testing`，`use temporary tables and ROW_NUMBER()`，`working principle`，`SCDs`，`limitations and drawbacks`，`MERGE command`，`track changes`，`upsert techniques`，`source table`，`structured ETL process`，`CarSales`，`control flow pane`，`Create staging table`，`drop staging table`，`MERGE function`，`OLEDB source`，`SSIS package`，`subsetting column data, string manipulation`，`title case conversion`，`unpivot command`。

## V

`Visual FoxPro Files`，`applications`，`import data`，`OLEDB provider`，`potential ways`，`SQL server`，`SSIS`。

## W

`Web Services`，`data flow`，`derived column transformation`，`ETL process`，`HTTP connection manager`，`input pane`，`output pane`，`script task editor`，`SSIS provider`，`variables pane`，`working principle`，`Web Services Description Language (WSDL)`，`Windows Management Instrumentation (WMI) Data`，`connection manager`，`into SQL server`，`reader task editor`，`script transformation editor`，`standard data flow`，`working principle`。

## X, Y, Z

`XML data sources`，`advantages`，`disadvantages`，`ETL process, import`，`data type warning`，`handling error`，`node selection`，`source advanced editor`，`SSIS data flow`，`T-SQL code`，`using SSIS`，`XML source editor`，`XSD file`，`flattening to import`，`uses, XSLT`，`XSLT file`，`XSLT transformation`，`importing complex files`，`denormalize`，`views, file structure`，`import large file, priority speed`，`Client_XMLBulkLoad`，`component object model`，`ErrorLog file`，`flattened file`，`mapping attributes`，`mapping schema`，`potential problems`，`semistructured XML`，`SQLXML`，`loading files for storage`，`destination table`，`dynamic SQL`，`encoding XML`，`source file`，`SSIS package`，`T-SQL code`，`XML data`，`XML fragment`，`XML schema`，`loading multiple tables`，`locating XML file`，`required tables`，`table dump`，`.vbs script`，`XML fragment`，`.xsd file`，`overcoming bulk load`，`crafting schema`，`data overflow`，`mechanism`。


## 加载和分解关系表

*   `options`
*   `TempFilePath`
*   `types`
*   `UNC path`
*   `VBScript file`
*   `.vbs file`
*   `relational tables, load and shred`
*   `execution`
*   `foreign key, creation`
*   `independent tables`
*   `locating file`
*   `parent–child relationship`
*   `primary and foreign key`
*   `relationships`
*   `XML elements`
*   `XML schema`
*   `rows and columns, load`
*   `attribute centric file`
*   `ColPattern`
*   `element centric file`
*   `OPENXML`
*   `pattern and schema`
*   `RowPattern`
*   `sp_xml_preparedocument`
*   `table query`
*   `to relational structure`
*   `T-SQL code`
*   `shredding into SQL server table`
*   `code snippet`
*   `CROSS APPLY`
*   `indexes`
*   `locating XML file`
*   `staging variable`
*   `T-SQL and XQuery`
*   `XQuery`

## 导入 SQL Server

*   `data access`
*   `Foreach loop`
*   `selecting file`
*   `SQLXML bulk loader`
*   `ETL process`
*   `execute`
*   `expressions`
*   `parameters`
*   `variables`
*   `.vbs file`
*   `XSD file`
*   `validating, scripted solution`
*   `properties`
*   `SuccessValue`
*   `VBScript`
*   `.vbs file`

## 在 SSIS 中验证

*   `data flow task`
*   `execute flow`
*   `import region`
*   `.NET XML schema`
*   `replacing procedure`
*   `variables`
*   `XML validation`