# W, X, Y, Z

## WAITFOR 等待类型
`WAITFOR` 等待类型。

## 等待统计
等待统计抽象视图、`DBCC` 命令、执行计划、实际选项、每查询分析属性类型、可视化表示、`WaitStats` 属性、扩展事件特性/配置选项、硬件/数据库配置历史、数学公式、Perfmon、阶段和队列、请求、可运行队列、`RUNNING` 阶段、调度器/任务/工作线程、处理器、页请求、调度器、调度模式、会话、源代码、`sys.dm_exec_sessions`、任务、工作线程、调度、SQLOS、线程调度、等待者队列、等待队列。

## WAIT_XTP_CKPT_CLOSE 等待类型
`WAIT_XTP_CKPT_CLOSE` 等待类型。

## WAIT_XTP_HOST_WAIT 等待类型
检查点进程定义、文件组、文件、内存优化表、结果、`sys.dm_os_waiting_tasks`、`sys.dm_os_wait_stats/sys.dm_os_waiting_tasks`、测试数据库创建。

## WAIT_XTP_OFFLINE_CKPT_NEW_LOG 等待类型
检查点进程定义、离线检查点进程、`sys.dm_os_waiting_tasks`。

## WRITE_COMPLETION 等待类型
`WRITE_COMPLETION` 等待类型。
