# S

## 服务器管理工作室 (SSMS)
数据库属性、操作模式。

## 共享意向更新 (SIU)
SIU。

## 共享意向排他 (SIX)
SIX。

## 网站可靠性工程师
网站可靠性工程师。

## SLEEP_BPOOL_FLUSH 等待类型
`CHECKPOINT` 命令、检查点进程、配置选项、数据修改过程、定义、`LOGBUFFER` 和 `WRITELOG`、查询结果、`TLog_demo` 数据库、事务日志。

## 坚实基线
坚实基线。

## SOS_SCHEDULER_YIELD 等待类型
复杂计算/数据类型转换、CPU 查询、GalacticWorks 数据库、`Ostress/RML` 实用程序、阶段/队列、时间片、查询存储、可运行队列情况、`SOS_SCHEDULER_YIELD` 等待类型、可运行队列、自旋锁争用、SQL Server 实例、`sqlservr` 进程、`sys.dm_os_wait_stats` 动态管理视图 (DMV)、等待任务、工作线程。

## 自旋锁
自旋锁优点、闩锁、轻量级同步对象、查询自旋锁争用、`sys.dm_os_spinlock_stats`。

## SQL 操作系统 (SQLOS)
SQL 操作系统架构、硬件概念、抢占式调度、资源、SQL Server 代理/计划测量、SQL Server Management Studio (SSMS)、自签名证书。

## SQL Server 等待统计
SQL Server 等待统计、等待统计、`sys.dm_exec_requests`、描述、执行过程、图形执行计划、查询信息、查询优化器、`sql_handle` 和 `plan_handle`、语句/执行计划、`sys.dm_exec_session_wait_stats`、`sys.dm_os_waiting_tasks`、内部会话 ID、查询信息、`THREADPOOL` 等待类型、`sys.dm_os_wait_stats`、`DBCC SQLPERF` 命令、描述、查询结果、等待信息。

