# T

## THREADPOOL 等待类型
公式、获取访问、连接、`CXSYNC_PORT`、DAC 调度器、用户连接、工作线程、`Ostress` 工具、`Ostress` 实用程序、并行度、处理器配置、`sys.dm_os_schedulers` DVM、`sys.dm_os_waiting_tasks` 动态管理视图 (DMV)、任务/工作线程、线程池饥饿、工作线程。

## TRACEWRITE 等待类型
`BatchCompleted` 事件、扩展事件、降低查询、扩展事件、分析器跟踪、服务器端跟踪、`sys.traces` 目录、跟踪定义、视图跟踪活动、管理工具、分析器跟踪、`sys.dm_os_waiting_tasks`、跟踪属性窗口。

## 透明数据加密 (TDE)
TDE。

## T-SQL
T-SQL。

