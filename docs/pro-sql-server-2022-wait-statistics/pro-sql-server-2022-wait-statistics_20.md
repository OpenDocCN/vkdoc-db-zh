# P

## PAGEIOLATCH_[xx] 等待类型
缓冲区页缓存、数据页、`CHECKPOINT`、数据页、`DBCC DROPCLEANBUFFERS` 命令、磁盘操作、闩锁等待类型、降低查询、基线测量、缓冲区缓存、索引碎片、监控存储性能、Perfmon、源代码、存储子系统内存页、存储子系统。

## PAGELATCH_[xx] 等待类型
B-树结构、聚集索引、数据结构、`DBCC IND` 结果、GalacticWorks 数据库、图形表示、不兼容闩锁、`IndexID/PageType` 列、内存页、最后一页插入争用、叶页、降低查询、优点、聚集索引、数据库数据文件、文件组、GUID、哈希分区、索引策略、索引分区、最后一页插入争用、逻辑处理器、非分区/分区索引、页闩锁争用、分区函数命名为 `LatchPartFunc`、`Ostress` 命令行、页类型、结果页、结果选项卡、行分布、`sys.dm_os_waiting_tasks` 动态管理视图 (DMV)、tempdb 数据库。

## Perfmon
Perfmon。

## 平台抽象层 (PAL)
PAL。

## 抢占式调度等待类型
抢占式调度等待类型、Linux PAL 层交互。

## PREEMPTIVE_OS_AUTHENTICATIONOPS
帐户验证、连接、登录进程、降低查询、性能问题、查询窗口、SSMS 帐户、Windows 身份验证。

## PREEMPTIVE_OS_ENCRYPTMESSAGE/PREEMPTIVE_OS_DECRYPTMESSAGE
证书、帐户选择、证书选择、连接属性、加密连接、加密/解密功能、视图、降低模式、权限、自签名证书。

## PREEMPTIVE_OS_GETPROCADDRESS
扩展存储过程、降低查询、`master` 数据库存储过程、`xp_cmdshell`、`xp_getnetname`。

## PREEMPTIVE_OS_WRITEFILEGATHER
AdventureWorks 数据库文件、数据库文件配置、定义、GalacticWorks 数据库、即时文件初始化、存储交互、存储子系统、`WriteFileGather` 函数、SQLPAL、Windows 操作系统、`WRITEFILEGATHER` 函数。

## 处理器
处理器。

