# D

## 数据库管理员 (DBAs)
DBA。

## 数据库管理系统 (DBMSs)
DBMS。

## DBMIRROR_SEND 等待类型
异步模式、通信积压、数据库镜像、数据库镜像监视器、数据修改事务、GalacticWorks 数据库、降低模式、镜像、镜像配置、`Mirror_Test` 表、主体服务器、同步/异步模式、事务日志记录。

## 专用管理员连接 (DAC)
调度器。

## DIRTY_PAGE_POLL 等待类型
检查点进程、脏页、间接检查点、修改恢复过程、目标恢复时间。

## Drawbridge 项目
Drawbridge 项目。

## 动态管理函数 (DMFs)
DMF。

## 动态管理视图 (DMVs)
阻塞信息、阻塞锁、数据库管理员 (DBA)、流程图、`KILL` 命令、查询调度信息、查询存储指标修改结果、`runtime_stats_interval_id`、源代码、`sys.query_store_wait_stats`、等待类别、请求结果信息、审查结果、调度器、会话信息、SSMS 程序、`sys.dm_exec_requests`、`sys.dm_exec_session_wait_stats`、`sys.dm_os_waiting_tasks`、`sys.dm_os_wait_stats`、任务、工作线程。

