# R

## 已提交读快照隔离级别 (RCSI)
RCSI。

## 恢复点目标 (RPO)
RPO。

## 恢复时间目标 (RTO)
RTO。

## REDO_THREAD_PENDING_WORK 等待类型
始终可用的可用性组、异步/同步复制、数据修改、`HADR_LOGCAPTURE_WAIT/HADR_WORK_QUEUE`、重做线程、事务。

## 关系数据库管理系统 (RDBMS)
RDBMS。

## RESOURCE_SEMAPHORE_QUERY_COMPILE 等待类型
编译争用、编译内存、编译过程、并发连接、`DBCC MEMORYSTATUS` 命令、网关、逻辑处理器、降低查询、内存资源、杂项属性、查询编译、查询操作、资源信号量、`sys.dm_os_waiting_tasks` 动态管理视图 (DMV)。

## RESOURCE_SEMAPHORE 等待类型
列信息、命令行、并发连接、`DesiredMemory/RequiredMemory` 属性、企业版、GalacticWorks 数据库、授予的工作空间内存 (KB)、降低/解决选项、`MemoryGrantInfo` 属性、内存请求、`Ostress` 查询、并行度、查询执行、资源信号量。

## 还原操作
还原操作。

## RML 实用程序
RML 实用程序。

