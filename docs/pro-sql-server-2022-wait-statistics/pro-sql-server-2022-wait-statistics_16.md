# L

## 闩锁类
闩锁类。

## 闩锁等待类型
缓冲区兼容性矩阵、定义、特性、`LATCH_[xx]`、B-树结构、缓冲区缓存、索引和数据页、`INDEXPROPERTY` 函数、中间级别、降低查询、内存区域、导航模式、非缓冲区类、非聚集索引、`Ostress` 命令、`resource_description` 列结果、根页、`sys.dm_os_latch_stats`、测试争用表、故障排除、轻量级同步原语。

## 锁/阻塞类型
锁/阻塞类型包括 ACID 属性、缩写、原子性、行为/隔离级别、特性、并发锁情况、一致性、持久性、排他锁、排他/架构锁、图形表示、层次结构、不兼容锁、不一致分析、意向锁、隔离、`LCK_M_I[xx]`、`COMMIT` 命令、定义、层次结构、意向排他锁、意向锁模式、意向锁、降低查询、`SELECT` 语句、共享锁、`LCK_M_SCH_S/LCK_M_SCH_M` 等待类型、`COMMIT/ROLLBACK` 命令、降低查询、`Person.Address` 表、架构修改 (Sch-M)、架构稳定性 (Sch-S)、`SELECT` 查询、`sys.dm_os_waiting_tasks`、`sys.dm_tran_locks` 表修改。

## LCK_M_S 等待类型
ACID 属性、并发事务、数据修改、死锁、GalacticWorks 数据库、图形表示、意向排他锁、降低查询、修改事务、优化、`Ostress` 实用程序、`Person.Address` 表、共享/排他锁、`sys.dm_os_waiting_tasks`、事务、更新模式、更新锁。

## LCK_M_X 等待类型
`COMMIT` 部分、排他锁、`HumanResources.Employee` 表、降低查询、共享锁、`sys.dm_os_waiting_tasks`、方法、模式/兼容性现象、查询信息、阻塞进程报告、配置选项、排他锁 (X)、`resource_description` 列、`sys.dm_os_waiting_tasks`、`sys.dm_tran_locks`、已提交读、关系数据库模型、行版本化、可序列化级别、共享/更新锁、事务。

## LOGBUFFER/WRITELOG 等待类型
并发连接、数据修改、事件、执行结果、`INSERT` 语句、日志缓冲区、降低查询、`TLog_demo` 数据库、事务。

