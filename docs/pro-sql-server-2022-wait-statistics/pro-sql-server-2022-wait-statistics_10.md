# C

## 中央处理器 (CPU)
CPU。

## 检查点文件对 (CFPs)
检查点文件对包括数据和增量文件、数据文件、数据文件/事务时间戳、内存优化表、合并操作、离线检查点线程、事务提交时间戳。

## CHECKPOINT_QUEUE 等待类型
自动检查点操作、检查点进程定义、`sys.dm_os_wait_stats/sys.dm_os_waiting_tasks`。

## CMEMTHREAD 等待类型
临界区、`EXECUTE (EXEC)` 命令、降低对象、内存对象、互斥 (mutex)、查询过程缓存、SqlDumper 线程安全、票证分配器。

## 公共语言运行时 (CLR)
CLR。

## CPU 相关等待类型
`CXCONSUMER` 等待类型。

## CXPACKET 等待类型
`CXPACKET` 等待类型的优点、配置设置、`CXCONSUMER` 等待类型、数据库作用域配置、`MAXDOP`、并行度设置、并行配置选项、并行查询线程、并行线程分发、`SELECT` 操作属性、服务器配置设置、偏斜工作负载、`SOS_SCHEDULER_YIELD`、`THREADPOOL`。

