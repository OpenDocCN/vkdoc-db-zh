# 索引 (Index)

## A

*   `ACID`
    *   参见 `原子性、一致性、隔离性、持久性 (ACID)`
*   `聚合函数`
    *   汽车公司
        *   汽车销售表
        *   公共表表达式 (CTE)
        *   累计销售额
        *   客户
        *   客户细分
        *   移动平均值
        *   嵌套 `GROUP BY`
        *   `PARTITION BY`
        *   汽车型号排名
        *   `ROW_NUMBER()`
        *   销售序列表
        *   标准聚合
        *   `SUM()/OVER()` 子句
        *   总销售额与排名
        *   窗口函数/传统聚合
    *   公共表表达式 (CTE)
    *   条件表达式
    *   `COUNT` 函数
    *   数据分析师/商业智能
    *   差异
    *   函数
    *   `GROUP BY` 子句
    *   分组子句
    *   健身房管理
        *   出勤记录
        *   出勤表
        *   课程表
        *   数据驱动决策
        *   `personal_training_sessions` 表
        *   会员类型
        *   `members` 表
        *   嵌套查询
        *   `payments` 表
        *   私人训练课程
        *   `SUM` 函数
    *   话题标签使用
    *   最大点赞与评论数
    *   多步骤计算
    *   `orders` 表
    *   主要函数
    *   常规算术函数
    *   `SUM()/COUNT()`
    *   汇总
    *   窗口函数
*   `AI`
    *   参见 `人工智能 (AI)`
*   `人工智能 (AI)`
*   `原子性、一致性、隔离性、持久性 (ACID)`

## B

*   `块范围索引 (BRIN)`
*   `B-tree` 索引

## C

*   `COALESCE` 子句
    *   条件表达式
*   `COLLATE` 子句
    *   口音/变音符号
    *   区分大小写
        *   不区分大小写
        *   不同的排序规则
        *   输出排序
    *   大小写敏感性
    *   `CREATE TABLE`
    *   `PostgreSQL`
    *   `SELECT` 查询
*   `命令行界面 (CLI)`
*   `公共表表达式 (CTEs)`
    *   条件表达式
    *   客户细分
    *   错误处理
    *   `PostgreSQL`
    *   结构化数据
    *   子查询
    *   语法
    *   前 N 分析
    *   视图/概念
*   `条件表达式`
    *   面积图
    *   条形图
    *   箱线图
    *   气泡图
    *   `CASE` 语句
        *   分类
        *   `COALESCE`
        *   会员数据表
        *   `NULLIF` 函数
        *   `orders` 表
        *   绩效指标
        *   原始指标
        *   `sales_data` 表
        *   结构化数据表
        *   表结构
        *   `WITH` 子句
    *   弦图
    *   环形图
    *   函数
    *   漏斗图
    *   甘特图
    *   热力图
    *   直方图
    *   医院数据
        *   `admissions` 表
        *   条形图/折线图
        *   分类
        *   数据准备过程
        *   诊断代码
        *   动态过滤
        *   `length_of_stay` 计算
        *   缺失值
        *   可视化工具
    *   折线图
    *   网络图
    *   运算符/表达式
    *   帕累托图
    *   饼图
    *   `PostgreSQL` 备忘单
    *   雷达图
    *   散点图
    *   堆积条形图
    *   树状图
    *   小提琴图
    *   可视化工具
    *   瀑布图
*   `控制函数`

## D

*   `数据分析`
    *   准确性与可靠性
    *   受众
    *   图表类型
    *   经典叙事结构
    *   组件
    *   数据故事呈现
    *   数据叙事
    *   决策制定
    *   动态查询
    *   探索
    *   `PostgreSQL` 查询执行
    *   呈现
    *   查询服务
    *   查询编写技能
    *   透明度
    *   语句/子句/操作/函数
    *   结构化数据
    *   可视化
    *   可视化工具
*   `数据库管理系统 (DBMS)`
*   `数据库`
    *   分析过程
    *   定义
    *   基本问题/答案
    *   工业进步
    *   关系型/非关系型数据库
*   `数据控制语言 (DCL)`
*   `数据定义语言 (DDL)`
*   `数据操作语言 (DML)`
*   `数据查询语言 (DQL)`
*   `日期和时间函数`
*   `动态对话（子查询）`
    *   繁忙的办公室
        *   单行子查询
    *   条件逻辑
    *   派生表
    *   增强的模块化
    *   食品配送平台
    *   交互式与响应式查询
    *   `JOINs`
    *   嵌套 SQL 子查询
    *   陷阱/解决方案
    *   可读性差
    *   实时适配
    *   重复执行
    *   返回数据

## E

*   `错误处理`
    *   `CAST()` 函数
    *   民事登记流程
    *   异常处理
    *   `EXCEPTION WHEN` 子句
    *   生成
    *   分子与分母
    *   在线服装市场
    *   递归查询
    *   `SPLIT_PART()` 函数
    *   `SUM()` 和 `RANK()` 函数
    *   事务
*   `提取、转换、加载 (ETL)`

## F

*   `函数`
    *   聚合函数
    *   控制函数
    *   相关性
    *   数据离散度/变异性
    *   日期和时间
    *   美元符号引用字符串
    *   错误处理
        *   参见 `错误处理`
    *   `JSON` 数据类型
    *   `PostgreSQL`
    *   排名函数
    *   统计与数学函数
    *   字符串函数
    *   系统函数
    *   窗口函数

## G

*   `通用倒排索引 (GIN)`
*   `通用搜索树 (GiST)`
*   `图形用户界面 (GUI)`
*   `GROUP BY` 子句
    *   聚合函数

## H

*   `处理异常`
*   `HyperLogLog`

## I

*   `索引与视图`
    *   `B-tree` 索引
    *   复合索引
    *   数据分析
    *   定义
    *   `DROP INDEX` 命令
    *   `EXPLAIN`
        *   `ANALYZE`
        *   性能测量
        *   查询执行
    *   `EXPLAIN` 关键字
    *   `通用倒排索引 (GIN)`
    *   `GiST` 索引
    *   哈希索引
    *   `INSERT/UPDATE/DELETE` 操作
    *   优化
    *   部分索引
    *   `PostgreSQL`
    *   性能优化
    *   性能/优化锦标赛操作
    *   查询模式/数据修改
    *   语法
    *   唯一索引



## J, K

*   JavaScript 对象表示法 (`JSON`)
*   `JOINs` 子句
    *   PostgreSQL 速查表
    *   类型与子查询概念
*   `JOINs` 操作
    *   优点
    *   `CROSS JOIN`
    *   数据库系统
    *   不同类型
    *   足球学院
        *   数据分析师
        *   `INNER JOIN`
        *   `MatchDetails` 表
        *   `matches` 表
        *   `player` 表
        *   查询结果
    *   `FOREIGN KEY`
    *   `FULL OUTER JOIN`
    *   基本概念
    *   医院管理系统
        *   预约
        *   科室
        *   医生列表
        *   `NULL` 值
        *   患者列表
        *   主治医生
        *   主要患者
    *   `INNER JOIN`
    *   `LEFT/RIGHT OUTER JOIN`
    *   `NULL`安全等于运算符
    *   `NULL` 值
    *   `REFERENCES`
    *   关系型数据库
    *   `SELF JOIN`
    *   子查询
    *   科技公司
        *   数据结构
        *   数据表分析
        *   `departments` 表
        *   `employees_projects` 表
        *   `employees` 表
        *   未匹配的记录/数据完整性
        *   `projects` 表
        *   自连接
    *   类型

## L

*   大型语言模型 (`LLMs`)
*   词法分析
*   线性回归分析

## M

*   数学变换
*   多版本并发控制 (`MVCC`)

## N

*   嵌套/多级子查询
    *   复杂对话
    *   复杂多层数据
    *   两级过程
*   非关系型数据库 (`NoSQL`)
*   规范化

## O

*   对象标识符 (`OID`)
*   联机分析处理 (`OLAP`)
*   联机事务处理 (`OLTP`)
*   `ORDER BY` 子句
    *   升序 (`ASC`)/降序 (`DESC`) 排列
    *   `COLLATE` 子句
    *   数据分析
    *   高速公路建设/交通问题
        *   `Avg_Travel_Time` 值
        *   `CASE` 语句
        *   转换过程
        *   拥堵等级
        *   高拥堵等级
        *   路线
        *   `TrafficData_After` 表
        *   `TrafficData_Before` 表
        *   `UNION` 运算符
    *   `LIMIT` 子句
    *   `NULL` 值
        *   聚合与排序
        *   列名
        *   `customers` 表
        *   数据/消除歧义
        *   降序排列
        *   `NULLS FIRST/LAST`
        *   `NULLS LAST`
        *   需求
        *   排序过程
        *   策略
    *   分页 (`OFFSET/LIMIT`)
    *   图表信息
    *   PostgreSQL
    *   现实世界场景
    *   数据排序
    *   财务交易排序
    *   字符串排序/大小写敏感性
    *   语法

## P, Q

*   PostgreSQL
    *   聚合函数
    *   速查表
        *   聚合与分组
        *   条件表达式
        *   数据修改
        *   日期和时间操作
        *   索引
        *   `JOIN`
        *   `LEAD` 和 `LAG` 函数
        *   查询
        *   排名函数
        *   字符串操作
        *   子查询
        *   视图
        *   窗口函数
    *   排序规则
    *   客户交互
    *   数据结构
    *   元素参考
    *   错误处理
    *   函数创建
    *   `INSERT` 语句
    *   安装过程
    *   `psql/pgAdmin`
    *   查询
    *   查询执行
    *   视图
*   过程式语言

## R

*   排名函数
*   递归公用表表达式 (`RCTEs`)
*   参照完整性
*   关系型与非关系型数据库
*   关系型数据库管理系统 (`RDBMS`)
*   行级安全

## S

*   `SELECT` 语句
    *   聚合
        *   参见聚合函数
    *   面包店销售数据
        *   分析
        *   `CASE` 语句
        *   `GROUP BY` 子句
        *   查询执行
        *   字符串模式
        *   表结构
    *   书店周年庆
    *   糖果店库存溢出
        *   `DISTINCT`
        *   销售日志
        *   唯一糖果种类
    *   数据叙事
    *   `DISTINCT` 关键字
    *   基本概念
    *   数学运算
    *   查询执行
    *   结构/语法
    *   表创建
        *   别名
        *   列选择
        *   `CONCAT()` 函数
*   语义分析
*   空间分区 GiST (`SP-GiST`)
*   统计与数学函数
*   统计函数
*   存储过程
    *   函数
*   字符串函数
*   字符串操作
    *   PostgreSQL 速查表
*   结构化英语查询语言 (`SEQUEL`)
*   结构化查询语言 (`SQL`)
    *   聚合
        *   参见聚合函数
    *   分析
        *   参见数据分析
    *   二进制数据类型
    *   布尔数据类型
    *   类别
    *   特征/要求
    *   命令类型
    *   条件逻辑
        *   参见条件表达式
    *   数据类型
    *   日期和时间数据类型
    *   定义
    *   方言差异
    *   动态对话
    *   函数
        *   参见函数
    *   地理空间数据类型
    *   标识符/命名约定
    *   索引和视图
    *   `JOINs`
        *   参见 `JOINs` 操作
    *   叙述方式
    *   自然语言查询
    *   数值数据类型
    *   `ORDER BY` 子句
    *   PostgreSQL
    *   查询语句
    *   `SELECT`
        *   参见 `SELECT` 语句
    *   特殊数据类型
    *   语句
    *   字符串数据类型
    *   术语
    *   事务语句
    *   `WHERE` 子句
        *   参见 `WHERE` 子句
*   子查询
    *   对话元素
    *   相关子查询
    *   派生表
    *   `FROM` 子句
    *   一般类型
    *   多列子查询
    *   多行子查询
    *   嵌套/多级子查询
    *   PostgreSQL 速查表
    *   单行子查询
    *   类型与概念
    *   非相关子查询
        *   另请参见 动态对话（子查询）
*   语法分析
*   系统函数

## T

*   事务控制语言 (`TCL`)

## U

*   用户定义函数 (`UDF`)

## V

*   视图
    *   优点/概念
    *   `ALTER VIEW/DROP VIEW`
    *   数据分析任务
    *   定义
    *   `DROP VIEW` 语句
    *   索引
        *   参见索引和视图
    *   `MIN()` 函数
    *   在线零售公司
    *   优化
    *   PostgreSQL
    *   比赛表现数据
    *   标准/物化视图
    *   语法
    *   更新/修改
*   易失性函数

## W, X, Y

*   `WHERE` 子句
    *   大小写敏感
    *   `CAST()` 函数
    *   子句
    *   比较运算符
    *   数据叙事
    *   数据类型
    *   过滤技术
        *   日期处理
        *   足球运动员
        *   模式匹配
        *   子查询
    *   逻辑错误
    *   `NULL` 值
    *   在线商店
        *   方面
        *   `BETWEEN` 运算符
        *   数据分析
        *   数据驱动的决策
        *   筛选订单
        *   `orders` 表
        *   待处理状态
        *   已收到订单
        *   结果表
        *   交易
    *   语法错误/语义错误
*   窗口函数
    *   数据分析
    *   函数
    *   标准聚合
    *   前 N 名分析
    *   传统聚合
*   预写式日志 (`WAL`)

## Z

*   零基索引
