# 索引

## A
`聚合操作` (`map-reduce` 管道 单用途 SQL 聚合 参见 SQL 聚合) `数组, MongoDB` (`$addToSet` 操作符 `$elemMatch` 操作符 `$pop` 操作符 `$push` 操作符 `$size` 操作符) `身份验证` (`访问控制` `管理员角色` `内置角色` `集群管理员角色` 进程 `用户角色`)

## B
`BASE 方法` `二进制 JSON (BSON)`

## C
`CAP 定理` `逗号分隔值 (CSV)` `并发事务` `创建、读取、更新和删除 (CRUD) CRUD 操作`

## D
`数据库, MongoDB` (创建 显示 列表 `删除版本`) `数据库分析器` (`mongostat` 查看 工作) `数据模型` (`文档引用` `一对多关系` 查询 嵌入式文档 `一对多关系` `一对一关系`) `db.createCollection() 方法` `db.dropDatabase() 方法`

## E
`嵌入式数据模型` `加密` (`动态数据` `静态数据` `本地密钥管理` 选项 输出) `现有用户` (`更改密码` 修改 访问 分配 `read 角色` 撤销 角色 `superUserAdmin`)

## F, G
`find() 方法`

## H
`水平扩展`

## I
`索引` (创建 `哈希` `多键` 属性 `部分` `稀疏` `TTL` `唯一` 策略 `多字段索引` `选择性` `单字段索引` 排序 `内存中的值` `文本` `2dsphere` 类型) `insertMany() 方法` `隔离` 测试

## J, K
`JavaScript 对象表示法 (JSON)`

## L
`轻量目录访问协议 (LDAP)`

## M
`树形结构建模` (`父引用` `祖先数组` `作者集合` `子引用` `MongoDB 聚合框架` `数组` 参见`数组, MongoDB` `BSON` `固定集合` 创建 `文档插入` 查询 `集合创建` 命令 `创建、读取、更新和删除 (CRUD) 操作` `数据库` 参见`数据库, MongoDB` `数据类型` `删除方法` `文档` `嵌入式文档` `相等` `GridFS` `导入` `插入方法` `多个` `单个` `安装` `Windows 上的 Compass` `Ubuntu` `Windows` `迭代游标` `limit() 和 skip() 方法` `mongo shell` `Node.js` 参见`Node.js, MongoDB` `空值或缺失值` `读操作` `复制` `限制字段` `检索文档` (`AND 条件` `相等条件` `OR 条件` `查询操作符`) `无模式数据库` `分片` 术语 `更新方法` (`多个` `替换` `单个`)) `mongostat 命令` `监控` (`数据库分析器` `db.serverStatus() 命令` `db.stats() 命令` `日志文件` 工作) `mongodb cursor.explain() explain() 方法` `mongodump` `mongorestore` (`数据恢复` `远程实例`) `mongostat` `mongotop` `性能下降` `设置日志级别` `多文档事务`

## N, O, P, Q
`Node.js, MongoDB` (`集合创建` `删除文档` `插入文档` `查询文档` `更新文档`) `规范化数据模型` `NoSQL 数据库` (`优势` `挑战` `特点` `类型`)

## R
`定期备份调度, mongodump` `关系数据库管理系统 (RDBMS)` `副本集` (使用密钥文件 `数据目录` 错误信息 `列出数据库` `mongod 实例` `端口 20001` `端口 20002` `端口 20003` `openssl 包` `关闭`) `复制` (`自动故障转移` `数据目录` `实例等待连接` `连接到 mongo 实例` 启动 `mongod` `三成员副本集` 定义 `策略`)

## S
`SCRAM` `session.commitTransaction() session.startTransaction()` `分片` (`数据目录` `配置服务器` `连接 shard1 服务器` `查询路由器` `shard1 服务器上的副本` `shard1 服务器` `shard2 服务器` `shard3 服务器` `数据分布` 定义 `mongod 实例` `sh.status()` `用户集合`) `SQL 聚合` `MongoDB 术语` `超级用户和验证用户` (`访问控制` 创建)

## T, U
`制表符分隔值 (TSV)` `跟踪用户活动` (`审计事件` `创建审计过滤器` `启用和配置`) `事务` (`丢弃数据更改` `限制` `多文档, 工作会话` `写入冲突`)

## V, W
`垂直扩展`

## X, Y, Z
`x.509 证书`
