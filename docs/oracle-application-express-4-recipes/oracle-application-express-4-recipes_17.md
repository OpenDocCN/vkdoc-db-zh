# 数据可视化与 Oracle APEX 索引

## 数据可视化
*   在图形图表中可视化数据：147–152
*   在多系列图表中可视化数据：153–158
*   在日历上可视化数据：159–162
*   在地图上可视化数据：162–169

## APEX 术语与概念
### 创建 APEX 表空间
7

### APEX 术语
13–16

*   `application`：13
*   `blank page`：14
*   `database application`：13
*   `database schemas`：13
*   `forms`：14
*   `interactive reports`：14
*   `master detail page`：14
*   `page`：14
*   `Report and Form page`：14
*   `reports`：14
*   `substitution strings`：113
*   `tabular form`：14
*   `templates`：112
*   `themes`：13, 113
*   `Websheet application`：13
*   `workspace`：13

## 命令、脚本与配置
*   `apexins` 命令：安装 Oracle APEX，8
*   `APP_IMAGES` 替换字符串：114
*   `apxldimg` 脚本：9, 10
*   `apxsqler` 命令：8
*   `apxxepwd` 脚本：10

## Application Builder
*   向页面添加自定义 CSS 样式：114
*   向表单添加图像头：107
*   创建数据库应用程序：35
*   创建表单以管理图书目录：283
*   在应用程序中创建主题：121
*   创建 `Websheet application`：61
*   生成应用程序依赖项列表：253
*   导入应用程序：260–267
*   使用自定义 CSS 文件：117

## 应用程序部署
### 方法决策
251–253

#### 导出应用程序
256–260

#### 导出单个页面
269–271

### 生成应用程序依赖项列表
253–256

### 导入应用程序
*   使用 `Application Builder`：260–267
*   使用 `SQL*Plus`：267–269

### 导入单个页面
271–275

### 发布应用程序 URL
275–276

## 应用程序与页面性能
*   `Application Express` 引擎：安装 Oracle APEX，12
*   `application id`：URL 解析，277
*   应用程序对象：导出应用程序，259
*   添加 Web 服务引用：98–99
*   更改应用程序的主页：307–308
*   创建数据库应用程序：31–42
*   在应用程序中创建主题：121–123
*   创建 `Websheet application`：61–65
*   启用页面缓存：218–219
*   启用区域缓存：216–218
*   识别 `APEX` 部署模型：4
*   测量页面访问频率：207–210
*   测量页面性能：210–212
*   测量区域性能：212–216
*   修改表单控件模板：125–128
*   切换应用程序主题：124–125
*   将 UI 翻译成另一种语言：180–199
*   何时不应使用 `APEX`：2
*   何时应使用 `APEX`：2

## 安全与攻击
*   跨站脚本攻击 (`XSS`)：245–250
*   `SQL` 注入：236–245
*   认证函数：223, 224, 226
    *   定义：222
*   创建认证方案：221–227
*   `Authentication Schemes` 页面：222, 224
*   授权：221
    *   定义用户访问权限：227–236
*   授权方案：235, 236
    *   创建：229, 230

## 其他功能
*   启用自动时区功能：201



### B

*   `备份 APEX 应用程序`，12
*   `批处理脚本`
*   `使用 SQL*Plus 导入应用程序`，267–269
*   `绑定变量`
    *   `SQL 注入`攻击，245
    *   防范，243
*   `空白页`
    *   APEX 空白页描述，14
    *   创建为空白主页，36
*   `图书目录应用程序`
    *   `Books` 表，279，282
    *   `Category` 表，281，282
    *   更改其主页，307–308
    *   创建前端，297–306
    *   创建在线目录，279–322
    *   创建页面以管理图书列表，282–297
    *   创建库存计数饼图，308–311
    *   在其中创建选项卡，316–322
    *   外键映射，表，282
    *   `Inventory` 表，281，282
    *   设置中央管理页面，311–316
    *   设置导航列表，311–313
    *   设置表，279–282
    *   上传图书照片，293
*   `图书详情输入表单`
    *   创建表单以管理图书目录，284
    *   `Books` 表，282
    *   创建表单以管理图书目录，283
    *   外键映射，282
    *   设置图书目录表，279
*   `浏览器`
    *   更改浏览器语言首选项，198
*   `内置替换字符串`，235
*   `按加权页面性能` 图表
    *   `监控活动` 工具，210
*   `字节`
    *   为双字节字符输入设置，173–178
    *   支持双字节数据输入，178–180

### C

*   `按用户缓存` 设置，219
*   `缓存`
    *   启用页面缓存，218–219
    *   启用区域缓存，216–218
*   `日历`
    *   `按日的页面视图月度日历` 图表，209
    *   视图，162
    *   在日历上可视化数据，159–162
*   `日历` 页面类型，159
*   `日历` 选项卡
    *   功能开发截止日期，28
*   `回调` 部分
    *   `渲染函数名称`，135
*   `级联关系`
    *   创建级联下拉列表，87，88–90
*   `目录应用程序` 参见 `图书目录应用程序`
*   `Category` 表，282
    *   创建，85
    *   创建页面以管理图书分类，295
    *   外键映射，282
    *   设置图书目录表，281
*   `字符输入`
    *   为双字节输入设置，173–178
*   `图表属性` 步骤
    *   创建多系列图表，155
*   `图表` 页面类型
    *   创建多系列图表，153
    *   在图形图表中可视化数据，148
*   `图表` 视图，152
*   `报表`，50
*   `图表`
    *   创建库存计数饼图，308–311
    *   使用统计图表，`监控活动` 工具，209
    *   在图形图表中可视化数据，147–152
    *   在多系列图表中可视化数据，153–158
*   `复选框` 控件
    *   在表单中从值列表中选择，55
*   `中文输入法编辑器`
    *   为双字节字符输入设置，174，176
*   `中文`
    *   双字节数据输入，173–180
*   `Class` 属性，`按钮`
    *   使用自定义 CSS 文件，119
*   `经典报表` 类型
    *   创建图书目录前端，298
    *   防止 `SQL 注入` 攻击，237
*   `经典报表`，143
*   `客户端 JavaScript` 验证，81–84
*   `代码片段`
    *   创建可重用片段，128–133
*   `柱形图` 图表类型
    *   创建多系列图表，153
*   `列格式化` 区域
    *   创建图书目录前端，304
*   `列名必须是有效的 Oracle 标识符` 错误，44
*   `列非空` 验证类型，79
*   `列`，`报表`
    *   计算列，50
*   `列`，`网页`
    *   更改项目类型，65–68
    *   为其创建下拉列表，66
*   计算列，`报表`，50
*   `条件类型` 字段
    *   服务器端验证，77
*   `确认弹出窗口`
    *   修改表单控件模板，125–128
*   `控件模板`，`表单`
    *   修改，125–128
*   `控件`
    *   通过 `插件` 扩展 `UI`，133
*   `创建应用程序` 向导
    *   创建表单以管理图书目录，283
*   `创建动态操作` 向导
    *   动态禁用/隐藏表单部分，91
*   `CREATE TABLE` 命令
    *   `Books` 表，279
    *   `Category` 表，281
    *   `Inventory` 表，281
*   `创建验证` 向导
    *   向表单添加服务器端验证，74
*   `跨站脚本 (XSS)` 攻击，245–250
*   `CRUD` 操作，1
    *   为应用程序创建 `CRUD` 基础，31
    *   创建表单以管理图书目录，295
    *   决定是否使用 `APEX`，2
*   `CSS` 文件，使用自定义，117–120
*   `CSS` 样式，自定义
    *   添加到页面，114–117
*   `CurrencyRateReq` 表单，创建，96–98
*   `Customers` 数据输入表单
    *   `Sales Force` 应用程序，40
*   `Customers` 表
    *   创建，32–35，236
    *   创建数据输入页面，38
*   `CustomLogins` 表，创建，221



### ![images](img/square.jpg) D

#### 仪表板

-   功能摘要，27
-   在页面中可视化数据，169–172

#### 数据

-   批量更改 Websheet 数据，69–70
-   创建报表以进行管理，42–50
-   创建报表以显示现有记录，42
-   在仪表板页面中可视化，169–172
-   在图形图表中可视化，147–152
-   在多系列图表中可视化，153–158
-   在日历上可视化，159–162
-   在地图上可视化，162–169

#### 数据录入

-   创建数据库应用程序，31
-   为其创建页面，37
-   创建表单形式，58–60
-   `Customers` 表单，40
-   支持双字节数据录入，178–180

#### 数据源

-   创建多系列图表，157

#### 数据类型

-   以 `N` 为前缀的数据类型，180

#### 数据库访问描述符

-   `URL` 解释，277

#### 数据库应用程序

-   13
-   其原子单元，16
-   创建空白页作为主页，37
-   创建，31–42
-   创建销售力量应用程序，36–39
-   `数据库应用程序` 类型，42
-   使用 `Application Builder` 导入应用程序，261
-   导入单个页面，272

#### 数据库项目

-   使用 `APEX` 时的限制，3
-   Oracle 的命名规则，44

#### 数据库对象依赖关系报告

-   生成应用程序依赖关系列表，253, 254, 256

#### 数据库对象

-   导出应用程序，259
-   生成应用程序依赖关系列表，253–256
-   设置图书目录表，279–282

#### 数据库模式

-   `APEX` 模式描述，13
-   为工作区创建模式，18, 19
-   导出应用程序，256–260
-   导入应用程序，263
-   将工作区映射到模式，24

#### 数据库工作区

-   25

#### 数据库

-   `APEX` 模式描述，13
-   决定是否使用 `APEX`，3
-   定义完整的数据库表集，42
-   存储计算值，94–96

#### 日期

-   创建图书目录前端，300
-   存储/显示带时区信息，199–205

#### 依赖关系

-   生成应用程序依赖关系列表，253–256

#### 部署

-   决定其方法，251, 253
-   识别 `APEX` 模型，4–6
-   两层部署，5

#### 开发

-   为开发者添加用户账户，21, 22
-   敏捷开发，28
-   决定部署方法，251–253
-   管理流程，25–28
-   多个团队使用 `APEX`，14
-   为团队设置工作区，17–25
-   何时使用 `APEX`，2

#### `显示为` 字段，`字段属性` 页面

-   创建表单以管理图书目录，286

#### 双字节数据录入

-   进行设置，173–178
-   支持，178–180

#### 在表单中下载文件

-   58

#### 下拉列表

-   创建级联列表，85
-   为 `Websheet` 中的列创建列表，66
-   创建表单以管理图书目录，285
-   动态更改其中的项目列表，85–90

#### 动态操作

-   93

#### 动态字段

-   内置替换字符串，235
-   插入到模板中，113

#### 动态变量

-   创建可重用的代码片段，133
-   动态更改下拉列表中的项目列表，85–90
-   动态禁用/隐藏表单部分，90–93

### ![images](img/square.jpg) E

#### `编辑页面项` 页面

-   更改表单中的字段项目类型，51

#### 电子邮件

-   `发送邮件` 过程，105–106
-   从表单发送，104–106

#### 创建 `EmpLeave` 表

-   139

#### 创建 `Employee` 表

-   78, 139

#### 英语键盘

-   双字节字符输入，173–178

#### 企业环境

-   识别 `APEX` 部署模型，4

#### `按列表条目` 页面

-   创建报表以管理数据，47

#### 创建 `EquipmentRequest` 表

-   85

#### 事件处理器，JavaScript

-   添加，82

#### 事件驱动架构

-   何时不使用 `APEX`，2

#### 创建 `Events` 表

-   199

#### Excel 电子表格

-   创建 `Websheet` 应用程序，61–65

#### `存在 SQL 查询` 方案类型

-   230, 235

#### 导出应用程序

-   256–260

#### 导出单个页面

-   269–271



### ![images](img/square.jpg) F

#### 故障转移功能
识别 APEX 部署模型，4
功能，创建/更新，25

#### 字段属性页面
创建用于管理图书目录的表单，286

#### 字段
在表单中更改项目类型，51
在表单中创建计算字段，94
在表单中创建文件上传字段，56–58
动态禁用/隐藏表单部分，90–93
将动态字段插入模板，113
在 PL/SQL 代码中直接引用表单字段，90

#### 文件
在表单中创建文件上传字段，56–58
使用自定义 CSS 文件，117–120

### 筛选，报表，50
动态，143

#### Flash 图表类型
创建多系列图表，153
在仪表板页面中可视化数据，171
在图形图表中可视化数据，148

### 重点领域和所有者选项卡，28

#### 页脚
在其中显示性能统计信息，213, 215

#### 外键映射
图书目录应用程序表，282

#### 外键
创建客户表，33

### 表单控件模板，修改，125–128

#### 表单控件
通过插件扩展 UI，133

#### 表单页面
创建数据录入页面，37
创建报表以显示现有记录，43

#### 表单区域模板定义页面
为表单添加图像页眉，109, 110

#### 表单
向表单添加客户端 JavaScript 验证，81–84
向表单添加图像页眉，107–114
向表单添加服务器端验证，71–77
适用于表格化表单，78–80
APEX 表单，14
APEX 报表和表单页面，14
APEX 表格化表单，14
从表单调用 Web 服务引用，99–101
在表单中更改字段项目类型，51
在表单中创建计算字段，94
创建 CurrencyRateReq 表单，96–98
在表单中创建文件上传字段，56–58
创建表格化数据录入表单，58–60
动态禁用/隐藏表单部分，90–93
与 Web 服务交互，96–101
使用 APEX 的限制，3
管理图书目录，282–288
上传图书照片，293
在 PL/SQL 中直接引用字段，90
从表单中的值列表中选择，53–56
从表单发送电子邮件，104–106
表格化表单，60

#### 前端门户
创建图书目录前端，297–306

### ![images](img/square.jpg) G

#### 全局区域
参见共享组件区域

#### 全局化属性
修改，200
设置，181–182

#### 图形图表
在其中可视化数据，147–152

### 分组记录，报表，50

### ![images](img/square.jpg) H

#### 入侵
跨站脚本 (XSS) 攻击，245–250
SQL 注入攻击，236–245

#### 页眉
为表单添加图像页眉，107–114
突出显示，报表，50

#### 历史记录选项卡
功能更新，28

#### 主页
在应用程序中更改主页，307–308
创建数据录入页面，37
创建数据库应用程序，36
创建报表以管理数据，46, 48
销售力量应用程序，40
工作区，23, 24

### `href` 属性，HTML 声明，120

#### HTML 表达式字段
创建图书目录前端，304, 306

#### HTML 表单元素属性字段
客户端 JavaScript 验证，82, 84

#### HTML 页眉和正文属性部分
向页面添加自定义 CSS 样式，114, 117
客户端 JavaScript 验证，81, 84
使用自定义 CSS 文件，118, 120

#### HTML 文本区域源
创建和引用快捷方式，132

#### HTTP 服务器
识别 APEX 部署模型，4
Oracle HTTP 服务器和 mod_plsql，4, 5–6
Oracle XML DB HTTP 服务器，4, 5

#### `http://localhost:8080`
URL 解释，277



### ![images](img/square.jpg) I

图像表头

添加至表单，107–114

图像前缀，重置，10

图像，113

`共享组件`区域，113

上传图书照片，图书目录，293

IME（输入法编辑器）

为双字节字符输入进行设置，174

支持双字节数据输入，178

导入应用程序

`Application Builder`，260–267

`SQL*Plus`，267–269

导入单个页面，271–275

输入

为双字节字符输入进行设置，173–178

`INSERT INTO` 命令

`Books` 表，279

`Category` 表，281

`Inventory` 表，281

安装 `Oracle APEX`，6–13

交互式报表，143, 147

`APEX 交互式报表` 描述，14

跨站脚本 (`XSS`) 攻击，245

动态筛选报表，144

互联网

将 `APEX` 应用程序暴露给，5

识别 `APEX` 部署模型，4

`Inventory` 表，282

创建页面以管理图书库存，295

外键映射，282

设置图书目录表，281

项级别验证，77

项名称

使用 `APEX` 时的限制，3

项类型

在 `Websheet` 列中更改，65–68

项

在下拉列表中动态更改，85–90

### ![images](img/square.jpg) J

`日语输入法编辑器`，178

日语

双字节数据输入，173–180

术语

`APEX` 术语，13–16

JavaScript

为表单添加客户端验证，81–84

添加事件处理器，82

客户端验证，84

跨站脚本 (`XSS`) 攻击，245–250

何时使用 `APEX`，2

`Jobs` 表，创建，227

### ![images](img/square.jpg) K

键盘

双字节字符输入，173–178

`键盘和语言` 选项卡

双字节字符输入，174

键

外键映射，图书目录应用程序，282

### ![images](img/square.jpg) L

`LABEL` 替换字符串，113

语言首选项，浏览器

更改，198

语言

翻译应用程序用户界面，180–199

`按日的使用情况折线图` 图表

`监控活动` 工具，210

`列表条目` 标签

创建报表以管理数据，47

`值列表` 区域

创建表单以管理图书目录，287

值列表 (`LOV`)

创建级联下拉列表，87

创建表单以管理图书目录，295

在下拉列表中动态更改 `LOV`，85–90

在表单中从 `LOV` 选择，53–56

负载均衡

识别 `APEX` 部署模型，4

`localhost`

`URL` 解释，277

本地化

双字节数据输入，173–180

日志文件

安装 `Oracle APEX`，9

登录到工作区，22, 23

逻辑层，71

登录页面

安装 `Oracle APEX`，11

`登录` 页面

`Sales Force` 应用程序，39

`LOV` `参见` 值列表

### ![images](img/square.jpg) M

`管理工作区` 菜单项

为团队开发设置工作区，17

`地图属性` 步骤

在地图上可视化数据，166

`地图` 页面类型

在地图上可视化数据，163

地图

渲染，168

在地图上可视化数据，162–169

主从页面，`APEX`，14

消息驱动架构

何时不使用 `APEX`，2

元数据

导出应用程序，259

安装 `Oracle APEX`，12

`mod_plsql` 插件

识别 `APEX` 部署模型，4

`Oracle HTTP Server` 附带，4

`Oracle HTTP Server` 附带，5

`监控活动` 工具

启用页面缓存，219

测量页面访问频率，207–210

测量页面性能，210–212

`页面视图分析` 部分，208, 209, 210

`按日的月度页面视图日历` 图表，209

`所有应用程序中最常查看的页面` 图表，208, 209

多系列图表

创建，153

在多系列图表中可视化数据，153–158

`MVC`（模型-视图-控制器）

何时不使用 `APEX`，2

`我的主页` `参见` 主页

### ![images](img/square.jpg) N

导航

在应用程序中创建选项卡，316–322

导航列表，316

设置导航，311–313

`Net8/SQL*Net` 连接，6

以 `N` 为前缀的数据类型，180

空值

`列不为空` 验证类型，79

数字

创建图书目录前端，300



### ![images](img/square.jpg) O

对象浏览器工具

在表单中创建计算字段, 95

在表单中创建文件上传字段, 56

定义完整的数据库表集, 42

在表单中从值列表中进行选择, 53

在线图书目录应用程序 参见 图书目录应用程序

`OOP`（面向对象编程）

使用 `APEX` 时的限制, 2, 4

优化

页面性能优化, 212

`Oracle`

`APEX` 的免费实例, 3

安装 `Oracle APEX`, 6–13

使用 `APEX` 时的限制, 3

数据库对象的命名规则, 44

`Oracle APEX` 参见 `APEX`

`Oracle 数据库`

`APEX` 应用程序, 3

决定是否使用 `APEX`, 2

安装 `Oracle APEX`, 6

`Net8/SQL*Net` 连接, 6

何时使用 `APEX`, 2

`Oracle HTTP 服务器` 与 `mod_plsql`

确定 `APEX` 部署模型, 4, 5–6

`Oracle XML DB HTTP 服务器`, 4, 5



### P

### ![images](img/square.jpg) P

#### 页面缓存

- 219

#### 页面定义区域

- 向页面添加自定义 CSS 样式，114，115
- 创建和引用快捷方式，131
- 启用页面缓存，218
- 导出单个页面，270
- 修改表单控件模板，126
- 使用自定义 CSS 文件，118

#### 页面执行

- 71

#### 页面级验证

- 服务器端验证，77

#### 页面呈现区域

- 向页面添加自定义 CSS 样式，114
- 向表单式表单添加服务器端验证，78
- 更改表单中的字段项类型，51，53
- 创建和引用快捷方式，131
- 创建授权方案，231
- 创建图书目录前端，300，303，304
- 创建用于管理图书目录的表单，285，290
- 跨站脚本 (`XSS`) 攻击，245，248
- 防止 `SQL` 注入攻击，237，238，242
- 设置中央管理页面，314
- 使用插件，136
- 在仪表板页面中可视化数据，170

#### 页面呈现视图

- 212，216，218

#### 页面视图分析部分

- `Monitor Activity` 工具，209
- 测量页面访问频率，208
- 测量页面性能，210

#### 按加权页面性能排列的页面视图页面

- 210，211

#### 页面

- 添加自定义 CSS 样式，114–117
- `APEX` 页面描述，14
- 空白页面描述，14
- 按加权页面性能排列的图表，210
- 选择页面类型，43
- 创建空白页面作为主页，37
- 创建数据录入页面，37
- 创建报表以显示现有记录，43
- 数据库应用程序，16
- `Database Application` 类型，42
- 启用页面缓存，218–219
- 启用区域缓存，216–218
- 导出单个页面，269–271
- 表单描述，14
- 导入单个页面，271–275
- 每日使用情况折线图，210
- 管理图书列表，282–297
- 主从页面描述，14
- 测量页面访问频率，207–210
- 测量页面性能，210–212
- 测量区域性能，212–216
- 按日显示的页面视图月度日历图表，209
- 所有应用程序中最常查看的页面图表，208，209
- 性能优化，212
- 区域，172
- `Report and Form` 页面描述，14
- 报表描述，14
- 保存时运行 `PL/SQL` 过程，102–103
- 设置中央管理页面，311–316
- 表单式表单描述，14
- 使用 `APEX` 时的技术限制，3
- 在仪表板页面中可视化数据，169–172

#### `Pages` 向导

- 创建数据库应用程序，36

#### 分页

- 报表，50

#### 参数化报表

- 创建，143–147

#### 密码

- 安装 `Oracle APEX`，10

#### 路径

- 安装 `Oracle APEX`，8，10

#### `PatientDB` 表

- 创建，104

#### `PatientDischarge` 表

- 创建，72

#### `PaymentHistory` 表

- 向表中添加记录，73
- 创建，72

#### 性能

- 按加权页面性能排列的图表，210
- 在区域页脚显示统计信息，213，215
- 启用页面缓存，218–219
- 启用区域缓存，216–218
- 测量页面访问频率，207–210
- 测量页面性能，210–212
- 测量区域性能，212–216
- 按加权页面性能排列的页面视图页面，210，211
- `RAD` 工具，207
- 用于收集统计信息的替换字符串，215，216

#### 照片

- 上传，图书目录，293

#### 饼图

- 创建库存量饼图，308–311
- 在仪表板页面中可视化数据，171，172
- 在图形图表中可视化数据，147，148，151

#### 拼音输入法

- 178

#### `PL/SQL`

- 决定是否使用 `APEX`，2
- 嵌入式 `PL/SQL` 网关，5
- 使用 `APEX` 时的限制，4
- `Oracle XML DB HTTP` 服务器，5
- 插件，138
- 保存页面时运行 `PL/SQL` 过程，102–103
- 何时使用 `APEX`，2

#### `PL/SQL` 代码

- 向表单添加服务器端验证，74
- 在代码中直接引用表单字段，90

#### 平台

- 安装 `Oracle APEX`，6

#### 插件

- 138
- 创建，134–135
- 通过插件扩展用户界面，133–138
- 使用插件，136–137

#### `Popup LOV` 控件

- 在表单中从值列表中选择，55

#### `端口 8080`

- `URL` 解释，277

#### 端口号

- 安装 `Oracle APEX`，11

#### 主键

- 创建客户表，33
- 使用 `APEX` 时的限制，2，3

#### 过程

- 103
- `PL/SQL` 过程，102
- 保存时运行，102–103
- `Send Email` 过程，104，105–106

#### 项目管理

- 管理开发过程，25–28

#### 项目

- 创建/更新功能，25
- 决定是否使用 `APEX`，1–4
- 何时使用/不使用 `APEX`，2

#### `发布应用程序翻译` 字段

- 196

#### 发布应用程序 `URL`

- 275–276



### Q

查询步骤  
创建多系列图表，156，157

### R

RAD 工具，1  
APEX，2  
性能，207

单选组控件  
在表单中从值列表中选择，55

*参见* APEX 配方

记录  
创建报表以显示，42  
创建，图书目录应用程序，279–282

区域和语言设置  
为双字节字符输入进行设置，174

区域缓存，216，217

区域，172  
在页脚显示性能统计信息，213，215  
启用区域缓存，216–218  
测量区域性能，212–216  
从区域引用快捷方式，132  
设置中央管理页面，314，315  
在区域中使用 HTML 文本（含快捷方式），131

`Render Function Name`，回调部分  
创建插件，135

渲染  
启用页面缓存，218  
启用区域缓存，217  
测量页面访问频率，210  
测量页面性能，210  
测量区域性能，212，214

报表和表单页面，14  
创建表单以管理图书目录，283  
创建报表以管理数据，46

`报表属性`选项卡  
创建图书目录前端，301

`报表页面创建`向导  
防止 `SQL 注入`攻击，237

`报表`页面类型  
创建经典报表，140  
动态筛选报表，143

报表，14，50  
聚合函数，50  
图书目录，289，292  
内置功能，50  
图表视图，50  
经典报表，143  
计算列，50  
创建图书目录前端，297–305  
创建经典报表，139–143  
创建参数化报表，143–147  
创建报表以管理数据，42–50  
创建报表以显示现有记录，42  
创建库存数量饼图，308–311  
动态筛选，143  
筛选，50  
分组记录，50  
高亮显示，50  
交互式报表，14，143，147  
使用 `APEX` 时的限制，3  
分页，50  
排序，50  
指定要包含在报表中的列列表，44  
在图形图表中可视化数据，147–152  
在多系列图表中可视化数据，153–158  
在日历上可视化数据，159–162  
在地图上可视化数据，162–169

`报表属性`选项卡  
创建图书目录前端，303

`reset_image_prefix` 脚本  
安装 `Oracle APEX`，10

可重用性  
授权方案，236

可重用代码片段，创建，128–133

行，报表  
高亮显示，50

`ROWS_FETCHED` 替换字符串  
收集性能统计信息，215

`运行应用程序`图标  
发布应用程序 URL，275


### S

### ![images](img/square.jpg) S

#### 薪资表
- 创建, 81

#### 销售力应用程序
- 创建数据库应用程序, 36–39
- 创建报表以显示现有记录, 42
- 客户数据录入表单, 40
- 主页, 40
- 登录页, 39

#### 销售数据表
- 创建, 163
- 在地图上可视化数据, 163
- 模式 *参见* `database schemas`

#### 脚本
- 使用 `SQL*Plus` 导入应用程序, 267–269
- 跨站脚本 (`XSS`) 攻击, 245–250

#### 安全
- 身份验证, 221–227
- 授权, 221
- 跨站脚本 (`XSS`) 攻击, 245–250
- 定义用户访问权限, 227–236
- 将 `APEX` 应用程序暴露到互联网, 5
- 识别 `APEX` 部署模型, 4
- `Oracle HTTP Server` 和 `mod_plsql`, 5
- `SQL` 注入攻击, 236–245
- 两层部署, 5

#### 安全区域
- 更改应用程序的主页, 307
- 创建授权方案, 229

#### 安全属性链接
- 更改应用程序的主页, 307

### 安全漏洞利用, 221

#### 选择列表控件
- 在表单中从值列表中选择, 56

### 发送电子邮件进程, 105–106

#### 服务器，HTTP
- 识别 `APEX` 部署模型, 4

#### 服务器端任务
- 过程, 103

### 服务器端验证, 77
- 添加到表单, 71–77
- 表格式表单, 78–80

### 服务器端验证 (*续*)
- `Condition Type` 字段, 77
- 项目级验证, 77
- 页面级验证, 77
- 表格式表单验证, 77

### 服务 *参见* `web services`

### 共享组件区域, 107, 109, 113
- 向页面添加自定义 `CSS` 样式, 115
- 更改应用程序的主页, 307
- 创建和引用快捷方式, 129
- 创建身份验证方案, 222
- 创建授权方案, 229
- 创建插件, 134
- 在应用程序中创建选项卡, 317
- 在应用程序中创建主题, 121
- 映射到已翻译的应用程序, 182
- 修改表单控件模板, 126
- 设置全球化属性, 181
- 存储/显示带时区信息的日期, 200
- 切换应用程序主题, 124
- 使用自定义 `CSS` 文件, 117, 119

### 快捷方式, 133
- 创建/引用, 129–133
- 从区域引用, 132
- 在区域中使用 `HTML` 文本, 131

#### 穿梭板控件
- 在表单中从值列表中选择, 56

#### 单点登录 (`SSO`) 集成
- 识别 `APEX` 部署模型, 4

### 软件配置管理, 25

### 排序，报表, 50

#### 语言
- `APEX` 术语, 13–16

#### 电子表格
- 创建 `Websheet` 应用程序, 61–65

#### SQL
- 导出应用程序, 259
- 保存页面时运行 `PL/SQL` 过程, 102–103

### SQL 注入攻击, 236–245
- 绑定变量, 245
- 替换变量, 244

#### SQL 报表类型
- 在 `Dashboard` 页面中可视化数据, 172

#### SQL 语句
- 图表, 152
- 创建经典报表, 141
- 创建多序列图表, 158
- 创建参数化报表, 144
- 动态过滤报表, 144
- 视图, 152
- 在图形图表中可视化数据, 150
- 在日历上可视化数据, 160

#### SQL 语句区域
- 在 `Dashboard` 页面中可视化数据, 171

#### SQL*Plus
- 导入应用程序, 267–269
- 安装 `Oracle APEX`, 7, 9
- 设置工作目录, 8

#### 统计信息，性能
- 在区域页脚中显示, 213, 215
- `Page Views by Weighted Page Performance` 页面, 210, 211
- 用于收集的替换字符串, 215, 216
- 使用情况统计图表, 209

### 库存数量饼图，创建, 308–311

#### 字符串
- 替换字符串, 113

#### 样式
- 向页面添加自定义 `CSS` 样式, 114–117
- 样式类示例, 114

#### 样式表类
- 向页面添加自定义 `CSS` 样式, 114, 117
- 使用自定义 `CSS` 文件, 117

### 子类别表，创建, 85

### 隶属于页面字段, 37

### 替换字符串, 113
- `APP_IMAGES`, 114
- 内置替换字符串, 235
- 更改应用程序的主页, 308
- 收集性能统计信息, 215, 216
- `LABEL`, 113
- `WORKSPACE_IMAGES`, 114, 120

#### 替换变量
- `SQL` 注入攻击, 244


### ![images](img/square.jpg) T

使用报表页面类型的表格

创建报表以显示现有记录，43

表格

Books（书籍）表，279

Category（类别）表，281

创建，279–282

Customers（客户）表，32–35

定义完整的表集，42

外键映射，282

Inventory（库存）表，281

表空间，创建，7

选项卡，322

创建数据库应用程序，38

在应用程序中创建，316–322

表格表单，14, 60

添加服务器端验证，78–80

创建表格数据录入表单，58–60

使用 APEX 时的限制，3

验证，服务器端，77

`Team Development`（团队开发）模块，APEX，28

基于团队的开发

管理开发流程，25–28

为团队开发设置工作区，17–25

模板，112

在模板中插入动态字段，113

修改表单控件模板，125–128

主题，113, 125

术语 `参见` APEX 术语

主题，13, 113

在应用程序中创建主题，121–123

切换应用程序主题，124–125

模板，113, 125

提供主题的网站，125

时区

启用自动时区功能，201

存储和显示日期，199–205

`TIMING` 替代字符串

收集性能统计数据，215, 216

工具

决定是否使用 APEX，1–4

`Monitor Activity`（监控活动）工具

测量页面访问频率，207–210

测量页面性能，210–212

`RAD`（快速应用开发）工具，1

`TOTAL_ROWS` 替代字符串

收集性能统计数据，215

事务量

何时使用 APEX，2

将应用程序用户界面翻译成另一种语言，180–199

导出 `XLIFF` 文件，187–190

导入已翻译的 `XLIFF` 文件，192–195

映射到已翻译的应用程序，182–185

发布已翻译的应用程序，195–197

将可翻译文本播种到翻译存储库，185–187

设置全球化属性，181–182

测试，197–199

翻译 `XLIFF` 文件，190–192

翻译存储库

将可翻译文本播种到，185–187

故障排除

安装 Oracle APEX，11

两层部署

Oracle XML DB HTTP 服务器，5

类型

更改表单中的字段项，51

更改 Websheet（网页表格）列类型，65–68

`Database Application`（数据库应用程序）类型，42

N 前缀的数据类型，180

### ![images](img/square.jpg) U

UI（用户界面）

通过插件扩展 UI，133–138

翻译成另一种语言，180–199

唯一约束

创建 Customers（客户）表，33

UNIX

导出应用程序，257

上传

在表单中创建文件上传字段，56–58

URL（统一资源定位符）

URL 各部分详解，277

发布应用程序 URL，275–276

使用统计图表，`Monitor Activity`（监控活动）工具，209

用户访问权限，定义，227–236

`User Interface`（用户界面）区域

创建和引用快捷方式，129

创建插件，134

在应用程序中创建主题，121

切换应用程序主题，124

### ![images](img/square.jpg) V

验证

列非空验证类型，79

使用 APEX 时的限制，3

向表单添加验证

客户端 JavaScript 验证，81–84

服务器端验证，71–77

向表格表单添加验证，78–80

值

批量修改 Websheet（网页表格）中的值，69–70

在表单中从值列表中选择，53–56

存储计算值，94–96

变量，动态，133

视图

日历视图，162

图表视图，152

`SQL` 语句，152



### ![images](img/square.jpg) W

**Web 服务器**
- 从表单调用 Web 服务引用，99–101
- 使用 Oracle XML DB HTTP 服务器作为 Web 服务器，5
- `Web Service References` 部分，101

**Web 服务**
- 添加引用，98–99
- 启用区域缓存，217
- 与 Web 服务交互，96–101
- APEX 中支持的类型，101

**Websheet 应用程序**，13
- 更改列的项类型，65–68
- 创建，61–65
- 为列创建下拉列表，66
- 批量修改值，69–70

**基于 Windows 7 的操作系统**
- 设置双字节字符输入，173

**工作目录**，`SQL*Plus`
- 安装 Oracle APEX，8

**工作区管理员用户**
- 为团队开发设置工作区，19, 20

`WORKSPACE_IMAGES` 替换字符串，114, 120

**工作区**，13, 14
- 创建 `Customers` 表，32
- 创建，17, 18
- 为其创建方案，18, 19
- 数据库工作区，25
- 主页，23, 24
- 登录，22, 23
- 映射到数据库方案，24
- 为团队开发设置，17–25

### ![images](img/square.jpg) X, Y, Z

**XLIFF 文件**，199
- 导出，187–190
- 导入已翻译的文件，192–195
- 翻译，190–192
