# 游标成本分析

`特征`, 461

## 数据库设计

`采用索引设计`, 552

`域和参照完整性约束`, 551

`实体完整性约束`, 549–550


内存存储，554

乐观并发模型，462, 468

过度规范化，549

`RBAR` 过程，459

`sp_` 前缀，554

只读并发模型，462, 468

触发器，554

滚动锁并发模型，463, 469

规范化不足，548

服务器端游标位置，462, 467

`数据库引擎优化顾问`，163, 165

静态游标，464, 470

高级调优选项，168

`T-SQL` 游标

`文件` 和 `表` 单选按钮，166

[www.it-ebooks.info](http://www.it-ebooks.info/)

■ 索引

单个表，167

`CountDuration`，522

限制，179

错误/警告，544

计划缓存，166, 178–179

`扩展事件` 会话，520

`查询` 单选按钮，166

外部因素分析

查询调优

代码修改，535

应用建议，174

连接选项，527

常规设置，169

成本降低，535

初始建议，173

碎片整理（ *参见* 碎片整理）

建议，174

执行计划，538

成功调优，175

内部行为，532

`T-SQL` 命令，174

处理策略，534

调优选项，172

查询执行计划，533

报告，170

统计有效性，527

服务器和数据库，166

包装过程，541

`开始分析` 按钮，168

深入分析，521

跟踪文件

`INSERT` 语句，545

创建，175

优化效果，543

`数据库` 调优引擎，178

查询优化过程，516

手动统计，178

查询类型，519

工作负载分析，177

资源使用情况和响应时间，543

调优选项，167

`SELECT` 语句，516, 545

调优进度，169

服务器资源，515

`数据库` 性能测试

`SLA`，543

`分布式重放`

`SQL` 查询，521, 544–545

架构，506

`SQL Server` 性能，517

客户端配置，512

`SSMS`，521

执行，512

`SumDuration`，522

预处理，511

`UPDATE` 语句，516

`XML` 配置文件，511

`XML` 字段数据，521


