# 第 20 章：阻塞与被阻塞进程
第 20 章：阻塞与被阻塞进程
## 阻塞基础
阻塞基础
## 理解阻塞
理解阻塞
### 原子性
原子性
#### SET XACT_ABORT ON
`SET XACT_ABORT ON`
#### 显式回滚
显式回滚
### 一致性
一致性
### 隔离性
隔离性
### 持久性
持久性
## 锁
锁
### 锁粒度
锁粒度
#### 行级锁
行级锁
#### 键级锁
键级锁
#### 页级锁
页级锁
### 区域级锁
区域级锁
#### 堆或 B 树锁
堆或 B 树锁
#### 表级锁
表级锁
#### 数据库级锁
数据库级锁
## 锁操作与模式
锁操作与模式
#### 锁升级
锁升级
### 锁模式
锁模式
#### 共享 (S) 模式
共享 (`S`) 模式
#### 更新 (U) 模式
更新 (`U`) 模式
#### 排他 (X) 模式
排他 (`X`) 模式
### 意向共享 (IS)、意向排他 (IX) 和 共享意向排他 (SIX) 模式
意向共享 (`IS`)、意向排他 (`IX`) 和 共享意向排他 (`SIX`) 模式
### 架构修改 (Sch-M) 和 架构稳定性 (Sch-S) 模式
架构修改 (`Sch-M`) 和 架构稳定性 (`Sch-S`) 模式
#### 批量更新 (BU) 模式
批量更新 (`BU`) 模式
#### 键范围模式
键范围模式
### 锁兼容性
锁兼容性
## 隔离级别
隔离级别
### 未提交读
未提交读
### 已提交读
已提交读
### 可重复读
可重复读
### 可序列化
可序列化
### 快照
快照
## 索引对锁定的影响
索引对锁定的影响
### 非聚集索引的影响
非聚集索引的影响
### 聚集索引的影响
聚集索引的影响
### 索引对可序列化隔离级别的影响
索引对可序列化隔离级别的影响
## 捕获阻塞信息
捕获阻塞信息
### 使用 SQL 捕获阻塞信息
使用 SQL 捕获阻塞信息
### 扩展事件与 blocked_process_report 事件
扩展事件与 `blocked_process_report` 事件
### 阻塞解决方案
阻塞解决方案
#### 优化查询
优化查询
#### 降低隔离级别
降低隔离级别
## 分区争用数据
分区争用数据
## 减少阻塞的建议
减少阻塞的建议
## 检测和收集阻塞信息的自动化
检测和收集阻塞信息的自动化
## 小结
小结
