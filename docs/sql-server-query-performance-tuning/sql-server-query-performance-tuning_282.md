# 第 19 章：减少查询资源使用
第 19 章：减少查询资源使用
## 避免资源密集型查询
避免资源密集型查询
### 避免数据类型转换
避免数据类型转换
### 使用 EXISTS 而非 COUNT(*) 验证数据存在
使用 `EXISTS` 而非 `COUNT(*)` 验证数据存在
### 使用 UNION ALL 而非 UNION
使用 `UNION ALL` 而非 `UNION`
### 为聚合和排序条件使用索引
为聚合和排序条件使用索引
### 避免在批处理查询中使用局部变量
避免在批处理查询中使用局部变量
### 命名存储过程时要小心
命名存储过程时要小心
## 减少网络往返次数
减少网络往返次数
### 一起执行多个查询
一起执行多个查询
### 使用 SET NOCOUNT
使用 `SET NOCOUNT`
## 减少事务成本
减少事务成本
### 减少日志开销
减少日志开销
### 减少锁开销
减少锁开销
## 小结
小结
