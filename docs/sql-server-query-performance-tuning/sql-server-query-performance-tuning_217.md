# 数据库阻塞

`blocked_process_report`

`blocked_process 字段`, 433–434

`阻塞脚本`, 431

`阻塞进程`, 430

`阻塞减少`, 437

`原因`, 430

`连接`, 398

`一致性`, 401

`死锁`, 397

`持久性`, 402

`隔离性`, 401–402

`锁定`, 397

`锁管理器` ( *参见* 锁管理器)

`阻塞分析`, 438–439

`阻塞检测`, 441

`阻塞会话`, 439

`SQL Server 警报`, 438

`解决技术`, 435

`User1 和 User2 事务`, 431

`原子性`

`特征`, 461

`客户端游标`, 460

`警报属性`, 440

`客户端游标位置`, 461, 466–467

`默认结果集`

`ADO.NET 环境`, 472

`优点`, 472

`描述`, 438

`缺点`, 473

`MARS`, 472

`动态游标`, 466, 471

`事件`, 475

`仅向前快速游标`, 470

`仅向前游标`, 464, 470

`键集驱动游标`, 465, 471

`低成本`, 480

