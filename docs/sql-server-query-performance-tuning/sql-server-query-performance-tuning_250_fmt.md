# 第 18 章：查询设计分析

## 查询设计建议

### 对小型结果集进行操作

### 限制 `select_list` 中的列数

### 使用高选择性的 `WHERE` 子句

### 有效使用索引

### 避免非 SARGable 搜索条件

### 避免在 `WHERE` 子句列上使用算术运算符

### 避免在 `WHERE` 子句列上使用函数

## 避免使用优化器提示

### `JOIN` 提示

### `INDEX` 提示

## 使用域完整性和引用完整性

#### `NOT NULL` 约束

### 声明式引用完整性

## 小结