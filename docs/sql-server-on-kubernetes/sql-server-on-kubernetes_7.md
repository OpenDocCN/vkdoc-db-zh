# 索引

**A**
应用程序编程接口 (`API`) 对象 原语 `ConfigMaps` 控制器 `pods` `ReplicaSet` 操作 `secret` 服务 `StatefulSet` 存储 服务器 Arc–启用的数据服务 架构 组件 构建 核心功能 数据控制器 部署 可用性组 `azdata` 命令 配置 配置文件 数据控制器 环境变量 `kubectl` 上下文 列出配置文件名称 监控部署状态 SQL 托管实例 基础层 Kubernetes 集群 命名空间 `PostgreSQL` 先决条件 SQL 托管实例 切换 `kubectl` 上下文 工具 可用性组 (AGs) Azure 账户 详情 `deployment-dynamic-azure.yaml` 存储 数据持久性 Azure Kubernetes 服务 (AKS) `az aks` 创建集群 (bash) 登录账户 PowerShell 资源组 检索凭据 虚拟机 Azure 资源管理器 (`ARM`)

**B**
大数据集群 (`BDC`) AI/应用程序 应用程序池 架构/组件 计算池 控制平面 数据池 数据虚拟化 部署 `azdata` 输出 `bdc.json` 配置 功能 Kubernetes 构建 Linux 托管数据平台 监控/管理 SQL Server 主实例 存储池 更新过程

**C**
命令行接口 (`CLI`) 容器 基础知识 应用程序部署 命令行工具 `sqlcmd` 数据持久性 (参见 数据持久性) 数据库备份 部署自动化 `docker pull` 元素 临时的 快速/一致的部署 镜像 内部结构 隔离 生命周期 列出数据库 本地系统 日志命令 `ls` 命令 操作系统 编排器 概述 删除选项 资源 运行命令 速度/打补丁 SQL Server 2019 CU1 `start/stop` 命令 故障排除技术 宇宙 虚拟机 虚拟化 Windows

**D, E, F**
数据持久性 容器生命周期 `cp` 命令 数据卷规范 文件位置 `inspect` 命令 实例级配置 `ls` 命令 Pod 生命周期 `remove` 命令 `run` 命令 `sqlcmd` SQL Server 实例 `stop/remove` 命令 存储 (参见 存储 数据持久性) 卷 可写层 可写层 Docker 主机

**G, H**
Grafana 仪表板 Arc–启用的数据服务 大数据集群 SQL Server 指标 系统指标 Telegraf 系统仪表板

**I, J**
安装 考虑事项/方法 额外选项 考虑事项 部署选项 GitHub 安装方法 网络要求 单节点集群 系统要求

**K**
交互 部署 声明式 命令式 配置修改 `nginx` 副本定义 暴露/访问服务 `ClusterIP` 服务 `get service` 详情 `LoadBalancer` 类型 `nginx` 部署 `NodePort` `kubectl` 命令 `AKSCluster` CLI 工具 上下文 异常 命名空间 操作 输出 资源 检索详细列表 Windows 工作站

**L**
Kibana 仪表板 大数据集群 组件 配置 `elasticsearch` `fluent-bit` 安装 直方图 索引模式 `kibana.yaml` 日志聚合/管理 概述 SQL Server 日志 错误日志 自由文本过滤器 `mssql` StatefulSet

**M**
监控方案 清理 Kibana 安装/配置 日志聚合/管理 SQL Server `kubectl`

**N, O, P, Q, R**
网络地址转换 (`NAT`) 网络文件系统 (`NFS`) 服务器 `deployment-dynamic.yaml` 动态配置磁盘 `enable-nfs.sh` `kube-apiserver.yaml` `nfs-provisioner.yaml` `nfs-rbac.yaml` `nfs-storageclass.yaml` 持久卷

**S**
自管理集群 控制平面 `ClusterConfiguration.yaml` 配置文件 创建 `init` 命令 `kubeadm` 版本 `kubectl` 配置 Pod 网络 存储 `wget` 命令 节点 虚拟机 配置文件 安装/配置 包 保留 系统参数 版本列表 结构化查询语言 (`SQL`) 备份/恢复 组件 配置选项 描述 部署 生命周期/数据持久性 Pod 准备 简而言之 密钥 存储平台 生产环境 服务器指标 组件 `DaemonSet` 仪表板 数据源 `deployment.yaml` 导入仪表板 `InfluxDB` 命名空间 要求 `service.yaml` 设置变量和密钥 统计信息 `storage.yaml` `telegraf-configmap.yaml` Telegraf 仪表板 更新镜像 YAML 定义 检查状态 `kubectl get pods/service` 清单 进程 `NFS` 共享 Pod/服务 规范 `sqlcmd` 查询/命令 `sql-deployment.yaml` 版本匹配 存储 数据持久性 Azure 类 别 动态配置 网络文件系统 持久卷 访问模式 绑定 声明 请求 定义 静态配置 存储类型 卷 `pv-nfs-data-static` 回收策略 有状态应用 静态类 独特配置属性 卷 系统 交换空间

**T**
工具安装 管理工作站 `azdata` Chocolatey/`choco` Linux 机器 工具 Windows 工作站 生产系统

**U**
联合文件系统

**V**
虚拟机 (`VM`) Azure 账户 详情 配置文件 计算和存储资源 容器 基础知识 安装/配置 Linux 主机 文件内容 网络配置 包 保留 系统参数 PowerShell 脚本 自管理集群 版本列表

**W, X, Y, Z**
Windows 机器
