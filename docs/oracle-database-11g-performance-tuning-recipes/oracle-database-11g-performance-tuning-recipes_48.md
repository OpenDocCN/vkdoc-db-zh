# 段顾问建议

显示表信息

建议与推荐，21

`ASA_RECOMMENDATIONS`，21

`AWR`，21

`DBMS_SCHEDULER`，20

`DBMS_SPACE` 包，19，20

企业管理器，23

发现结果，20

检索工具，21

自动通过电子邮件发送段建议，27–28

释放未使用空间，32

`ALTER TABLE...SHRINK SPACE` 语句，32–33

启用行移动，32

手动生成建议，23

`DBMS_ADVISOR.CREATE_TASK` 过程，对象类型，26–27

`DBMS_ADVISOR` 包，23，25

`DBMS_ADVISOR.SET_TASK_PARAMETER` 过程，27

`DBMS_SPACE` 包，24

重建跨行行，28

`MOVE` 语句，29

`PCTFREE`，30

行链接，30，31

`UPDATE` 语句，30

`SELECT` 语句，254

`FROM` 子句，255

`SELECT` 子句，255

子查询。*参见* 子查询

`SELECT_BASELINE_DETAILS` 函数，125

单行子查询，264–265

跳跃扫描功能，62

`SQL`，253，299

避免全表扫描，288–290

避免使用 `NOT` 子句，293

比较运算符，294

缺点，293

`NOT IN`，294

`NOT LIKE`，294

`BETWEEN` 子句，274

Oracle 优化器，276

陷阱，275

比较表

`INTERSECT` 集合运算符，271

`MINUS` 集合运算符，269–270

控制事务大小，295–297

执行计划。*参见* 执行计划，`SQL`

内联视图，290–292

`BILLING_INFO` 视图，292

`PRODUCT_INFO` 视图，292

`SERVICE_INFO` 视图，292

`ISO` 语法，253

连接表

内连接。*参见* 内连接

外连接。*参见* 外连接

空值，277

在 `SELECT` 子句中，277–278

在 `WHERE` 子句中，279–280

部分列值

优点，283

注意事项，282

`LIKE` 运算符，281

`TO_CHAR` 函数，281

计划基线。*参见* 计划基线

重用 `SQL` 语句，284

绑定变量，284–285

`execute immediate` 语句，绑定变量，287

硬解析，286–287

`PL/SQL` 块，284–285

软解析，285

步骤，285–286

`TKPROF` 实用程序，286

`SAVEPOINT` 命令，297

`SELECT` 语句。*参见* `SELECT` 语句



