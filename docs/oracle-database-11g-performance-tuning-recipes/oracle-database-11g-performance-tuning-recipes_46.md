# ![images](img/square.jpg)C

笛卡尔积 (Cartesian join), 261

专家中心 (Center of Expertise, CoE), 342

`COALESCE function`, 280

组合索引 (Concatenated index), 60–62

## 争用 (Contention)

147

分析 Oracle 等待事件, 151–152

阻塞锁, 163–165

DML 锁, 168

入队等待事件, 165–167

排他锁, 164

长期策略, 167

共享锁, 164

短期策略, 167

事务锁, 168

`V$LOCK view`, 165

`V$SESSION view`, 166

缓冲区忙等待, 157

数据块, 157–158

段头, 157

UNDO 头和 UNDO 块, 158

识别锁定对象, 168–169

识别 SQL 语句, 150–151

闩争用, 178

高速缓存缓冲区链, 179

高速缓存缓冲区 LRU 链, 179

`CURSOR_SHARING parameter`, 180

共享池和库缓存闩, 179

日志文件同步等待事件, 158–160

Oracle 企业管理器

分析等待, 182–183

管理锁, 181–182

Oracle 等待接口, 147

由其他会话读取等待事件, 160–161

数据库中最近的等待事件, 174–175

数据库中最近被锁定的会话, 171–174

减少直接路径读取等待事件, 161–162

`RVWR`, 162–163

SGA 中的并发请求, 147

锁定所花费的等待时间, 175–178

`TM lock` 争用, 169–171

事务锁, 147

理解响应时间, 147

等待事件的详细信息, 148–149

处理时间, 148

时间模型统计, 149

等待时间, 148

理解等待类事件, 152

`Application wait class`, 152, 153

`User I/O wait class`, 152

`V$SESSION_WAIT view`, 153, 154

等待类, 154

并发问题, 157

类型, 154–155

`CONTROL_MANAGEMENT_PACK_ACCESS parameter`, 115

关联子查询 (Correlated subqueries), 267

`EXISTS clause`, 268

`NOT EXISTS`, 269

基于成本的优化器 (Cost-based optimizer, CBO), 335, 358

`CPUspeedNW system statistics`, 465

## CREATE DATABASE 脚本

创建自动 UNDO 表空间, 3

默认表空间 USERS, 3

默认临时表空间 TEMP, 3

联机重做日志, 4

将数据文件放置在目录中, 4

设置 DBA 相关用户的密码, 4

SYSTEM 表空间, 3

交叉连接 (Cross join), 261

游标泄漏 (Cursor leak), 223

`cursor_sharing initialization parameter`, 473–476

