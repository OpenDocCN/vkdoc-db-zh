# 索引

## A
- 警报日志文件
- `APEX` (`Application Express`)
- 应用领域索引
- 架构组件：数据库分片、专用服务器配置、实例/数据库、多租户容器数据库、网络案例：客户端软件、连接字符串、专用服务器连接、轻松连接加、监听器进程、共享服务器连接、`tnsnames.ora` 文件、`SGA`/后台进程、分片数据库、共享服务器、单租户/非容器数据库
- 归档进程 (`ARCn`) 重做/撤销
- `自动存储管理`
- 后台 (`ASMB`) 专注的后台进程 数据泵
- 原子性 自动提交
- `自动诊断仓库` (`ADR`)
- `自动诊断仓库命令解释器` (`ADRCI`)
- 自动索引：操作 工作流程 `CONFIGURE` 过程 数据库管理 `DBA_INDEXES` 功能 创建 管理
- `自动内存管理` (`AMM`)
- `自动共享内存管理` (`ASMM`) 自动技术 组件 手动管理 `SGA_TARGET`
- `自动存储管理` (`ASM`) 数据库类型 数据文件
- `自动工作负载仓库` (`AWR`)
- 自动事务 操作/后果 `AUTONOMOUS_INSERT` 过程 控制方法 错误日志信息 功能 `LOG_ERROR` 过程 `NONAUTONOMOUS_INSERT` 过程 `PL/SQL` 语句 过程 `ROLLBACK` 命令 `SYS.SEQ$` 表

## B
- 后台进程
- 不良事务习惯 配置问题 数据完整性 循环过程 `ORA-01555` 错误 性能影响 可重启进程 快照/旧 错误信息 `UPDATE` 语句
- 位图索引 即席查询 `AND`/`OR` 数据仓库/大型报表系统 `DBMS_RANDOM` 包 `DEPT.DEPTNO` 实验 功能 `GENDER`, `LOCATION`, 和 `AGE_GROUP` `JOB-IDX` 表示 连接索引 低不同基数 `OR` 表示 读写密集型环境
- 黑盒方法 方法 自动事务 位图索引 阻塞/串行化问题 内置队列能力 胜任 并发问题 数据库应用 错误记录机制 恐惧、不确定性和怀疑 (`FUD`) 功能 查找/锁定记录 硬 数据库索引 代码插桩 大量性能问题 `PDB1` 源代码 项目失败 可扩展数据库场景 串行化问题 `SKIP LOCKED` 子事务 未处理记录
- 区块链技术 `CREATE BLOCKCHAIN TABLE` 子句 `DBMS_BLOCKCHAIN_TABLE` 删除表子句 功能 哈希/数据格式子句 仅插入表 行保留子句控制 敏感财务信息 表创建

- 块清除 重做 人工条件 缓冲区缓存 `COMMIT`-时处理 概念 `DBMS_STATS` 修改的块 `OLTP` 系统 `PL/SQL` 过程 处理信息 移除 `SELECT` 命令 表和提交 工作流程 撤销 `ALL_OBJECTS` 表 `COMMIT` `SCN` 修改 `ORA-01555` 错误条件 `PL/SQL` 过程 `run.sh` 文件 `run.sh` 脚本 表空间 `test2.sql` 事务表 `UPDATE` 语句
- 阻塞 `AUTONOMOUS_TRANSACTION` `DBMS_LOCK` `DML` 语句 持有会话 `INSERT` 选项 合并/更新/删除 `SYS_GUID()` 内置函数 设计良好的应用程序

## C
- `B*Tree` 索引 二叉搜索树 分支块 聚集因子 `COLOCATED_PK` 索引 降序索引 不同情况 快速全扫描 分数 通用机制 高度平衡索引 键压缩 `COMPRESS` 索引 `IDX_STATS` 表 `OPT_CMPR_PCTSAVE` `OWNER` 组件 前缀和后缀组件 表/索引 索引范围扫描 布局 叶节点/块 非唯一索引 物理组织 `ARRAYSIZE` 逻辑 I/O `COLOCATED` 表 `DISORGANIZED` 表 功能 物理数据布局 主键 `TKPROF` (`SQL Trace`) 输出 主键索引 反向键索引 `DUMP` 函数 功能 网络连接 性能测试 `PL/SQL` 环境 `RAC` 实例 `REVERSE` 命令 经验法则 `TABLE ACCESS BY INDEX ROWID`
- `CASCADE UPDATE`
- 变更跟踪文件
- `变更跟踪进程` (`CTWR`)
- 字符（或逗号）分隔值 (`CSV`)
- 检查点进程 (`CKPT`)
- 提交/回滚处理 块清除
- `CLASSPATH` 变量
- `COMMIT` 语句
- `COMMIT` 修改的块 `DBMS_UTILITY` 包 `doInserts()` 方法 删除表结构 因素贡献 人类可读的 性能输出 `INSERT`/`COMMIT` 插入/绑定/执行 选项 `LGWR` 写入 主方法 操作 `OS` 命令行 `PL/SQL` 提交时优化 `ROLLBACK` 稀缺的系统资源 `系统变更号` (`SCN`) `TKPROF` 工具 跟踪目录
- 复合分区
- 并发控制机制 访问问题 上下文切换 数据库事务 实现 锁模型 多版本控制 参见 `Multiversioning` 模型 非阻塞方法 页级锁定 资源调度程序 `SCHEDULES` 表 `TX` 事务锁
- 容器数据库 (`CDB`)
- 控制文件

## D
- `数据库管理员` (`DBA`)
- 数据库独立性 `ANSI` 方法 方法 并发请求 死锁 实体关系图 (`ERD`) 功能/函数 `IDENTITY` 属性 初始功能 内联视图 分层编程 非阻塞 `NULL` 比较 开放性 结果 标量子查询 序列生成 可串行化事务 会话的事务 解决问题 `SQL99` 标准 `SQL` 查询 存储过程 理论优势 `UPDATE`/`SELECT` 查询 `Windows`/`UNIX`/`Linux`
- `数据库驻留连接池` (`DRCP`) 优势 服务器进程池化
- `数据库资源管理器进程` (`DBRM`)
- 数据库表 区块链表 区块链技术 聚集索引 集群对象 定义 外部表 一般信息 哈希聚集表 哈希集群 堆组织表 索引组织表 嵌套表 对象表 对象类型 排序哈希聚集表 排序哈希集群 临时表 术语 类型
- `数据定义语言` (`DDL`) 压缩 区修剪 分配 描述 `UNIFORM` *对比* `AUTOALLOCATE` 区 `AUTOALLOCATE_TEST` 表空间 `big_table.log` 文件 `CREATE TABLE AS SELECT` 外部表定义 本地管理表空间 (`LMT`) 表空间 `UNIFORM_TEST` `USER_SEGMENTS` 锁定 `ALTER TABLE T CREATE/ALTER CREATE VIEW` 命令 `DBA_DDL_LOCKS` 视图 直接加载 (`DL`) 锁 `ONLINE` 关键字 操作 存储过程类型 并行化命令 `CREATE TABLE AS SELECT` 语句 直接路径 区修剪 `USER_INFO` 表
- 数据文件 文件系统 机制 `ASM` 设计 块 聚集文件系统 区 `OS` 文件系统 段 存储层次结构 表空间 `OMF` 命名约定 可插拔数据库 单实例 容器数据库 `SYSTEM` 数据文件
- 数据加载/卸载 大批量数据加载工具 数据泵格式 外部表 方法 `DEFAULT DIRECTORY` 子句 `demo1.bad` 文件 `demo1.log` 文件 错误信息 `EXTERNAL_TABLE` 参数 功能 `INSERT` 语句 加载不同文件 修改 多用户问题 并行处理 `PREPROCESSOR` 子句 项目 引用列 `SQLLDR` 控制文件 `SQL*Plus` 任务 `tmp` 目录 平面文件
- `AUTHID CURRENT_USER` 控制文件 `CONTROL_FILES` 参数 原始数据 (`.dat`) 文件 数据文件 `DESCRIBE` 表 `NLS_DATE_FORMAT` `PL/SQL` 工具 `SQL*Plus` 命令行 `UTL_FILE`
- `SQLLDR` 参见 `SQLLDR` 技术
- `数据操作语言` (`DML`) 类别 `TM` 锁 事务 (`TX`) 锁 存储过程 `TRACE` 文件
- 数据类型 `BFILE` 二进制数据 二进制数据 (`RAW` 类型) `BINARY_FLOAT`/`BINARY_DOUBLE` `BLOB` `CHAR` 字符/二进制字符串类型 字符集 `NLS` 控制 `CHAR`/`VARCHAR2`/`NVARCHAR2` 字符串 字节/字符 `CHAR`/`NCHAR` 类型 `CHAR`, `VARCHAR2`, `NCHAR`, 和 `NVARCHAR2` `CHAR` 类型 `CHAR_COLUMN` Hello World “N” 变体 字符串类型 变长字符串 `CLOB` `DATE` `DATE`, `TIMESTAMP`, 和 `INTERVAL` 加/减时间 `ADD_MONTHS` 函数 `DATE` 类型定义 差异 格式 `INTERVAL` 类型 `MONTHS_BETWEEN` 函数 `TIMESTAMP` 扩展类型 容器数据库 `LOB` 段详情 `MAX_STRING_SIZE` 非扩展列 单实例数据库 `USER_LOBS` `USER_OBJECTS` `HEXTORAW` 函数 `INTERVAL DAY TO SECOND`/`YEAR TO MONTH` `JSON` `JSON` 格式 `LONG` 类型 `ALL_VIEWS` 字典视图 `AUTHID CURRENT_USER` 复制类型 `DBMS_SQL.COLUMN_VALUE_LONG` 关键点 限制 (`LONG RAW` 类型) 手动状态 `SUBSTR_OF` `WHERE` 子句 操作 `LONG RAW` `NCHAR` 类型 `NCLOB` 数字类型 近似数字 `BINARY_FLOAT`/`BINARY_DOUBLE` 类型 浮点数 非原生类型 `NUMBER` 类型 性能影响 存储数字 `VSIZE` 函数 概述 `RAW`/`NUMBER` `RAWTOHEX` 函数 `ROWID` `ROWID`/`UROWID` 类型 `TIMESTAMP TIME ZONE` `UROWID` `VARCHAR2`
- 数据仓库/决策支持系统
- 数据仓库技术
- `数据库块写入器` (`DBWn`)
- 死锁 `COL_CNT` 提交/回滚 `DEPT` 表 外键 有问题的案例 会话 短期发生 源代码 表修改 跟踪文件 未索引的外键
- 专用服务器连接 架构 功能 一对一映射
- 可延迟约束 `CASCADE UPDATE CHILD COMMIT` 语句 `IMMEDIATE` 模式 `NOT NULL` `NULL`s 优化器 索引 源代码 `UNIQUE`/`PRIMARY KEY` 约束 `UPDATE` 命令
- `诊断进程` (`DIAG`)
- 分布式事务
- `数据泵主进程` (`DMnn`)
- 自己动手 (`DIY`) 并行 `DBA_PARALLEL_EXECUTE_CHUNKS` `DBMS_JOB` `DBMS_PARALLEL_EXECUTE` `JOB_QUEUE_PROCESSES` `NTILE` 内置分析函数 `PL/SQL` 块 `PROCESS BIG TABLE` `ROWID` 范围 `SERIAL` 过程 任务移除

## E
- 基于版本的重定义 (`EBR`)
- `企业 JavaBeans` (`EJBs`)
- `实体关系图` (`ERD`)
- `事件监视器进程` (`EMNC`)

## F
- `快速恢复区` (`FRA`)
- 文件类型 警报日志 数据泵 备份/恢复操作 变更跟踪 控制文件 数据库/实例 数据文件 数据泵/平面 处理 闪回日志 平面文件 参数文件 参见 `Parameter files` 密码文件 重做日志 `SPFILE`s 参见 `Server Parameter Files` (`SPFILE`s) 临时数据文件 (临时文件) 跟踪文件
- `细粒度访问控制` (`FGAC`)
- `闪回数据归档进程` (`FBDA`)
- 闪回日志文件
- 平面文件 `CSV` 文件 定义 `HTML` 文件 `JSON` 文件 `SQLLDR`/外部表
- 专注的后台进程 架构 设计 `ARCn` 进程 检查点 数据库实例 数据库块写入器 (`DBWn`) `DIAG` 进程 监视器 闪回数据归档进程 一般任务执行 `init.ora` 参数 `LGWR` 进程 `LMON` 进程 `LREG` 进程 命名约定 不可商量的功能 `PMON` `RAC` 目标 `RECO` 进程 资源管理器 单实例/RAC 实例 `SMON` 系统 源代码 `UNIX`/`Linux` 系统
- 基于函数的索引 大小写不敏感索引 `CREATE INDEX` 语句 `DETERMINISTIC` 关键点 `MY_SOUNDEX` 函数 行 索引 `SCOTT` 模式 选择性 唯一性 实现 `SUBSTR` `TEMPORARY` 列 `TO_DATE` 函数 未索引 *对比* 索引 `UPPER` 值 `UPPER_ENAME` 虚拟列

## G
- 通用任务执行进程 (`GEN0`)
- `全局唯一标识符` (`GUID`)
- `全局临时表` (重做) 概括 `INSERT`/`UPDATE`/`DELETE` `PERM`/`TEMP` 表 备用数据库 临时/永久表 `TEMP_UNDO_ENABLED` 参数
- 全局索引 异步 数据完整性 数据仓库 `DELETE` 和 `INSERT` 操作 快速访问 维护注意事项 `MAXVALUE` 非前缀索引 `OLTP` 系统 `PARTITIONED_IDX_LOCAL` 索引 分区操作 滑动窗口 `TIMESTAMP` 列 `UPDATE GLOBAL INDEXES` 子句

## H
- `哈希聚集表` `ALL_OBJECTS` 特征 集群键 索引 传统索引 `CPU` 利用率 `CREATE CLUSTER` 语句 图形化表示 `HASHED` 实现 `HASHKEYS` 选项 `HEAP`/`HASHED` 索引集群 关键点 一对一映射 `PL/SQL` 代码 模拟 `SINGLE TABLE` 单表哈希集群 `SQL_TRACE`
- `哈希分区` 分而治之 `HASH_KEY_COLUMN` 直方图 数据 插入 分区扩展表 分区位置 (`PNAME`) 表创建 工作流程
- `堆组织表` `COMPRESS`/`NOCOMPRESS` `CREATE TABLE` 语句 `DBMS_METADATA` `PCTFREE`/`INITRANS` 检索数据 空间/磁盘/内存
- 高水位标记 (`HWM`)
- `超文本标记语言` (`HTML`)

## I
- `索引聚集表` 集群键 索引 传统表 `CREATE TABLE` 语句 数据库块 `DBMS_ROWID` 包 `DEPT` 表 `EMP` 记录 图形化表示 正常 存储参数 对象相关数据 根容器 `SIZE` 参数 `TABLESPACE`/`PCTFREE`
- 索引 分区 全局索引 本地/全局索引 本地索引 方法 部分索引
- 索引 应用领域索引 位图索引 黑盒方法 `B*Tree` 参见 `B*Tree` indexes 案例研究 自动索引 字符列 `DBMS_XPLAN` 包 区分元素 `FULL SCANNING INDEXED_COLUMN INDEX UNIQUE SCAN` 优化器 用法 `SELECT COUNT(*) FROM T` 查询 源代码 空间重用 过程 阈值 `TRUNC(DATE_COL)` 常识方法 降序索引 扩展列 创建 基于函数的解决方案 选项 `STANDARD_HASH` 函数 虚拟列解决方案 `WHERE` 子句 外键 基于函数的索引 索引组织表 `INSERT` 语句 不可见 修改 多（列）索引 空条目 反应式调优 反向键 统计信息 类型 视图 工作流程
- `索引组织表` (`IOTs`) `ANALYZE` 命令 `AUTOTRACE` 优势 `BETWEEN` 查询 块 `COMPRESS` 传统堆表 `DBMS_METADATA` 定义 `EMP` 表 `HEAP_ADDRESSES`/`IOT_ADDRESSES` 表 `HOME`/`PREV`/`SCHOOL` `INCLUDING` 子句 `KEYWORDS` 表 逻辑行 id `NOCOMPRESS` 选项 `OPT_CMPR_COUNT` `OPT_CMPR_PCTSAVE` `ORCL` 记录 `OVERFLOW AND INCLUDING` 溢出段 `PCTTHRESHOLD` `PCTUSED` 子句 `PL/SQL` 块 `TABLE ACCESS` 表创建 `WORD`/`WORD`/`POSITION` 列
- 内部锁和门闩
- `进程间通信` (`IPC`)
- `INTERVAL` 类型 `DAY TO SECOND` 定义 `EXTRACT` 内置函数 `LOB` 参见 `Large objects` (`LOB`) `NUMTODSINTERVAL` `NUMTOYMINTERVAL` 函数 `YEAR TO MONTH`
- `间隔和引用分区`
- `间隔分区` `AUDIT_TRAIL` 行 定义 `HIGH_VALUE INTERVAL` 分区 `MAXVALUE` 分区 `NUMTOYMINTERVAL` 规格 范围分区 递归事务 传统方法 `USERS` 表空间

## J, K
- `JavaScript 对象表示法` (`JSON`) 平面文件
- `Java 服务器页面` (`JSPs`)
- `Java 虚拟机` (`JVM`)

## L
- `大对象` (`LOB`) `BasicFiles` `BFILE` 类型 组件 `CHUNK` 子句 `IN ROW` 子句 内部类型 `LOBINDEX`-`LOBSEGMENT` 主键约束 `RETENTION` 子句 `BasicFiles CREATE TABLE` 语句 `LOBSEGMENT` 读一致性 `SecureFiles` `STORAGE` 子句 `SecureFiles` `TABLESPACE` 类型
- `门闩` 原子指令 队列 昂贵操作 内存数据结构 轻量级串行化设备 共享资源 硬编码变量 (绑定变量) 多用户环境 性能/可扩展性比较 字符串连接 (绑定变量) 测试过程 复杂的串行化设备 自旋
- 锁 (`LCK0`) 进程
- 日志写入器 (`LGWR`) 重做/撤销
- `轻量目录访问协议` (`LDAP`)
- `列表分区`
- `锁管理器守护进程` (`LMD0`) 进程
- `锁监视器` (`LMON`) 进程
- `锁管理器服务器` (`LMSn`) 进程
- 本地索引 消除分区 `DBMS_XPLAN.DISPLAY` `LOCAL_NONPREFIXED` `LOCAL_PREFIXED` `PSTART`/`PSTOP` 列 源代码 类型 `UNIQUE`/`PRIMARY KEY` 约束
- 锁定机制 基准 阻塞 聚集表 组件 `DBMS_LOCK` 包 `DDL` 操作 死锁 `DML` 锁 升级 一般类别 问题 关键特征 门闩 参见 `Latches` `LOCK TABLE` 语句 手动锁定/用户定义锁 互斥锁 乐观方法 页级/并发 悲观方法 行级 串行化设备 共享资源 单用户数据库 有状态/连接环境 `Sybase SQL Server` 双重更新
- `监听器注册进程` (`LREG`)

## M
- 内存管理 学习 应用模式 进程 全局区 分配内存 自动管理 控制内存分配 手动管理 含义 `MEMORY_TARGET` `PGA_AGGREGATE_TARGET` `SGA` 参见 `System Global Area` (`SGA`) 用户全局区
- `内存管理器` (`MMAN`)
- 多（列）索引
- `多租户容器数据库` (`CDB`)
- `多版本控制` 账户表 内容 闪回查询 读一致/非阻塞读 数据仓库技术 I/O 热表 系统变更/系统提交号 (`SCN`) 技术方面 时间/操作 事务数据 撤销/回滚段 事务隔离 参见 `Transaction isolation levels` 写一致性
- `互斥锁`

## N
- `国家语言支持` (`NLS`) `ASCII` 数据 字符集转换 控制 `DUMP` 函数 `SQL DUMP` 函数 异构 有损转换 无意字符集转换
- `嵌套表` `CAST` 和 `MULTISET` 语法 `CREATE TABLE` 语句 `DEPT_AND_EMP` 表 `EMP` 和 `DEPT` 数据 等效实现 插入和删除表 `NESTED_TABLE_GET_REFS` 对象关系扩展 父/子关系表 永久存储机制 `PL/SQL` 代码 `SELECT` 命令 存储信息 `CREATE` 语句 `CREATE TABLE` 命令 `DBMS_METADATA.GET_DDL` 索引结构 `NESTED_TABLE_ID` 列 物理实现 `RETURN AS VALUE` 子句 唯一约束 语法
- `NOLOGGING` 操作 `ARCHIVELOG` 模式 `CREATE TABLE` 命令 索引/表 关键点 操作 `SQL` 语句

## O
- `对象表` `ADDRESS_TYPE CREATE TABLE` 语句 数据结构 定义类型 `DESCRIBE` 对象关系组件 `OIDINDEX` 子句 `OID` ‘<大十六进制数>’ 语法 `people.sql` 文件 `PERSON_TYPE` 伪主键 单对象表 `SYS_NC_OID$` 列
- `操作系统` (`OS`)
- `乐观锁定方法` 校验和/哈希函数 方法 数据库功能 目标/概念 `ORA_HASH` 内置函数 客户端/服务器应用 并发控制 实现 `UPDATE` 版本列 工作流程
- Oracle 应用 应用开发 黑盒方法 宽泛陈述 并发控制 游标共享 数据库 以数据库为中心的方法 `DBA`-开发者关系 设计和实现级别 开发 数据库独立性 多版本控制 `PDB1` 源代码 `PL/SQL` 场景 `SQL Server` 架构 `Windows` *对比* `UNIX`/`Linux`

## P, Q
- `并行 DML` (`PDML`) 协调会话/无 提交 数据仓库环境 `DBMS_XPLAN` 功能 独立事务信息 `INSERT`, `UPDATE`, `DELETE`, 和 `MERGE` 并行操作 伪分布式实现 `UPDATE` 语句
- `并行执行` 定义 目标 `PQ` 参见 `Parallel Query` (`PQ`) 从属 过程并行性 `DIY` 过程 管道函数 `PROCESS_DATA` 过程 `SESSION_ID` 类型 `UPDATE`/`INSERT` `V$MYSTAT` 处理类比 一页摘要 十章报告 快速资源
- `并行查询协调器` (`Pnnn`)
- `并行查询` (`PQ`) 从属 `ALTER TABLE` 命令 `BIG_TABLE` `COUNT(STATUS)` 值 `CPU` 机器 执行服务器 并行 `DDL` 并行 `DML` (`PDML`) `PARALLEL_MIN_SERVERS` 初始化参数
- `并行语句排队` (`PSQ`) 物理设备 可插拔数据库 查询协调器 `SID` (`QCSID`) `SID` 会话
- 参数文件 备份和恢复工具 `CONTROL_FILES` 参数 `init.ora` 文件 旧版 `init.ora` 文件 `SCOTT` 模式 `SHOW PARAMETER` 命令 `tnsnames.ora` 文件
- `分区` 管理负担 聚合索引 `ALTER TABLE MOVE` 语句 大量事务 含义 `MOVE` 操作 滑动窗口 表空间 临时资源 优势 减少争用 分而治之逻辑 易于维护 级联 截断 交换 功能 维护操作 `ON DELETE CASCADE` 子句 `TRUNCATE ... CASCADE` 工具函数 增强语句性能 数据仓库 `OLTP` 系统 并行 `DML` (`PDML`) 查询性能 `SELECT`, `INSERT`, `UPDATE`, `DELETE`, `MERGE` 增加可用性 索引 `ORDER BY` 语句 查询性能 段空间 压缩 表和索引 表方案 复合分区 哈希表 间隔分区 间隔引用 分区 列表分区 方法 范围 表引用 分区 行移动 基于时间的数据 虚拟列分区
- 密码文件
- `悲观锁定方法`
- `可插拔数据库` (`PDB`)
- `进程监视器` (`PMON`)
- 预处理 案例 文件系统 `PREPROCESSOR` 指令 读取/过滤 压缩文件 `run_df.bsh` 脚本 修剪字符
- `私有临时表` (重做)
- `过程级原子性` 不同块 `PL/SQL` 存储过程 `RAISE_APPLICATION_ERROR` 存储过程 `WHEN OTHERS` 异常处理器
- 进程 后台 类别 专注进程 含义 源代码 工具进程 广泛类别 服务器 参见 `Server processes` 从属 系统
- `进程（程序）全局区` (`PGA`) 分配内存 `gen_load.sql` 文件 初始化 `LAG()` 函数 指标 观察 `PGA_AGGREGATE_TARGET` 设置 透视功能 `SQL*Plus` 脚本 `stats.sql` 文件 表创建 自动管理 控制内存分配 `CHAR` 数组 专用服务器 `PGA_AGGREGATE_TARGET` 服务器代码 利用率 手动管理 内存管理 `MEMORY_TARGET` `PGA_AGGREGATE_TARGET` `PL/SQL` 表 特定于进程的部分 `SORT_AREA_SIZE`
- `进程生成器` (`PSP0`) 进程

## R
- `范围分区` 错误信息 插入选项 `MAXVALUE` 子句 `RANGE_KEY_COLUMN` `SELECT` 语句
- `范围分区表`
- `再平衡` (`RBAL`) 进程
- `读一致性` 数据仓库技术 I/O 表 非阻塞读 `SERIALIZABLE` 会话
- `真正应用集群` (`RAC`) 实例/数据库 多租户数据库 单租户数据库
- `恢复写入器` (`RVWR`)
- 重做日志文件 `ARCHIVELOG`/`NOARCHIVELOG` 模式 检查点 `DROP TABLE` 语句 实例/介质故障 在线日志文件 峰值处理 恢复目的
- 重做/撤销 归档器 (`ARCn`) 进程 `AUTOTRACE` 块清除 `COMMIT` 参见 `Commit/rollback processing` `DBWn` 进程 数据库写入器 (`DBWR`) `DELETE` 功能 闪回技术 `GET_STAT_VAL` 函数 索引语句 `INSERT` 缓冲区缓存 填充 日志缓冲区 刷新 场景 系统崩溃 日志写入器 (`LGWR`) `LGWR` 后台进程 日志缓冲区内存区域 日志争用 原因 熟文件系统 `LGWR` 和 `ARCn` 日志文件同步事件 日志流 最优日志配置 `RAID-5` 日志文件 日志生成 索引/表 含义 `NOLOGGING` 模式 `SQL` (`NOLOGGING` 子句) 警告信息 (alert.log) `NOARCHIVELOG` 模式 数据库 在线/归档 `RAC` 配置 串行化 临时表 功能 全局表 私有表 事务数据并写入重做 撤销 参见 `Undo` `UPDATE` 应用 回滚 日志文件表示 系统崩溃 工作流程
- `引用分区`, `ORDERS` 表
- 根容器数据库
- `ROWID` 和 `UROWID` 类型
- `行移动` 方案

## S
- `SERIALIZABLE` 事务 数据修改 错误信息 事件 `OLTP` 基准 乐观方法 `SELECT ... FOR UPDATE`
- `服务器参数文件` (`SPFILEs`) `ALTER SYSTEM SET` 命令 转换 `CREATE PFILE...FROM SPFILE` 命令 修复损坏 初始化/参数文件 旧版参数文件 命名约定 可插拔数据库 取消设置 值 值设置
- 服务器进程 连接模式 连接类型 连接 *对比* 会话 `AUTOTRACE` 命令 `CONNECT` 命令 `DBMS_LOCK` 命令 定义 `DISCONNECT` 命令 `DML` 操作 网络 `PADDR` 服务器参数 物理连接 可插拔数据库 `PROGRAM` 信息 `SCOTT` 共享服务器 会话 `tnsnames.ora` 文件 用户账户 和 查询 专用服务器模式 共享服务器连接 工作马
- `共享服务器连接` 架构 人工死锁 优势 并发用户 *对比* 每秒事务数 数据库链接 集中 功能 多对一关系 内存系统 `OLTP` 系统 操作系统 进程/线程
- `站点标识符` (`SID`)
- `单客户端访问名称` (`SCAN`)
- `单租户数据库` 架构
- 从属进程 I/O 系统/设备 并行查询执行 (`Pnnn`) 参数 控制 类型
- `空间管理协调器` (`SMCO`)
- `系统监视器` (`SMON`)
- `排序哈希集群`
- `SQLLDR` 访问 参数 `APPEND`, `TRUNCATE`/`REPLACE` `CHAR(255)` 命令行覆盖 控制文件 控制文件 `demo1.ctl` `DEPT` 表 嵌入换行符 字符选择 `FIX` 属性 自由格式 数据 四舍五入 `STR` 属性 `VAR` 属性 快速模式 `CREATE ANY DIRECTORY` `dept.dat` 文件 `dept.log` 文件 直接路径方法 `INSERT` 语句 `SQL` 表 表创建 临时表/目录对象 外部表 关键功能 大对象 (`LOBs`) 复杂辅助数据文件 `DBMS_LOB` 包 `DIRECTORY` 对象 文件结束 (`EOF`) 错误信息 内联函数 `LoadCLOBFromFile` 加载数据 行 `LONG`/`LONG RAW` 字段 对象列 `PL/SQL` 过程 辅助数据文件 (`SDFs`) 加载数据 逗号分隔值 (`CSV`) 格式 日期 定界数据 嵌入换行符 `ENTIRE_LINE` 列 `FIELDS TERMINATED BY` 子句 `FILLER` 关键字 函数 (数据库序列) 加载固定宽度 定位数据 日志文件 `POSITION` 关键字 制表符分隔数据 `TERMINATED BY WHITESPACE` 子句 操作模式 `Oracle 调用接口` (`OCI`) `ORGANIZATION EXTERNAL` 子句 `REUSE STORAGE` 选项 四舍五入 存储过程 `TRUNCATE` 选项 `TRUNCATE SQL` 命令
- `语句级原子性`
- `结构化查询语言` (`SQL`) 绑定变量 方法 `AUTONOMOUS_TRANSACTION` `CPU` 时间 `CREATE PROCEDURE` 易出错的 邪恶 开发者/用户 授予权限 硬/软解析 注入 门闩 资源密集度较低 恶意代码 `NLS_DATE_FORMAT` 解析 唯一语句 占位符 查询 `PL/SQL` 过程 `PWD_MGR` 可扩展性 通过隐匿性实现安全 `SELECT` 权限 串行化设备 小过程 存储过程 字符串连接
- `系统全局区` (`SGA`) 联合 `USER_PW` 表 `NOLOGGING` 子句 后果 单一连接
- `系统管理员` (`SA`), 信息系统
- `系统全局区` (`SGA`) 缓冲区缓存 块管理 `DBA_OBJECTS` 视图 `DUMMY` 列 多块大小 回收池 `SGA_MAX_SIZE` 参数 存储缓存块 数据库块 缓冲区缓存 热段 块 固定组件 粒度 内存中 功能 `Java` 池 大池 内存组件 内存管理 `AMM` 建议 自动内存管理 建议 共享管理 共享内存区域 `MEMORY_TARGET` 参数 重做缓冲区 日志 `SGASTAT` 共享内存结构 共享池 绑定变量 程序 数据 `SHARED_POOL_SIZE` 标准操作程序 唯一查询 源代码 流池 `UNIX`/`Linux` 各种池
- `系统全局区` (`SGA`) 后台进程 内存组件

## T
- `事务` (`TX`) 锁 `AUTONOMOUS_TRANSACTION` 字符串 数据字典 数据文件号 数据修改 `DEPT` 更新 `EMP` 和 `DEPT` 表 `INITRANS` 关键点 锁 查询 `MAXTRANS` 物理属性 参数 存储过程 `V$LOCK` `V$TRANSACTION`
- `临时数据文件` (临时文件)
- `临时表` `CREATE TABLE AS SELECT` (`CTAS`) 全局表 属性/特征 基于成本的优化器 (`CBO`) `DELETE`/`TRUNCATE` 差异 直接路径操作 缺点 `INLINE VIEW ON COMMIT DELETE ROWS` 手掌同步应用 `PL/SQL` `SCOTT.EMP` 表 基于会话的行 会话级 共享统计信息 统计信息 表空间 `TEMP` 表 `TEMPORARY` 和 `DURATION` 列 事务/会话 `USER_TAB_STATISTICS` 私有表 `COMMIT`/`ROLLBACK` 定义 功能 `ON COMMIT` 子句 `PRIVATE_TEMP_TABLE_PREFIX` 会话/事务 类型
- 术语 字符大对象 (`CLOB`) `CREATE TABLE` 语句 高水位标记 (`HWM`) 索引分区 `INITRANS` `LOGGING`/`NOLOGGING` 管理空间 `PCTFREE` 参数 合并自由空间 数据块 定义 迁移行 描述 行迁移 值 段类型
- `TIMESTAMP` 类型 加/减 时间 组件 定义 差异 `DUMP` 函数 `LOCAL TIME ZONE` `NUMTOYMINTERVAL` `TIME ZONE` 类型
- `传输监视器进程` (`TMON`)
- 跟踪文件 命令行工具 调试信息 诊断/性能相关信息 一般类型 插桩 内部错误位置 命名约定 服务请求 (`SR`) 标记文件 类型
- `事务隔离级别` `ANSI` 隔离级别 定义 脏读/不可重复读 幻读 `READ COMMITTED` `READ COMMITTED`/`SERIALIZABLE` `READ ONLY` 事务 `READ UNCOMMITTED` `REPEATABLE READ` 目标 丢失更新 防止 非 Oracle 数据库 共享读锁 `REPEATABLE READ` 和 `SERIALIZABLE` 级别 回滚段 `SERIALIZABLE` 事务
- `事务` `ACID` 特征 原子性 自动 不良习惯 `COMMIT` 语句 控制语句 分布式 持久性 `ALL_OBJECTS` 案例 `COMMIT` 语句 `PL/SQL` 非分布式 块 `WRITE` 扩展 文件系统 完整性约束 `DEFERRABLE` `IMMEDIATE` 模式 `ROLLBACK TO SAVEPOINT` 命令 `ROLLBACK WORK` `SAVEPOINT` `SET TRANSACTION` 事务级原子性
- `事务` (`TX`) 锁 `COMMIT`/`ROLLBACK` 并发控制 `ID` 表示 基于内存的锁管理器 `V$` 表
- `重做传输从属` (`TT00`)

## U
- `撤销` 段 自动管理 块清除 组件 定义 空表创建 错误信息 高水位标记 (`HWM`) 索引 `INSERT` 语句 `INSERT`/`UPDATE`/`DELETE` I/O 表 最长运行查询 长时间运行进程 手动管理 误解 修改 `ORA-01555` 错误信息 `PL/SQL` 过程 `ROLLBACK` 语句 序列 空间分配 步骤 表空间 事务地址 (`TADDR`) `UNDO_RETENTION` 设置 `UPDATE` 选项 工作流程
- `用户全局区` (`UGA`) 参见 `Process (Program) Global Area` (`PGA`)
- `工具后台进程` `CTWR` 进程 `DMnn`/工作进程 `EMNC` 进程 功能 作业队列进程 可管理性监视器 `MMAN` 进程 `QMNC`/`Qnnn` 进程 恢复写入器 传输监视器进程/重做传输从属

## V
- `虚拟列分区`
- `资源管理器虚拟调度器` (`VKRM`)
- `虚拟时间守护者` (`VKTM`) 进程

## W, X, Y, Z
- `写一致性` 一致/当前读 非事务性触发器 读 参见 `Read consistency` 重启选项 `TKPROF` 工具 `UPDATE` 语句 `WHERE` 子句
