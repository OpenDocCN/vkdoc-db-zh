# 关于 `ARCHIVELOG` 模式

不要让任何人劝你放弃 `ARCHIVELOG` 模式。你花了很长时间开发你的应用程序，因此你希望人们信任它。丢失他们的数据不会增强对你系统的信心。

> 注意
> 在某些情况下，一个大型数据仓库（DW）可以证明使用 `NOARCHIVELOG` 模式是合理的——前提是它明智地使用了 `READ ONLY` 表空间，并且愿意在发生故障时，通过重新加载数据来完全重建任何 `READ WRITE` 表空间。

